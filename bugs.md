# YancoTV — Bug Register

Living register of **open** bugs across desktop and mobile. Closed bugs move to the changelog; incident reports (deep post-mortems or one-off refactor bug dumps) go to [docs/incidents/](docs/incidents/).

**Last updated:** 2026-08-19 (**MB-349 — CI had been running zero tests since 2026-07-25.** Found by watching the first run after pushing 71 commits: MB-295's release-signing check threw at Gradle *configuration* time, which takes down every task in the build rather than just release ones, so ktlint / migrations / both unit suites / assembleDebug / the iOS job all died before doing any work on every keystore-less runner. Three weeks of pushes landed with no test signal while this register listed CI as a closed release gate. Guard relocated to `gradle.taskGraph.whenReady` with exact-name task matching; MB-295's rule is unchanged, only its blast radius. Also folded the stale `claude/intelligent-bohr-494fa8` audit branch into master and deleted it — one trunk now.) (**Fire TV device-verification session, 2026-08-19.** MB-343 verified end-to-end on AFTDCT31 against a real 272k-item Xtream catalog: the up-next card names the correct next episode with the countdown correctly suppressed while `autoPlayNext` is off, and the dock's NEXT button advances S1E1 → S1E2 → S1E3 re-resolving its target each time. MB-230 also held — a **272,419-item** sync (53,167 live / 174,000 VOD / 45,252 series) ran flat at 12-13 MB of a 24 MB heap with `0(0B) LOS objects` and zero OOM, far harsher than the conditions the bug originally reproduced under; MB-299's EPG-after-catalog ordering confirmed in the same log. Three bugs came out of the session: **MB-346** (the dock badged EVERY episode "MOVIE") and **MB-347** (type labels hardcoded English in search + coverflow — invisible to lint `MissingTranslation`, which is how they survived MK.31's 1024-key pass), both fixed and device-verified, plus **MB-348** filed for two further sites that need a copy/brand decision rather than a mechanical fix. **MB-345 re-rated Low → Medium and fixed**: it blocked the MB-343 verification twice within the hour despite two refuting agents having judged it not a defect — a refutation that assumes an expert user is not a refutation for a TV remote.) (**MK.31.4 + MK.33 shipped 2026-07-31/08-01.** Full `<plurals>` support landed (MB-339) — Arabic counts were grammatically wrong for 2-10 app-wide until now. Multi-playlist source-scoped categories (MK.33.1), destructive-action confirmation (MB-335), the five-cause scroll cut-off (MB-336) and seek acceleration (MB-338) all shipped. MB-342 is the third `java.time`-on-minSdk-24 crash in this project; lint's NewApi was again the only gate that caught it.) (**MK.26 Cast-to-TV + MK.27 phone-UX shipped 2026-06-15.** MK.26 Track A (LAN handoff: phone → YancoTV-on-TV) + Track B (Google Cast via on-device ffmpeg `CastProxy`) landed; the **R8 release blocker** they introduced is fixed — the embedded Ktor server pulls in `java.lang.management.*` (JVM-only), so `proguard-rules.pro` now `-dontwarn`s those classes (`:app:minifyReleaseWithR8` was aborting the release build). MK.27 phone-UX pass: single-pane Settings master/detail on phones (`MB-232` SettingsSlider negative-padding crash fixed), touch-to-show player chrome on LIVE + VOD (`MB-231`), on-screen back/cast/more buttons, cast stuck-menu full-close (`MB-233`), selector black-line artifact removed. Release-polish batch: cast-failure toasts, receiver `startForeground` guarded, `SecureRandom` pairing code, version → 1.2.0. **Release gate:** `MB-230`/`MB-229` Critical heap items still Open — run a 1+ hour Fire TV soak with the new ffmpeg cast proxy before tagging. Earlier 2026-04-28: **Stage 5.2.1 shipped 2026-04-28** — `2202460`. `UpdateChecker` pure-logic poller in `:shared/commonMain` + 11 table-driven tests. First slice of Stage 5.2 sideload auto-update check; 5.2.2 (WorkManager + Settings toggle) and 5.2.3 (UI prompt + APK install) pending. **MK.20 polish-sweep complete 2026-04-28** — pin-a-bucket shipped `1b875bf`: per-type `pinnedParentsFlow`, `CategoryTreeBuilder.build` extended with `pinnedParentCodes`, "Pinned categories" UI section in `SettingsGroupsTab`, callers in `BrowseSection` + `GuideScreen` wired, 9 new tree-builder tests. Multi-word region parsing + 10 missing 2-letter codes shipped earlier same day in `5eef8a0`. Hands-on Fire TV verify deferred to user's MB-229/MB-230 soak session. **MK.24 complete — Sprint G.2 shipped 2026-04-28** — `92bfafc`. Pure-function extraction (`decideRecoveryAction` + `dbArtifactPaths`) + 9 tests including end-to-end "corrupt file on disk → JdbcSqliteDriver fails → cleanup → fresh DB queryable" integration test. Closes MB-228. **All MK.24 work shipped: H + E + I + F + G.1 + G.2.** Remaining MK.24 follow-ups (24.I.X.1–6) are heap-pressure soak items, picked up only if F3 Sentry probe fires in the wild. **MK.24 Sprint G.1 shipped 2026-04-28** — `bf12d65`. 5 per-hop migration isolation tests for 3.sqm…7.sqm in `MigrationTest.kt` (582 lines added). Closes MB-227. All shared tests green; only G.2 (DB corruption-recovery integration test) remains in MK.24. **MB-224 closed 2026-04-28** — `.github/workflows/android-tests.yml` runs `:shared:testDebugUnitTest`, `:app:testDebugUnitTest`, `:app:assembleDebug` on every push to `master` + PR; gradlew exec bit fixed in git tracking + workflow `chmod +x` belt-and-suspenders; Linux runner re-enables SQLDelight build-time migration verification (`:shared:verifyCommonMainYancoDbMigration`) which is disabled on Windows hosts. **MK.24 Sprint F closed by audit 2026-04-28** — F.1 (`CategoryRail` debounce) verified non-bug after read-the-file gate (callers write to stable `MutableState`, fresh-lambda is harmless); F.2 motion-polish backlog audit found zero items (MK.22.A/B already shipped every relevant fix). Zero commits, ~30 min total — saved 1–2 h of speculative wrapping. **Two sprints remain: G.1 + G.2**. **MK.24 Sprint I shipped 2026-04-28** — 2 commits, `f9d966f` (F2 shared OkHttp) + `a71edfe` (F1 onTrimMemory + F3 heap-watermark Sentry probe). Verified on Fire TV: post-fix `am send-trim-memory RUNNING_CRITICAL` releases -3 MB Java (vs pre-fix -0.5 MB, 6× the reclaim). MB-230 status flipped to "Open · first-fix shipped, awaiting soak verification" — the heap-watermark probe will fire in Sentry on the next wild occurrence and tell us whether a deeper investigation (Sprint I follow-up 24.I.X.1, high-heap hprof dominator analysis) is needed. **MB-229** still blocked on MB-230 — retest scheduled after 1+ hour soak.) Earlier 2026-04-28: (**MB-230 Critical filed + Sprint I investigation through I.6 done**. YancoTV process exhausts heap (376/384 MB, 98%) and enters runaway GC after sustained use, causing every reactive flow + DB write + file write to silently stall. Captured live on Fire TV during user's recording attempt: 101 consecutive `GC freed 0(0B)` events, threads blocked 12+ s on `WaitForGcToComplete`, `am dumpheap` returned 0-byte file because process couldn't respond. Investigation ruled out EPG sync (well-designed streaming importer), content/EPG paging (all queries use `*Paged`), background-timer leaks (idle heap stable at 12 MB for 90s), single-screen retention (all main screens 18-22 MB Java). Confirmed gaps: missing `onTrimMemory` hook in YancoApp + Coil bitmap cache doesn't honor pressure signals + duplicate OkHttp instance for Coil. Diagnosis: "1000 cuts" leak compounding past 384 MB heap budget over long session. Fix in flight: F1 `onTrimMemory` hook + F2 shared OkHttp + F3 heap-watermark Sentry probe. 6 deferred items tracked as Sprint I follow-ups (high-heap hprof, RGB_565 logos, EPG-sync deep-dive, MB-229 retest, Ktor pool audit, Compose retention audit). **MB-229 demoted** from independent High to "blocked on MB-230" — the "recording AND failed" UI is a downstream symptom of memory exhaustion, not a recording-subsystem race. **MK.24 Sprint I refocused** to heap-pressure investigation (was: recording-subsystem). **MK.24 Sprints H + E shipped 2026-04-28** in [PRODUCTION_PLAN_NATIVE.md](PRODUCTION_PLAN_NATIVE.md). 4 commits (`d390eff` → `ae56b4d`): MB-226 closed via load-bearing schema comment in `Reminders.sq` (read-the-file gate retargeted from a low-value test); MB-225 closed via `playLaunchDecision` pure-function extraction with 14-case test (read-the-file gate confirmed both call-site AND controller-level guards exist); SourceSyncCoordinator lifecycle teardown tests cover both DONE and exception paths; mid-chunk syncSource cancellation test pins abortSource ran + FK back ON after partial writes. MB-227 + MB-228 remain Open · planned for Sprint G. Earlier 2026-04-28: **MK.24 — Audit follow-ups planned across 4 sprints (E/F/G/H)**. MB-225 / MB-226 / MB-227 / MB-228 filed for the test gaps the Sprint A/B/C/D verification audits surfaced — `PlaybackController` two-tap unpinned, `reminders.programme_id` design pin missing, per-migration isolation for `3.sqm`–`8.sqm`, DB driver corruption-recovery integration. Earlier 2026-04-28: MK.22 Sprint A + B motion fixes shipped — closed MB-221 (sidebar 120 ms delay + spring tail + per-row `AnimatedVisibility`) + MB-222 (`OnNowTile.nowSec` frozen). MK.23 Sprint C + D test hardening shipped — 10 new tests covering PlaybackController.persistResumePoint, BulkContentWriter.abortSource cross-source FK survival, FavoritesRepository multi-list, finishSource failure path, SourceSyncCoordinator re-entrancy, syncSource cancellation, WatchHistory orphan rows, EPG re-sync vs schedule SET NULL, v9→v10 migration, schedule.recording_id SET NULL. MB-224 filed Open — no CI runs any test suite, which is what hid MB-223's compile breakage for 24 h. **Pre-2026-04-28 day-of:** MB-220 + MB-223 filed + fixed — every source sync was silently wiping favorites + watch_history via FK CASCADE on `content.id`, plus 10 shared/ test files broken by f157250's `auto_sync_on_start` column add. **Pre-2026-04-28:** MB-219 filed + fixed — boot-recovery sweep was losing fully-flushed recordings to FAILED when process death interrupted the status flip; MB-211 → MB-218 filed from a deep audit of the recording subsystem after MK.19.8 backup engine landed — dead FK column, handleEnd race, test-coverage gaps, orphan recovery, AutoCloseable refactor, imported-recording file existence)
**Format:** ID · Platform · Severity · One-line · Status · First seen · Fix target

> **RN app frozen 2026-04-20.** All open `MB-*` bugs targeting React Native milestones are **Deferred to native rewrite** — they'll be resolved by construction in the Kotlin/KMP app. See [PRODUCTION_PLAN_NATIVE.md](PRODUCTION_PLAN_NATIVE.md). RN bugs remain in the register for historical reference; do not attempt RN fixes.

| ID | Platform | Sev | Summary | Status | First seen | Fix target |
|---|---|---|---|---|---|---|
| MB-359 | Native | Medium | **Six hardcoded English strings in the LIVE player chrome — MK.31 shipped "full i18n" with the player layout still untranslated.** `activity_player.xml` carried literal `android:text` for `LIVE`, `STREAM INFO`, `INFO to dismiss`, `JUMP TO LIVE`, `Casting` and `Stop casting`. The layout is live, not dead: `PlayerActivity:597` calls `setContentView(R.layout.activity_player)` and hosts the Compose surfaces inside it via `ComposeView`/`ViewStub`, so these render on screen during ordinary live playback. On an Arabic device the player therefore showed Latin-script English labels beside a fully translated UI — visible on the user's own Google TV, which runs Arabic. | **Fixed** | 2026-08-22 | Found by auditing what the lint baseline was hiding rather than by running lint alone: `HardcodedText` ×4 sat inside `lint-baseline.xml`, filtered from every run and therefore invisible. The baseline knew about 4; two more (`Casting`, `Stop casting`) had been added since it was generated, which is the standing hazard of a stale baseline. The companion 11 `SetTextI18n` entries in the same baseline are genuinely stale — `PlayerActivity` no longer calls `setText` at all after the Compose rewrite. Fix: `LIVE` reuses the existing `badge_live`; five new keys (`pl_stream_info`, `pl_info_dismiss`, `pl_jump_to_live`, `pl_casting`, `pl_stop_casting`) added across all four locales (en/ar/fr/es). **Translations are machine-produced and want a native check** — particularly `pl_info_dismiss`, which references the remote's INFO key by name and reads awkwardly if translated literally. |
| MB-360 | Native | **High** | **The Guide renders channel rows but NO programme cells at all — an empty grid over a database holding 179,593 programmes.** Observed on Fire TV 2026-08-23 00:05: the guide header reports `Showing 100 of 2907 channels`, the channel column lists rows normally (`beIN BARAEM`, `beIN GOURMET`, `beIN JEEM`, …), the time axis renders (`Now`, `02:00`) — and the entire programme band right of the channel column is empty. No errors logged, persists across a fresh entry into the screen and 25+ seconds of settle time. | **Open · mechanism unknown** | 2026-08-23 | **CORRECTION to this entry's first version.** It originally claimed the Guide was paging 100 channels that simply have no EPG, with no EPG-aware ordering. **That diagnosis is wrong and is ruled out by the code**: `guideChannelsAllPaged` already requires `EXISTS (SELECT 1 FROM epg_programmes ep WHERE ep.channel_tvg_id = c.tvg_id AND ep.end_time > :startTime AND ep.start_time < :endTime)`, so every channel it returns provably HAS a programme overlapping the visible window. The 2907 count is itself the count of EPG-bearing channels. Filed on a dump-filter artifact (`x >= 900` excluded the channel column at x=768) before that query was read — recorded here rather than quietly rewritten. **What is actually ruled out:** "these channels have no EPG" (the query forbids it); a load race (unchanged after 25s and a fresh screen entry); and a window mismatch between the two queries — `forChannelsRange` uses the identical overlap predicate `end_time > ? AND start_time < ?` and the same `startTime`/`endTime` values as the channel query, so the two cannot disagree about which programmes are in range. **Still unexplained:** why the fetched programmes do not reach the grid. Note `forChannelsRange` carries a cross-source dedup `NOT EXISTS` clause keyed on `(channel_tvg_id, start_time)` and source `epg_priority` — that is the one part of the path that can drop rows the channel query counted, and it is the first place to look. **Possibly transient, and this matters before anyone spends a day on it:** a catalogue re-sync was in flight throughout (`Re-syncing yaman…` on screen, heavy GC in logcat, 148 MB freed in one pass). `BulkContentWriter` deletes and re-inserts the source's `content` rows during a sync, so the tvg_id set the guide joins against is being rewritten underneath it. **Re-check once the sync completes before treating this as a standing bug** — the same screen at ~18:00 the same day rendered programmes normally. **Impact if it is not transient: severe.** Arming a scheduled recording is only reachable from a Guide programme cell, so an empty grid means the recording feature has no entry point at all — which is exactly how this blocked MB-355's device verification. |
| MB-357 | Native | Medium | **`@UnstableApi` is used where `@OptIn(UnstableApi::class)` was meant — 64 declarations, zero correct uses.** The two annotations mean opposite things. `@UnstableApi` on our own class declares *that class* to be unstable API and **propagates** the opt-in requirement to every caller; `@OptIn(UnstableApi::class)` **consumes** media3's requirement at the usage site, which is what an application module wants. Nothing is broken at runtime — both are compile-time only — but the codebase is annotating its own classes as a public unstable API surface that nothing consumes, and the requirement leaks outward. It surfaces as 4 `UnsafeOptInUsageError` lint errors at the boundary where a non-annotated file merely *references* an annotated class (`Intent(context, MainActivity::class.java)` in `RecommendationsSync.deepLinkFor` and `UpdateNotifier.build`, plus `PlaybackController.kt:1343`). Only 4 show because most callers are themselves `@UnstableApi`, so the requirement is satisfied transitively — the count understates the problem. | **Open** | 2026-08-22 | Found by the 2026-08-22 lint sweep (the first time Android lint had ever been run to completion on this repo). Fix is mechanical — swap the 64 `@UnstableApi` for `@OptIn(UnstableApi::class)` — and low-risk, since neither annotation has any runtime effect and the 1,150-test suite plus compile would catch a mistake. **Deliberately NOT done in the release-prep pass**: it touches 64 files for zero user-visible benefit, and wide mechanical churn immediately before cutting a build is how avoidable regressions get in. Do it as its own commit after the release, then the 4 errors disappear without suppression. |
| MB-358 | Mobile (frozen) | Low | **A Sentry DSN is hardcoded in source at `packages/mobile/src/sentry.ts:4`, and the repository is now public.** Added by `2ad3fad` when `packages/mobile/` was still active; that package has been frozen since 2026-04-20 and is never built or shipped, so the DSN is dead code that is nonetheless world-readable since the repo was made public on 2026-08-22 to unblock CI. | **Open** | 2026-08-22 | **Severity is genuinely Low and the reasoning matters:** a Sentry DSN is an ingest key designed to be embedded in client builds — every shipped app exposes one — so this is not a credential leak in the sense AGENTS.md rule 3 covers. The realistic risk is a stranger posting junk events to that project and burning its quota, not data exposure. It is still worth closing because it contradicts our own convention: the native app deliberately keeps its DSN in `packages/android/local.properties`, out of source, and this is the one place that does not. Two options: delete the constant from the frozen file (cheapest — nothing builds it), or rotate the DSN in Sentry if quota abuse is a concern. **Verified clean in the same sweep:** no keystore, credential or token is tracked or has ever been committed. `local.properties` appears once in history at **0 bytes**, and the only committed keystore is `packages/mobile/android/app/debug.keystore`, which is the standard publicly-known Android debug key (`androiddebugkey`, `CN=Android Debug`) shipped with the SDK. `backups/*.json`, which does hold plaintext provider credentials, is correctly gitignored and untracked. |
| MB-337 | Native | **High** | **The only 1-stream recording guard fails OPEN — a DB read error is indistinguishable from "no recording in flight".** `RecordingScheduleReceiver.kt:186-189` does `runCatching { recordings.getByStatus(RECORDING) }.getOrDefault(emptyList())`. Any read failure therefore looks exactly like an idle recorder, so the receiver takes the else branch, calls `playbackController.play()` to switch the player OFF the channel currently being recorded, and starts a second recording. `grep -c getByStatus RecordingService.kt` returns **0** — the service never re-checks the cap, so this receiver check is the *only* concurrency guard in the system. Outcome: a corrupted recording file plus a provider connection rejection (most IPTV accounts cap concurrent streams), silently. Found by reading during the 2026-07-31 session-planning survey; verified in source, not observed on device. | **Fixed (code) · awaiting device verification** | 2026-07-31 | **User chose fail-closed with the notification (2026-07-31).** The read now yields `null` on failure instead of `emptyList()`, and the routing moved into a pure `preFireRoute(sameChannel, activeRecordingCount, currentUrlPresent)` (`PreFireRoute.kt`) so every branch is table-tested — the same extract-the-decision playbook as `playLaunchDecision` / `tryStartOrFailSchedule`. `null` routes to the new `MISSED_STATE_UNREADABLE`, marking MISSED with the new `REASON_RECORDING_STATE_UNREADABLE` (kept distinct from `REASON_CONCURRENT_RECORDING_ACTIVE` so a broken database is not disguised as ordinary cap contention) and posting the failure notification. `sameChannel` deliberately still outranks the null check: teeing off an already-open connection cannot breach the 1-stream cap, so refusing there would skip a recording for no benefit. **Also fixed in the same pass:** the concurrent path marked MISSED *silently* — same class of harm, so both MISSED paths now notify via `markMissedAndNotify`, whose transition is wrapped so a failing DB write cannot suppress the notification (in the unreadable case the DB is the broken thing, making the notification the only channel left). Negative control run: reverting to `activeRecordingCount ?: 0` turns `unreadable recording state fails CLOSED…`, `unreadable is never confused with an idle recorder` and the exhaustiveness test RED. 9 new tests, 970 total. Device verification needs a *forced* read failure — the fail-closed path cannot be reached by normal operation. |
| MB-338 | Native | Medium | **Holding D-pad LEFT/RIGHT scrubs unpredictably — no `repeatCount` guard on the seek path.** `PlayerActivity.kt:2412-2461` (VOD and timeshift branches) calls `seekTo(±10s)` on every key-down with no auto-repeat filter; `repeatCount` appears only at lines 2239 and 2266, both for the CENTER long-press. So the OS auto-repeat fires one discrete 10-second seek per tick: a two-second hold jumps a minute or more in uneven steps instead of accelerating smoothly. Every VOD and timeshift session goes through this path, and it is the most-used control on the remote. Verified in source. | **Fixed** | 2026-07-31 | `f741c58` + earlier. `SeekAccelerator` kernel (10s -> 30s -> 60s -> 5min by hold duration), single `commitSeek` commit point, `armSeekHold` / `endSeekHold` torn down in `onPause` / `onStop` / PiP. Badge reports the APPLIED delta so it stops growing once the seek clamps at either end. |
| MB-339 | Native | Medium | **No `<plurals>` resources exist anywhere, so every Arabic count string is grammatically wrong for 2-10.** Verified: zero `<plurals>` elements and zero `getQuantityString` / `pluralStringResource` call sites app-wide across all four locales. 74 translatable strings carry `%d`; roughly 25-30 are true counts. Arabic requires six CLDR categories (zero/one/two/few/many/other) and singular-for-everything is outright wrong in the 2-10 range, which is the commonest in practice. Worst instance is self-inflicted: `se_soon_full_many` renders `بقي %2$d يوم` — singular "day" — in the branch that exists *specifically* for 2-10 days. Ships alongside 8 confirmed mistranslations (`pa_buffering` reads "saving in progress"; `rec_release_folder` reads "edit" not "relinquish"; `pb_buffer_profile` reads "buffer file"; `voice_input` duplicates the Audio label; plus `ca_rename_body`, `par_restored_count` and inconsistent breadcrumb arrows). Found by a full-file Arabic audit (every one of ~985 keys compared), spot-verified independently. | **Fixed** | 2026-07-31 | `d7adb85` (5 Arabic mistranslations + 9 RTL arrows) + `9364267` (23 `<plurals>` x 4 locales, 24 flat strings retired, 13 call sites). Arabic carries all six CLDR categories with real morphology. Proof is `PluralResourceParityTest` — 9 cases, negative-controlled twice, emits the resolved matrix to `build/reports/plurals/plural-matrix.txt`. |
| MB-334 | Native | Medium | **`AppLanguage.englishName` was dead, and its KDoc claimed it was wired to accessibility labels.** No reader existed outside the enum declaration, so the comment documented behaviour the app did not have. `ui/HelloScreen.kt` was likewise leftover pivot scaffolding with no call site. | **Fixed** | 2026-07-31 | Wired `englishName` into the language picker's `contentDescription` so a TV reader announces the English gloss beside the endonym; `HelloScreen.kt` deleted. The endonyms themselves stay untranslated on purpose (MK.31.19-31.25) — the picker is the one screen a user may reach while the app is in a language they cannot read. |
| MB-335 | Native | **High** | **No confirmation on destructive actions — a single stray D-pad CENTER deleted a source and its entire content tree.** Found the hard way: a blind CENTER during device probing destroyed a real 278k-item source with no undo and no dialog. Source deletion cascades to `content`, `epg_programmes` and `watch_history`, so the only recovery is re-adding the provider credentials and re-syncing. | **Fixed** | 2026-07-31 | `ConfirmDangerDialog` gates source deletion and the backup-registry delete. Initial focus lands on **Cancel**, so the failure mode of a stray double-press is dismissal rather than destruction. |
| MB-336 | Native | Medium | **Scrolling down then back up left the top of the screen cut off, intermittently, across multiple settings tabs.** Five distinct causes, not one: the snap threshold (140dp) was shorter than the tallest header; the snap fired on any small offset rather than only on upward travel; it fought the IME when a text field was focused; the settled-value baseline initialised to 0; and — the reason it was intermittent — a competing scroll cancelled `animateScrollTo` with an uncaught `CancellationException`, which killed the `snapshotFlow` collector permanently for the life of the composition. | **Fixed** | 2026-07-31 | `FocusScrollSpec`: threshold 200dp, `shouldSnapToTop` requires `scrollValue < previousSettledValue`, `imeVisible` gate via `rememberUpdatedState`, baseline seeded from `scrollState.value`, and the animate call wrapped so a cancellation rethrows only when the scope itself is gone. Still ScrollState-only, so no LazyColumn surface can adopt it yet. |
| MB-340 | Native | Medium | **The VOD dock showed elapsed time only — no remaining, no finish time — and a series episode gave no way to reach the next one without leaving the player.** | **Fixed** | 2026-07-31 | `DockTimeLabels` adds `-MM:SS` remaining plus a wall-clock `vd_ends_at`, DST-correct via a real calendar rather than epoch arithmetic. `EpisodeKicker` renders the next episode's `S1E2` label, matched unpadded against `ContentDetailScreen.resumeButtonLabel`. |
| MB-341 | Native | Medium | **The seek badge kept counting up after the seek had clamped, and blinked during auto-repeat's initial delay.** The badge rendered the *requested* delta, so holding RIGHT at the end of a file showed +90s, +100s, +110s while playback sat still at the duration. | **Fixed** | 2026-07-31 | `f741c58`. `commitSeek` reports the delta actually applied after clamping, and the hide timer is hold-aware so it no longer expires inside the auto-repeat gap. `seekFlashJob` cancelled in `onStop`. |
| MB-342 | Native | **High** | **`DockTimeLabels` used `java.time` (API 26) on minSdk 24 — `NoClassDefFoundError` on Fire OS 6 the moment a VOD dock rendered.** Self-inflicted, in the same slice. **Third occurrence of this exact class in the project** after MB-241 and MB-294. The release build and 1004 unit tests both passed; only lint's NewApi caught it. | **Fixed** | 2026-07-31 | `f741c58`. `java.util.Calendar` + `TimeZone` (API 1), equally DST-aware since the TimeZone carries the transition rules — the same remedy MB-294 used. All 13 `DockTimeLabelsTest` cases pass unchanged, including the Europe/Berlin fall-back case chosen because naive epoch arithmetic gives 01:00 there where the correct answer is 02:00 CET. **Standing lesson: `lintDebug` is the only gate that catches this, so it must run before any commit that formats a date or time.** |
| MB-343 | Native | Medium | **Autoplay-next fired from `STATE_ENDED` with no warning and no way to jump early**, so a binge either lurched into the next episode or the user sat through credits. Compounding it, the VOD dock's `›` NEXT transport button — shipped in MK.16.player.vod.dock specifically to go to the next thing — has been **dead for every episode** since: `PlaybackController.play(episode)` synthesises a ONE-item queue and the button gated on `queue.size > 1`. The dock's own comment recorded this as interim, "until sibling-episode loading lands as a follow-up MK". | **Fixed (code) · awaiting device verification** | 2026-08-01 | `f984bcc`. Two halves. (1) `UpNextOverlay` — a non-focusable card in the last 20s naming the next episode; no focus target, no key binding, absent from `noOverlay`, advances nothing (each recorded as a decision in its KDoc). Timing in `UpNextDecision`, a pure kernel with 16 tests; hysteresis because MB-338's hold reaches 300s per tick and one gesture would otherwise strobe a single threshold. **Zero new coroutines** — it rides the existing 1 Hz live-offset ticker rather than adding a tenth entry to an onStop cancel list that has already drifted three times. (2) `hasNext` split from `hasSiblings` so the dock's NEXT routes to the prefetched episode. A 31-agent adversarial review found 13 defects, 9 surviving refutation, all fixed in the same commit — chief among them that `autoPlayNext` defaults to OFF, so the countdown was promising an advance that would never happen. **Device verification pending: no test hardware was reachable.** |
| MB-344 | Native | Medium | **`autoplayInFlight` is never cleared on the error path, so it can latch true for the life of the activity and dead-lock autoplay-next.** Set in `tryAutoplayNextEpisode` before the IO lookup and cleared only on `Player.STATE_READY` or the no-next-episode bail-out. If the next episode fails to prepare, the player goes to `STATE_IDLE` + `showStreamError` and never reaches READY — so every subsequent end-of-episode silently declines to advance until the activity is recreated. MB-343 now also claims this guard for the manual dock-NEXT path, which inherits the same weakness. | **Fixed** | 2026-08-01 | Found by the W4 review agents and confirmed independently. Deliberately NOT fixed inside `f984bcc`: widening a guard whose whole job is the ENDED→prepare window, inside a commit that adds a second claimant to it, is how you ship two bugs instead of one. Wants its own slice — clear the flag in the error path (`showStreamError` / `STATE_IDLE`) and add a test for the failed-prepare sequence. **Fixed 2026-08-22.** Release is now centralised in `releaseAutoplayGuard(event)`, the single place the flag is lowered, driven by a pure `shouldReleaseAutoplayGuard` kernel (`AutoplayGuard.kt`, 7 tests). The root cause was not the guard but that its release was scattered across four call sites covering only the happy path — adding a fifth writer (MB-343's dock NEXT) is what made the gap visible. **Two latch paths were found, not one.** The filed one (terminal error never reaches `STATE_READY`) plus a second: if `controller.play(...)` throws, no MediaItem is prepared at all, so neither READY nor `showStreamError` runs. Both release now. The distinction the kernel encodes is terminal-vs-recoverable, NOT error-vs-success: `onPlayerError`'s BEHIND_LIVE_WINDOW re-seek and transient-network retry deliberately re-prepare and are expected to reach READY, so releasing there would drop the guard mid-prepare and reopen the double-`STATE_ENDED` skip it exists to absorb — trading a stuck guard for a silently skipped episode, which is worse. If those retries fail they fall through to the terminal path anyway. 2 negative controls: reverting terminal-error to non-releasing, and over-releasing on recoverable errors, each redden exactly the matching test. **Not device-verified** — reproducing it needs an episode that fails to prepare followed by a second end-of-episode; the fix is a pure predicate with the call sites routed through it. |
| MB-345 | Native | **Medium** (was Low) | **The VOD dock's 4-second auto-hide is not reset by D-pad focus traversal**, only by clicks and key handlers that call `onUserInteraction`. `TransportButton`'s `onFocusChanged` only flips a visual flag, and there is no `Activity.onUserInteraction` override. So navigating from the initially-focused play/pause out to a far control (FAV, MENU, or the newly-live NEXT) races a 4s timer that nothing the user is doing resets. **It does not merely time out — it does the WRONG thing:** once the dock is gone LEFT/RIGHT are seeks and CENTER re-opens the dock, so a user mid-traversal silently scrubs the film instead of pressing the button they were reaching for. | **Fixed** | 2026-08-01 | **Re-rated Low → Medium on device evidence (2026-08-19).** Filed after **both refuting agents judged it not a defect** (4s is enough for a deliberate press). It bit within the hour: verifying MB-343 on a Fire TV, reaching the dock's NEXT button — two RIGHT presses from default focus — failed twice because the dock hid mid-traversal, and only succeeded by batching all four key events into one adb call faster than a human can press. The refutation assumed a user who already knows where to go; MB-343 had just made NEXT a control people must navigate to. Fixed as the entry itself proposed: `shouldResetDockAutoHide` (pure kernel, 4 tests, 3 negative controls each failing exactly one distinct test) called as the FIRST statement of `dispatchKeyEvent` so no early-return path can skip it — one predicate covering all 11 dock controls instead of 11 `onUserInteraction` call sites. Device-verified: dock open at t=0, RIGHT at t=2/4/6, still visible at t=7; then focus verified on `Next` at t~6 and CENTER advanced S1E2 to S1E3. **Lesson: a refutation that assumes an expert user is not a refutation for a TV remote.** |
| MB-346 | Native | Medium | **The VOD dock badged EVERY series episode "MOVIE"**, directly above a kicker line reading `S1E2 · ... S01E02` — one surface giving two answers. Not a typo in the mapping: `buildDockData`'s `ContentType.SERIES -> "EPISODE"` arm was simply **unreachable**, because `PlaybackController.toContentItemView` synthesizes a playing episode's `ContentItem` with `type = ContentType.MOVIE` deliberately and correctly (an episode IS a VOD file, so resume-point logic behaves and the ~20 `type == ContentType.LIVE` checks across the player read false). The typing is not the bug; an internal playback-semantics decision reaching a **display** surface is, because "is this a VOD file" and "what is the user watching" are different questions. MB-343 made it glaring by adding the episode kicker right next to the chip. | **Fixed** | 2026-08-19 | Found while device-verifying MB-343 on a Fire TV. `DockTypeLabel` (pure kernel, 6 tests) resolves from `PlaybackController.currentEpisode` — the stashed `Playable.Episode` — and falls back to `ContentType` only when there is none. Same shape as the tested precedent `subtitles/buildSubtitleQuery`, which threads the episode alongside the item and is exactly why subtitle search was never affected and correctly asks OpenSubtitles for `type=episode`. Also fixes an untranslated literal: the three labels are now `content_type_*` resources in all 4 locales. `buildDockData` now reads `currentEpisode` ONCE and feeds both the chip and the kicker — two reads of the same StateFlow could straddle an item swap and describe two different things in one dock. Negative control: deleting the guard fails exactly the 3 episode-bearing cases. Device-verified: badge reads `EPISODE` on S1E2 and S1E3. |
| MB-347 | Native | Medium | **Content-type labels were hardcoded English string literals in search results and the coverflow overline**, so Arabic / French / Spanish users saw Latin "LIVE" / "MOVIE" / "SERIES" beside otherwise fully translated rows. `CoverflowSectionScreen`'s overline is the tell: its `ContentType.LIVE` arm called `stringResource(R.string.cf_kicker_live_channel)` while the `MOVIE` and `SERIES` arms sat right beneath it as bare literals. **String constants in code are invisible to both lint `MissingTranslation` and the MK.31 sweep**, which is exactly how they survived a 1024-key / 4-locale pass that was declared complete. | **Fixed** | 2026-08-19 | Found by a targeted detector rather than by eye — an ad-hoc read caught 2 sites, a grep for `ContentType.X -> "literal"` caught 4. Introduced surface-neutral `content_type_movie` / `_series` / `_episode` / `_live` (SINGULAR — they label one item, unlike the PLURAL `section_*` names that title a screen) shared by the dock, search and the coverflow, so the same word cannot drift across three prefixed copies. `LIVE` keeps `cf_kicker_live_channel` in the coverflow because "LIVE CHANNEL" describes the item rather than naming the type. `SearchScreen.typeLabel` now returns a `@StringRes Int` resolved in composition. Remaining sites split out as MB-348 — they are not drop-in. |
| MB-348 | Native | Low | **Two further hardcoded-English type-label sites that are NOT mechanical fixes.** (1) `SettingsGroupsTab.typeChipLabel` returns `"Live TV"` / `"Movies"` / `"Series"` — values identical to the existing `section_*` resources — but the call sites apply `.uppercase()` (chip row) and `.lowercase()` (embedded into `grp_no_groups`, "No groups found for %1$s."). Localizing naively would be **worse than the bug**: bare `.uppercase()`/`.lowercase()` hit the Turkish dotless-I trap, Arabic has no case at all, and splicing a noun into a sentence breaks grammatical agreement (French needs *des films* vs *des séries*). Correct fix restructures `grp_no_groups` into three complete per-type sentences and drops the case transforms. (2) `CoverflowSectionScreen.PreviewIdleArtwork` renders `"MOVIES"` / `"SERIES"` as idle watermark artwork where the `LIVE` arm deliberately draws the untranslatable `brand_plus` (`YANCOTV+`) — so whether these should be localized at all is a brand-design decision, not a defect. | **Fixed** | 2026-08-19 | Split from MB-347 rather than half-fixed. Both need a decision before code: (1) is a copy restructure, (2) is a brand call. **(1) FIXED 2026-08-22.** `typeChipLabelRes` now returns a `@StringRes` resolved from the existing `section_live_tv` / `section_movies` / `section_series` strings — already shipped in all four locales — rather than a parallel set that could drift. `grp_no_groups` and its `%1$s` splice are gone, replaced by three complete sentences per locale (`grp_no_groups_live` / `_movies` / `_series`), and the `.lowercase()` on an already-translated noun is removed. The splice was untranslatable, not merely awkward: French needs the article to agree ("de films" vs "de séries" vs "de TV en direct") and Arabic contracts the preposition into the article (لـ + الأفلام becomes للأفلام), neither of which a substitution can produce. **One premise in this entry was wrong and is corrected here:** it cited the Turkish dotless-I trap for the `.uppercase()` call site. Kotlin's no-arg `uppercase()` is locale-INVARIANT, unlike Java's `toUpperCase()`, so that hazard does not exist — the call is kept, and it is a harmless no-op on Arabic, which has no case. The real defects were the hardcoded English and the splice. Verified: all four locale files still parse, `:app:compileDebugKotlin` and ktlint clean, Android lint reports no `MissingTranslation` / `StringFormat` issue for the new keys. **(2) still open** — `PreviewIdleArtwork`'s "MOVIES" / "SERIES" watermark sits beside a deliberately untranslatable `brand_plus`, so whether it should be localized at all remains a brand decision, not a defect. Detector for regressions: `grep -rn -A4 'when *(.*type.*) *{' --include=*.kt | grep -E 'ContentType\.[A-Z]+ *-> *"'` — this is what found all four sites and should be run whenever a `ContentType` branch is added. **Closed 2026-08-22.** Part (1) had already been fixed in an earlier pass and this entry was stale on it: `grp_no_groups` was long since split into three complete per-type sentences (`grp_no_groups_live/movies/series`, all four locales), the `.lowercase()` splice is gone, and the surviving `.uppercase()` now carries a documented rationale — Kotlin's `uppercase()` is locale-INVARIANT (unlike Java's `toUpperCase()`), so the Turkish dotless-I hazard this entry cited does not apply, and on Arabic it is a no-op. Part (2) was the open half and was always a brand decision, not a defect. **Owner decision 2026-08-22: draw the brand mark on all three arms.** `PreviewIdleArtwork` now renders `brand_plus` (YANCOTV+) for LIVE, MOVIE and SERIES alike, instead of the brand mark for LIVE and hardcoded English "MOVIES"/"SERIES" for the others. Rationale recorded at the call site: this is artwork, not copy — mixing a brand mark with a translated category noun is a mixed metaphor, and the English words rendered untranslated on an Arabic or French device beside a fully translated UI. The brand mark is consistent, carries no localization debt, and reinforces the brand on every idle screen. `type` is retained (a future per-type tint/artwork treatment is plausible) but deliberately no longer selects the text. |
| MB-350 | Native | Low | **The startup splash clipped the YancoTV logo into a horizontal oval.** `Theme.YancoTV.Splash` pointed `windowSplashScreenAnimatedIcon` straight at `@drawable/ic_logo`, which is the 16:9 badge+wordmark lockup (640x360). The splash API masks that icon to the system icon shape — a circle — so both ends of a wide image fall outside the mask and the user sees the logo cut at the left and right edges. | **Fixed** | 2026-08-19 | User-reported. New `ic_splash_logo.xml` sizes the lockup to fit the visible circle: the icon canvas is 240dp with only a 160dp-diameter circle visible, and a 16:9 rect fits a circle of diameter D at `w = D / sqrt(1 + (9/16)^2)` = 139dp, so 132x74dp clears it with margin at the exact 16:9 ratio. The transparent 240dp square beneath is load-bearing rather than padding: without it the drawable’s intrinsic size is the bitmap’s, and the platform scales the icon to fill the canvas — which would blow the logo back to full width and re-crop it. Kept as the full lockup rather than swapping to the square `ic_logo_mark`, because the request was for all of the logo with no cuts; the cost is that it renders smaller than before. |
| MB-349 | Native | **High** | **CI has run zero tests since 2026-07-25 — MB-295's release-signing check throws at Gradle CONFIGURATION time, so it kills every task in the build.** The `throw GradleException` lived inside `buildTypes { release { … } }`, and Gradle evaluates that block whenever it configures `:app` — which it does for *every* task. So on any machine without a release keystore (i.e. **every CI runner**) nothing could run at all: ktlint, `verifyCommonMainYancoDbMigration`, both unit-test suites, `assembleDebug`, and even the `macos-latest` iOS-compile job died during configuration, before a second of real work. The workflow's `-PallowUnsignedRelease=true` escape hatch was passed only to the `assembleRelease` step — the one place it was not sufficient — because the failure was never about that task. **Three weeks of pushes landed with no test signal at all**, and the register meanwhile recorded CI as a closed release gate. | **Fixed** | 2026-08-19 | Found by watching the first CI run after pushing 71 commits; both jobs failed at `app/build.gradle.kts` line 234 with MB-295's own message. MB-295 was right about the RULE (a debug-signed release APK installs locally and can never be updated by anyone holding a real build — a one-way door for the whole install base) and wrong only about WHERE to enforce it. The guard now runs from `gradle.taskGraph.whenReady`: after configuration, before execution, which is the earliest point where "will a task that actually SIGNS something run?" can be answered. Signing tasks are matched by **exact name** (`packageRelease`, `packageReleaseBundle`, `validateSigningRelease`) rather than a "contains Release" pattern — `lintRelease`, `compileReleaseKotlin` and `packageReleaseResources` all carry Release in the name and produce nothing distributable, so a loose match would reintroduce the same bug one level down. `releasePackage` / `bundleRelease` are covered transitively. Verified locally by simulating a keystore-less runner (temporarily repointing `release.keystore.path`, restored byte-identical after): `ktlintCheck` and `testDebugUnitTest` now SUCCEED where they previously died, `assembleRelease` still FAILS with MB-295's message, and `-PallowUnsignedRelease=true` still passes with the debug-signing warning. **Lesson: a build-script guard that throws during configuration has the whole build as its blast radius, not the task it names.** |
| MB-351 | Native | Low | **`content_first_seen` rows are never deleted when a source is, so a deleted provider leaves one orphan row per item behind forever.** `ContentFirstSeen.sq` declares a `deleteBySource` query and **nothing in production calls it** — `BulkContentWriter`'s stamping INSERT is the table's only writer. The table deliberately has no FK to `content(id)` (a cascade would wipe it on every sync, which is the bug it exists to prevent), so deleting a source cascades away its `content` rows and leaves the stamps untouched. Deleting the 272,419-item Xtream provider would strand ~272k rows, roughly 15 MB. | **Fixed** | 2026-08-21 | Found by counting rows in the device's real database rather than by reading code — `content_first_seen` held 967 rows against 272,423 in `content`. **Correction to this entry's original evidence:** those 963 rows were first described here as orphans left by a deleted source. They were not. Their source still existed and merely had 0 content rows, because a sync was in flight — `prepareSource` had deleted its content and the re-INSERT never ran (see MB-315). `14.sqm` keys on `source_id NOT IN sources` and therefore leaves them alone, which is correct. The device in fact held **zero** true orphans, so the sweep deleted nothing there. The leak itself is still real and still worth fixing — the code path simply had no instance on this device. Recording the correction because a bug entry whose evidence does not survive checking is worse than one with no evidence. **Not a correctness bug and not a release blocker**, for two reasons checked rather than assumed: `recentlyAddedVod` INNER JOINs `content`, so an orphan can never render; and a re-added source cannot inherit stale stamps because ids are `src-$ts-$rnd` (`SourceRepository.kt:815`), so `countFirstSeen` on the new id is 0 and its first sync is correctly stamped as an initial import. Had ids been deterministic this would instead be High — re-adding a provider would have declared its whole catalogue "recently added". Fix: call `deleteBySource` from the source-deletion path, plus a one-off sweep of rows whose `source_id` is absent from `sources`. `recent_channels` has the same no-FK design and does NOT need this — `trimToNewest` caps it at a fixed row count. **Fixed 2026-08-21.** `removeSource` now deletes from both side tables inside one transaction before dropping the source. Ordering is load-bearing and documented at the call site: `recent_channels` FIRST, because it keys on `content_id` and can only be scoped to this source by joining through `content` — once the source goes, the FK cascade takes that content and the rows are no longer identifiable. Scope beat MB-351's own recommendation in one place: the entry said `recent_channels` did not need this because `trimToNewest` bounds it. True for growth, wrong for the rail — the cap is 100 rows, and stale entries occupy slots in that window while the read path INNER JOINs `content`, so deleting a source you had watched silently thinned "Recent channels" instead of leaving it alone. A scoped `deleteBySourceContent` was added rather than a general "delete rows whose content is missing" sweep, which would erase the list mid-sync (MB-289's failure shape). `14.sqm` sweeps the debt already on disk, keyed on `source_id NOT IN sources` for the same reason. Data-only, so upgraded and fresh databases stay structurally identical. Tests: two sources, so a fix that wiped the table cannot pass, asserting on `countAll`/`countBySource` rather than the read queries — both INNER JOIN content, which the cascade has already emptied, so a rail-level assertion would pass whether rows were deleted or merely orphaned. Four negative controls, each reddening exactly one test: dropping either cleanup call, replacing the scoped delete with `clearAll()`, and rewriting the migration to sweep by missing `content_id` (the dangerous variant). 697 shared tests pass, ktlint clean. **Device-verified 2026-08-21 (Fire TV AFTDCT31, real 352 MB catalogue).** Both sample sources deleted through the real Settings UI: `content_first_seen` went **967 -> 0**, sources 3 -> 1, `content` 272,423 -> 272,419 (exactly the 4 rows the deleted M3U file provided), and the surviving Xtream source kept all 272,419 rows. Post-delete the database has ZERO dangling references across watch_history, favorites, episodes, epg_programmes, content and both side tables. The two other deltas are correct FK cascades the confirmation dialog explicitly warns about: one watch_history row (a sample movie) and 1,730 epg_programmes (the deleted source's guide data). **Caveat: the `recent_channels` half of the fix was NOT exercised on device** — that table was empty before and after, because no live channel on a deletable source had been watched for the 30 s dwell. It remains JVM-tested only. |
| MB-352 | Native | Medium | **Deleting the bundled sample source does nothing — it is silently re-created on the very next launch, with auto-sync switched back on.** `MainActivity.kt:215` re-adds it unconditionally whenever it is absent: `if (existing.none { it.name == sampleVodName }) { addSource(...); setAutoSyncOnStart(vod.id, true) }`. The comment says "Always ensure the bundled VOD sample is present so movie playback + casting are testable" — a development convenience that shipped. The user is shown a confirmation dialog reading **"This removes the source and everything it provides — channels, movies, series, and their favorites and watch history. There is no undo."**, confirms it, watches the row disappear, and finds it back after the next cold start. A destructive action that silently reverts is worse than one that is refused: the user cannot tell whether the app ignored them, whether the delete failed, or whether something re-added it. It also silently re-enables `auto_sync_on_start`, so a source the user deliberately removed resumes syncing on every launch. Note the neighbouring live-news seed at `MainActivity.kt:196` is guarded by `if (existing.isEmpty())` and is therefore correct — deleting THAT one sticks. Only the VOD sample is unconditional. | **Fixed** | 2026-08-21 | Found on device while verifying MB-351: deleted both sample sources, confirmed at the database level (sources 3 -> 1, `content_first_seen` 967 -> 0), then relaunched the app and Settings showed "YOUR SOURCES · 2" with "Sample movies (bundled)" restored. The MB-351 measurements were taken with the app force-stopped and are unaffected. Fix is a decision, not a patch, because the seed exists for a reason (a device with no working provider still has something playable for testing casting and VOD). Options: (1) seed it only on a genuinely fresh install, matching the `existing.isEmpty()` guard the news sample already uses; (2) record a `sample_vod_dismissed` flag in `settings` when the user deletes it, and honour that forever; (3) keep re-seeding but make it honest — do not offer Delete on that row, or tell the user it will return. Option 2 is the one that respects the confirmation dialog already shown. Whichever is chosen, `setAutoSyncOnStart(..., true)` must not be re-applied to a source the user removed. **Fixed 2026-08-22 — seeding is now once-ever instead of "re-add whenever absent".** Tracked by a local `SharedPreferences` flag, deliberately NOT the `settings` table, which backup/restore would carry onto machines where it means nothing. Four cases are handled separately on purpose: flag set → never add; sample present but no flag → adopt it so a later delete sticks; no sources at all → genuine fresh install, seed it; has sources but no sample and no flag → an established user who removed it BEFORE this fix, who must not be handed it one last time. **Correction to this entry's original framing:** the whole seeding block sits inside `if (BuildConfig.DEBUG)`, so this never affected a release build — the impact was developer/test builds only, which the entry did not say. |
| MB-353 | Native | **Critical** (data loss) | **A sync that stalls after `prepareSource` leaves the catalogue EMPTY indefinitely, with no error shown and no recovery.** `BulkContentWriter.prepareSource` opens its own transaction, deletes every `content` and `content_fts` row for the source, and **COMMITs** — before a single replacement row is fetched. Everything after that (network fetch, chunked inserts, `finishSource`) is a separate sequence. If the process is killed, the sync errors, or the writer simply never gets the write connection back, the committed deletion stands and the catalogue stays empty until some LATER sync happens to succeed. The UI shows no error — `last_sync_error` is only written on an error path that a hang never reaches — and `sources.channel_count` still reports the pre-delete figure, so Settings claims "272.4k items" for a source with zero rows. | **Fixed** | 2026-08-22 | **Observed on the Fire TV, not theorised.** A yaman sync started 18:33:07 and never completed; five hours later there were ZERO `syncSource ... done` lines, no error, no abort — it simply hung, starved of the write connection (see MB-315). Meanwhile the deletion it had already committed stood: the EPG importer, which counts live channels with a `tvg_id` at the start of every run, reported **35** where it had reported **3,769** an hour earlier. The database fell from 352 MB to 126 MB. Ten consecutive EPG imports then ran against the gutted catalogue and kept 1,021 rows across 22 channels instead of ~215,000 across 3,110 — so the guide was destroyed as a downstream consequence. Recoverable only by a successful re-sync (~8 minutes for this catalogue); nothing in the app initiates that or tells the user why everything is empty. **Why this is worse than the starvation that triggers it:** MB-315 costs latency, this costs the user their entire catalogue. The two must not be conflated — fixing MB-315 reduces how OFTEN this fires but leaves the destructive-commit-before-replacement ordering intact, and a process kill mid-sync reaches the same state with no contention at all. Fix direction: do not commit the deletion until replacement data is in hand. Either stage the new catalogue and swap (costs disk, and the same trade-off rejected for MB-315 needs re-examining here because the stakes differ), or keep the delete and the first insert batch in ONE transaction so a failure rolls the deletion back. Cheaper mitigation worth doing regardless: record a `sync_in_progress` marker (same shape as `epg_import_state`) so a stalled sync is detectable, and stop reporting a stale `channel_count` for a source whose content rows are gone. **Fixed 2026-08-22 by deferring the destruction, not by staging.** `prepareSource` no longer deletes anything; it sets the marker and drops the FTS trigger. The two DELETEs moved into `clearIfFirstWrite`, called at the top of all seven chunk writers' transactions, so the old catalogue is dropped in the SAME transaction as the first batch of replacement rows — both land or both roll back. **A sync that delivers nothing now destroys nothing**, which covers the whole common class: dead URL, expired subscription, no network, process kill before first byte. Chosen over a staging table because staging duplicates ~250 MB of a 374 MB database on a Fire TV, and over one-giant-transaction because that is the design the chunked writers exist to avoid (MB-230 heap, MB-315 lock hold). **The subtle half is `abortSource`:** its DELETEs exist to sweep HALF-WRITTEN rows, and left unconditional they would have destroyed an untouched catalogue on exactly the error path this fix is about — the bug reintroduced one function along. They are now guarded on `clearedFor`. Marker lives in the key/value `settings` table, so no migration — deliberate while CI cannot verify migrations (MB-349). Set before anything destructive, cleared in the same transaction that completes the sync; `syncWasInterrupted` is wired into `SourceRepository.syncSource` so an abandoned run is traceable in logs. One existing test changed contract: the MB-289 empty-catalog guard asserted `countBySource == 0` ("the empty sync really did leave the catalog empty") and now asserts the catalogue survives — its favourite assertions still pass, they are just no longer the last line of defence. 4 tests added; 4 negative controls each reddening the expected tests (delete eagerly in prepareSource; never clear; abort unconditionally; stop clearing the marker). 701 shared tests pass, ktlint clean. **Residual, deliberately not fixed here:** a sync that dies AFTER its first chunk still leaves a partial catalogue — the marker makes it detectable but nothing auto-repairs it yet, and `channel_count` still reports the last completed sync's figure. Both want their own slice. |
| MB-354 | Native | Medium | **The app is landscape-only on a phone, and the plan claims otherwise.** `MainActivity` declares `android:screenOrientation="sensorLandscape"` and `PlayerActivity` declares `landscape`, so a phone user holds the device sideways for every screen including browse and settings. MK.27.C ("Settings phone mode") records being **"Verified on-device portrait (list → body → BACK-to-list; Network readable + crash-free) + sw=411 confirmed"**, which cannot be true against these manifest values — the activity can never enter portrait. Either the verification predates the orientation lock, was done with the lock removed, or was done on a tablet; the plan does not say. | **Fixed** | 2026-08-22 | Found during the 2026-08-22 reconciliation pass while checking whether a release was safe to hand to a phone user. Not a crash and arguably correct for a TV-first app — but it is a significant experience characteristic that nothing in the plan or register stated, and it contradicts a written verification claim. **Not changed as part of a stability release:** unlocking orientation is a real UX change needing a form-factor branch (TV must stay landscape), a portrait layout pass over browse/player, and device testing on an actual phone — none of which belongs in a build meant to be stable. Fix direction when picked up: drive orientation at runtime from the existing `isTv` form-factor check rather than statically in the manifest, then re-verify MK.27.C's portrait claim honestly. **Resolved 2026-08-22 — owner decision: portrait is cancelled, landscape is the intended design on every form factor.** The shell is laid out landscape-first throughout and a TV-first app gains nothing from rotating, so "the plan claims otherwise" is fixed by correcting the plan, not by building portrait: the MK.27.C row's "Verified on-device portrait" line was false and has been struck. The audit did surface a genuine phone defect underneath it, now fixed: `MainActivity` was `sensorLandscape` while `PlayerActivity` was pinned to `landscape`, so a phone held in reverse-landscape browsed upright and then played video **180° upside-down** on entering fullscreen. Both are `sensorLandscape` now — still landscape-only, but consistent about which way up. No TV effect, where the sensor never reports a rotation. |
| MB-355 | Native | **High** | **A scheduled recording runs for minutes, writes 0 bytes, and reports success the whole time.** The schedule fires correctly, the foreground service starts, the output file is created — and then nothing is written, with no error surfaced and no diagnostics logged. The UI shows `Recording · ends 6:00 PM` for the full duration, and afterwards offers a `Play` button on a 0 KB file. Split out of MB-229, which had filed this as a symptom of MB-230 heap pressure. It is not: reproduced on 2026-08-22 with a Java heap of 68 MB. | **Fixed** | 2026-08-22 | **Device evidence (Fire TV AFTDCT31, 2026-08-22).** Full log for the run, and the silence between the second and third lines is the finding:\n```\n15:12:30.052 YancoSchedReceiver: fire[sched-c04bfc40-...] path=headless_fresh_get\n15:12:30.126 YancoRecordingSvc: start[sched-rec-...] format=MPEG_TS path=fresh-get\n15:12:30.169 YancoStorageResolver: Public mode allocation failed (WRITE_EXTERNAL_STORAGE not granted on API 28;\n             Public mode falls back to app-private...); falling back to app-private.\n15:16:22.209 YancoRecordingSvc: stop[sched-rec-...] cancelled by user — 0 bytes (232s)\n```\nBetween start and stop the process logged **nothing at all** — no HTTP attempt, no stream error, no progress tick — verified by filtering the whole window and finding only `chatty` line-expiry notices and unrelated system noise. The output file was created and stayed empty: `/sdcard/Android/data/com.yancotv.android/files/Movies/yanco-recordings/sched-rec-sched-c04bfc40-....ts`, 0 bytes, timestamped 15:12. **Recording is not wholly broken** — MediaStore still holds a real 7.98 MB `.ts` from 2026-05-15, so this pipeline has produced valid files before. **NOT yet established: whether the source stream was viable.** beIN SPORTS 1 was never played back to check, so a dead/geo-blocked channel remains a live hypothesis. That does not change the severity: if the stream is dead the defect is that four minutes of "Recording" and a `Play` button are offered for a stream that never connected, with nothing logged to say so. If the stream is fine the defect is the writer. Both are High. **First step for whoever picks this up:** play that channel live and see. Then instrument — the absence of any log line between start and stop is itself a defect, since it makes the failure undiagnosable from a user report. **Note the storage fallback line** as a possible contributor: `WRITE_EXTERNAL_STORAGE not granted on API 28` sends recordings to app-private storage. Worth confirming the writer handles that path correctly rather than assuming the fallback is inert. **CONFIRMED NOT STREAM-SPECIFIC, 2026-08-22.** The open question on this entry was whether the source stream was viable. Repeated the test on a DIFFERENT channel from a different broadcaster — "The Making of Mourinho" on ELEVEN SPORTS 1 UHD, 18:05-18:35, fired 18:04:30 — and got the identical result. Two files now sit side by side, both empty:\n```\n0 bytes  sched-rec-sched-c04bfc40-....ts   15:12   beIN SPORTS 1\n0 bytes  sched-rec-sched-c7e4e369-....ts   18:04   ELEVEN SPORTS 1 UHD\n```\nSame three log lines each time (`fire`, `start format=MPEG_TS`, storage fallback) and then total silence until stop. **So the channel is not the variable and the user's provider list is exonerated** — the user had already pointed out they have no geo-blocked channels, and that is borne out. **Recording is broken for every stream tried, not for one.** Severity stands at High and the diagnosis narrows sharply: it is the writer or the fetch, not the content. Note the storage fallback fires on BOTH runs, which makes it the strongest remaining lead — `WRITE_EXTERNAL_STORAGE not granted on API 28` diverts output to app-private storage, and the file IS created there (so the path resolves) but never written to. Next step is unchanged and now better justified: instrument the fetch/write loop, because the absence of a single log line between start and stop is what makes this undiagnosable. A working reference exists to compare against — MediaStore still holds a valid 7.98 MB `.ts` from 2026-05-15, so this pipeline did work at some point; a bisect against that date would bound the regression. **ROOT-CAUSE PASS, 2026-08-22 evening — the provider is exonerated with numbers and two earlier hypotheses on this entry are now dead.** (1) **The silence was never evidence.** `MpegTsRecorder` took a `logger` annotated `@Suppress("unused")` and never called it, and `RecordingService.onRecorderResult` logged only on its two exceptional catches — so a healthy recording and a failed one were equally invisible. Reading "no log lines between start and stop" as a hang was wrong; there was simply nothing to hear. Instrumented now: the recorder logs stream-open, response-open (with ms), first-byte (with ms), 30s progress ticks and its terminal result, and the service logs allocation, `markStarted` duration, and every terminal result. (2) **Hard row data** (pulled via `run-as` + local sqlite, the build on the device being debuggable): Brentford `started_at` 15:12:30 → `ended_at` 15:16:22, `status=cancelled`, 0 bytes — **232 seconds in RECORDING state without a single byte.** (3) **The second data point was mine, not the app's.** The Mourinho run reads `orphaned_by_app_kill` because I `am force-stop`ped the app during the investigation. Citing it above as an independent confirmation was incorrect — one clean occurrence is what this entry actually rests on. (4) **The provider is healthy.** Both stream URLs return `302` and redirect to a per-session random host on a different domain (`*.ip1-st45.me`, cleartext). Following the redirect from the PC: HTTP 200, first byte **0.53s** and **0.62s**, **1,792,956** and **73,992,708 bytes in 15 seconds**. The streams are fast and fine; the app is what fails. This closes the question the entry opened. (5) **`markStarted` is not the stall.** It completed inside the same second as the service `start` log, so the MB-356 leaked-write-connection mechanism is eliminated as the cause here — the two bugs are not the same bug. (6) **The cleartext allow-list is not the cause either**, though the redirect made it the obvious suspect: `CleartextAllowlistInterceptor` is registered with `addInterceptor` (an OkHttp *application* interceptor, run once per call) rather than `addNetworkInterceptor` (per hop), so it never sees the redirected request. ExoPlayer also plays these same channels through `OkHttpDataSource` carrying the same interceptor. (7) **The storage fallback is a red herring** — the output file is created at the app-private path in both runs, so allocation succeeded and the fallback is inert. Dropping it as the "strongest lead". **What remains, and it is sharp:** the 20-second `socketTimeoutMs` — added on 2026-05-15 for precisely this failure shape — **did not fire** across 232 seconds of zero bytes. Either the recorder never reached its read loop (stalling in `performGet`/redirect), or `socketTimeoutMs` is ineffective on the `streamLive` path in `AndroidKtorHttpClient.getSource`, which bridges the body through `channel.toInputStream()`. The new instrumentation distinguishes these on the next run: "opening stream" with no "response open" means the fetch; "response open" with no "first bytes" means the body read. **ROOT CAUSE FOUND AND FIXED, 2026-08-22.** Reproduced OFF-DEVICE by running the real production classes (`AndroidKtorHttpClient` + `MpegTsRecorder`) against the real stream from a JVM harness — `Success(bytesWritten=0, secondsElapsed=115)`, the headline symptom on a PC, no Fire TV in the loop. A three-leg isolation in one JVM, same URL, same UA, same timeouts, then located the layer:
```
LEG-A raw OkHttp        headers at   773ms, first bytes at   782ms, 1,343,397 bytes
LEG-B our Ktor getSource body open at 83,749ms                    <-- 108x slower
LEG-C full recorder      no response within 25s
```
Timing probes inside the transport put **all 83.7 seconds inside `performGet`**; everything after it took 17ms, and the body then yielded 4,744,372 bytes in **2 milliseconds** followed by immediate EOF. That last number is the proof: the body was already sitting in memory. **`performGet` calls `ktor.get(url)` — a non-prepared call, which reads the ENTIRE response body before returning the `HttpResponse`.** Correct for an API call; fatal for a continuous MPEG-TS live stream, which never ends, so the call never returns. The recorder's read loop, its `maxDurationMs`, its heartbeat watchdog and its progress logging all live inside a block that is never reached — which is exactly why the recording sat at 0 bytes, logged nothing, and failed no timeout. The `streamLive` flag added earlier was an incomplete fix: it skipped the temp-file spool (the second buffer) and left Ktor's own in-memory body buffering upstream in place. **Fix:** added `KtorHttpClient.performGetStreaming`, using Ktor's documented streaming form `prepareGet(...).execute { }`, which hands over the response without reading the body; `AndroidKtorHttpClient`'s `streamLive` branch now uses it. `performGet` and the streaming path share one `applyRequestOptions` so headers and timeouts cannot drift apart. **Verified on the same live stream after the fix:** LEG-B body open at **756ms** (was 83,749ms), and the full recorder logged `response open after 560ms` → `first bytes after 565ms` → `completed via deadline — 1319KB in 15s`, writing **1,351,264 bytes to a real file**. A raw-OkHttp control run immediately after the Ktor leg returned in 605ms, which also rules out provider-side throttling of repeat requests. **Second defect fixed in the same pass:** `finishCompleted` returned `RecordResult.Success` even with `bytesWritten == 0`, so a stream that served nothing was recorded as a completed recording — "Saved 0 KB" with a `Play` button that hits ExoPlayer's 3003 error. Both `MpegTsRecorder` and `HlsRecorder` now fail such a run as `no_response_from_server`, matching the vocabulary `RecordingService.handleStop` already used for the identical condition. Covered by `emptyBodyFailsInsteadOfReportingSuccess`, negative-controlled (guard removed → exactly that one test reddens). **Hypotheses tested and eliminated along the way**, each with evidence rather than assumption: the cleartext allow-list (an OkHttp *application* interceptor, so it never sees the redirect hop), User-Agent gating (the recorder's exact UA pulls 1.6 MB), a provider concurrent-connection cap (two simultaneous connections both streamed, 1.16 MB and 1.61 MB), the storage fallback (inert — the file is allocated fine), and MB-356's leaked write connection (`markStarted` completed within the same second). Shared suite 705 tests green, ktlint clean. **Device verification of the headless fresh-GET path is still outstanding** — the on-device test exercised the `live-tee` path (84 MB, healthy), and the scheduled path needs a scheduled recording to fire. | **Fixed (device verification of the scheduled path pending)** |
| MB-356 | Native | **High** | **The primary SQLite connection is leaked: every write in the app starves indefinitely while nothing is running, and the UI gives no sign.** Distinct from MB-315, which was a long statement legitimately holding the lock. Here **nothing holds it** — the pool reports `0 active, 1 idle, 3 available` and prints no "Requests in progress" section, yet writer threads queue forever on `flags 0x2` (primary/write connection). Reads keep working because WAL lets them through, so the app looks healthy: screens render, navigation responds, and every write — favourite toggles, watch history, resume points, recording state — is silently dropped. Recovers only on app restart. | **Fixed (mechanism removed; field confirmation pending)** | 2026-08-22 | **Observed live on a Fire TV, 2026-08-22, on a build that already includes MB-315's batched clear** — so batching does not prevent it and this is a different mechanism. Three writer threads starved simultaneously, each still climbing when caught:\n```\nthread 7853 (DefaultDispatcher-worker-3) flags 0x2 for 1230.03 s   <- 20.5 minutes\nthread 7799 (DefaultDispatcher-worker-4) flags 0x2 for  540.01 s\nthread 7869 (DefaultDispatcher-worker-7) flags 0x2 for  150.00 s\nConnections: 0 active, 1 idle, 3 available.      <- nothing is running\n```\nThe absent "Requests in progress" section is the diagnosis. When MB-315 starved writers, that section named the blocking statement (`DELETE FROM content_fts ... 62392ms ago - running`). Here it is empty: the connection is checked out by something that is neither executing nor releasing. Corroborating state: the WAL had grown to **95 MB** and could not checkpoint (a checkpoint needs the write lock), and the app logged **nothing at all** for the preceding ten minutes. After `am force-stop` + relaunch: zero warnings, WAL fell to 70 MB and resumed checkpointing. So it is an in-process leak, not disk or DB corruption. **Timeline points at the recording path.** The earliest starved thread began waiting ~15:41; the only app activity before that was the scheduled recording of 15:12:30-15:16:22 that wrote 0 bytes (MB-355). A leaked connection there would explain both entries: the recording service could not persist progress, and every later writer inherited the loss. **Not proven** — the correlation is timing plus absence of other candidates, not a captured stack. **Why this is worse than it looks:** there is no error, no toast, no log line. A user toggles a favourite, sees the star fill, and the row never persists. This is the most likely explanation for any past "my settings did not save" report. First steps: audit every `BEGIN IMMEDIATE` for a path that can exit without `COMMIT` or `ROLLBACK` (the recording service and `BulkContentWriter` both hold raw transactions), and add a watchdog that logs the owning thread when a write waits more than a few seconds — the current failure is silent by construction. **SECOND REPRODUCTION, same day, and it narrows the cause.** After `force-stop` + relaunch at 16:02:37 the leak returned within ~40 s: first wait began ~16:03:18 and had reached 1230 s when observed, with the yaman auto-sync frozen at "Fetching catalog…(1254s)" — a sync that cannot finish because it cannot write. So this is not occasional; on this device it now recurs on essentially every launch, and it is almost certainly what wedged the sync in the 5-hour incident that emptied the catalogue (MB-353's trigger). **It is NOT simply caused by the 2026-08-22 transaction changes (MB-353 deferred delete, MB-315 batched clear).** The decisive control: the 12:56-13:16 sync ran on that exact build and logged **zero** starvation warnings while completing 272,871 items. Same code, clean run. Something about the state or trigger differs, not the code alone. Both leaks that day began AFTER the 15:12 scheduled recording, but the second one survived a full process restart with no recording running, so a live leak inside the recording service is ruled out as the sole explanation. **Do not guess the mechanism from here.** The next step is instrumentation, not a patch: log thread id + a stack at every `BEGIN IMMEDIATE` / `COMMIT` / `ROLLBACK`, then reproduce and find the BEGIN with no matching COMMIT on the same thread. Android's `SQLiteSession` is thread-local, so a transaction begun on one thread and committed on another leaks the primary connection permanently — the EPG importer's session spans suspend points (`onProgress.report`) where a coroutine may migrate threads, which makes it the first place to look and is PRE-EXISTING code, not part of this day's changes. **INSTRUMENTED 2026-08-22 — and that hypothesis is DISPROVEN.** Added `TxnTrace` (thread name logged at every raw BEGIN / COMMIT / ROLLBACK, 16 call sites across `BulkContentWriter` and `BulkEpgWriter`) and ran a full cycle on the Fire TV. The decisive pair: `17:10:34 txn BEGIN site=epg.begin thread=DefaultDispatcher-worker-6` / `17:12:33 txn COMMIT site=epg.commit thread=DefaultDispatcher-worker-6` — **same thread**, so the EPG session is not leaking the connection that way. The whole cycle was clean: a complete 272k catalogue sync produced 130+ traced transactions (`prepareSource`, `clearIfFirstWrite`, `writeVodChunk`), every one opening and closing on its own thread, with **zero** starvation warnings end to end. Also eliminated by inspection: all 8 `db.transaction { }` blocks are synchronous (no `withContext` / `await` / `emit` inside) and the codebase has no `awaitTransaction` or suspending transaction anywhere, so SQLDelight's own handling cannot be migrating threads either. **Caveat on the clean result:** zero starvation across a 119-second held EPG transaction means nothing else attempted a write in that window, not that holding it is harmless — absence of contention is not absence of the defect, the same trap that made two earlier MB-315 runs look like proof. **So the leak did NOT reproduce and its mechanism remains unknown.** What the trace bought is elimination, with evidence rather than assumption: the catalogue sync path and the EPG session are both cleared. Remaining lead: both observed occurrences followed the 15:12 scheduled recording, and the recording subsystem is not instrumented — though its repositories use only reactive reads (`asFlow().mapToList`) with no raw transaction of their own. The tracer is the right tool to leave in place, and now ships trimmed to the long-lived transactions only (~10 lines per sync instead of ~1,100). **The recording correlation was tested and is NOT confirmed.** A second scheduled recording was run deliberately (18:04:30, ELEVEN SPORTS 1 UHD) with the tracer active, precisely because both earlier occurrences followed a recording. **Zero starvation warnings** during and after it. So "a recording triggers the leak" does not hold as a reproducible rule — the correlation from the two morning occurrences looks like coincidence, or needs a further ingredient not present here. Tally of what is now eliminated with evidence rather than assumption: the catalogue sync path (130+ traced transactions, all paired same-thread), the EPG session (begin and commit both on worker-6), SQLDelight's `db.transaction` blocks (all synchronous, no suspending transaction in the codebase), and a scheduled recording as a trigger. **The mechanism remains unknown and the bug is not reproducible on demand.** It has been seen twice, both times around 15:41-16:03. The honest position is that it needs to be caught in the act: leave the tracer in, and when starvation next appears, read back for the last BEGIN with no matching COMMIT. **New instrument, and it fired on its first outing (2026-08-22).** The MB-355 work added timing around `RecordingService`'s `markStarted` — a blocking DB write sitting between file allocation and the recorder starting — with a WARN above 1s. First live recording after install:
```
19:36:13.548 alloc[rec-...] path=...; writing RECORDING row
19:36:15.342 markStarted[rec-...] took 1794ms — write lock contention
```
A healthy `markStarted` is single-digit milliseconds. 1794ms means a writer was queued behind the primary connection while an EPG re-sync was running (the Guide showed "Re-syncing yaman…" at the time). This is the same starvation as MB-356, caught early and non-fatally rather than after it had wedged for 20 minutes — which is what the entry asked for: this failure is silent by construction, so it needed an instrument that speaks up. Keep the WARN; it is now the cheapest early detector we have for this bug. **MECHANISM FOUND AND REMOVED, 2026-08-22.** Found by static audit rather than by catching it live, and it fits every recorded symptom. Android's `SQLiteSession` is **thread-local**, and `SQLiteSession.executeSpecial` intercepts a raw `BEGIN`/`COMMIT`/`ROLLBACK` issued through the driver and turns it into a session-level transaction. So the primary connection is owned by the thread that ran BEGIN and is handed back only when **that same thread** commits or rolls back. `BulkEpgWriter.Session` deliberately holds ONE `BEGIN IMMEDIATE` across an entire streaming XMLTV import, and its caller `AndroidEpgImporter` suspends inside that window — `streamInto` is `suspend` and calls the `suspend` `onProgress.report(...)` between batches — all under `withContext(Dispatchers.IO)`, a 64-thread pool. Every one of those suspensions is a legal thread-migration point. When a resume lands on a different thread, COMMIT runs against a session with no transaction and **the original thread never gives the connection back**. That reproduces the exact captured state: `0 active, 1 idle, 3 available`, no "Requests in progress" section, writers queued on `flags 0x2` forever, and recovery only on process restart. It also explains why it resisted reproduction: it fires only when the dispatcher happens to reschedule, which depends on pool load — and the one traced run on record had `epg.begin` and `epg.commit` both on worker-6, i.e. it got lucky. And it explains the silence: the wrong-thread COMMIT throws, `AndroidEpgImporter` catches it and calls `session.rollback()`, which runs on the wrong thread too and is swallowed by `runCatching`. **Scope audit** — `BulkContentWriter` has zero `suspend` functions and no suspension point between any BEGIN and its COMMIT, so its transactions are thread-confined by construction. That matches the earlier runtime finding of 130+ traced content transactions all paired on the same thread, and confirms the EPG session was the only exposure. **Fix:** the import's phase 2 is extracted into `runPhase2` and run on a dedicated single-thread dispatcher (`yanco-epg-db`). `withContext` resumes on the dispatcher of the context it was called in, so every suspension inside the transaction now returns to the same thread, including returns from progress callbacks that hop to Main internally. Deliberately NOT `Dispatchers.IO.limitedParallelism(1)`, which bounds concurrency to one task at a time but promises nothing about WHICH thread runs it — thread identity is the only property that matters here. **Detector kept:** `Session` records the BEGIN thread and logs `MB-356: epg session COMMIT on 'X' but BEGIN was on 'Y'` at error level on any cross-thread close. It warns rather than throws — throwing would convert a leaked connection into a crash, and the caller cannot migrate back anyway. Covered by `session_closedOnAnotherThreadIsReported`, negative-controlled (detector call removed → exactly that one test reddens). 706 shared tests green, ktlint clean. **Honest status: the hazard is removed in code and the mechanism is proven by construction, but this was NOT observed live** — the EPG refresh could not be triggered through the TV UI in this session (focus would not reach the Refresh EPG control while a content sync was in flight). Confirmation to look for on the next EPG import: `epg.begin` and `epg.commit` both tracing `thread=yanco-epg-db`, and no MB-356 error line. |
| MB-317 | Native | Medium | **Spatial D-pad exit from the Settings content pane cleared focus entirely.** Pressing UP on the pane's first row, or ENDWARD from anywhere in it, left ZERO focused nodes (confirmed by `uiautomator dump` on a Chromecast with Google TV) and did not recover on its own — on a TV the cursor is simply gone and the user has to guess a direction to get it back. There is no focusable in either of those directions: the breadcrumb is plain `Text`, and the content pane is the last pane in the row. Compose's spatial search therefore escaped the focus group, found no candidate anywhere, and cleared focus. Pre-existing; MK.31.2 only altered the horizontal directions and the vertical path behaved identically before it. | **Fixed (code) · device-verified** | 2026-07-31 | MK.30.7 + MK.30.8. `ContentPane`'s `focusProperties.exit` returns `FocusRequester.Cancel` for Up, Down and endward, which refuses the exit and keeps focus put. That is the correct end-stop rather than a workaround: **startward (toward the tab rail) is the only direction that legitimately leaves this pane.** `Next`/`Previous` still fall through to `Default` so keyboard TAB traversal on a phone is unaffected. Verified on device: nine consecutive UP presses previously lost focus on the 6th and never recovered; now focus holds from the 5th onward. Endward verified in Arabic (RTL), where it is physical LEFT — focus stops at the last chip and holds. |
| MB-316 | Native | Medium | **MK.30.1 was insufficient — the Settings section header was still clipped on a real device.** The Playback tab opened with its "Video" header and subtitle above the viewport and "Resize mode" as the topmost visible row, with nothing focusable above it to scroll further. Measured: content pane viewport `[808,238][1824,1000]` (762px), scroll stranded at ~163px. Root cause is a limitation of the instrument, not a coding error: a `BringIntoViewSpec` only ever sees the FOCUSED ELEMENT's rect, and the focusable is a chip sitting ~290px below its section's top (section title, section subtitle, row label and row hint all precede it). 96dp of headroom capped at a quarter of the viewport is ~190px, so no value of it can reveal a header 290px further up. Raising the cap would have made the focus cursor ride ~45% down the viewport app-wide to fix one edge case. | **Fixed (code) · device-verified** | 2026-07-31 | MK.30.6. `snapToTopNearStart` — the scroll container knows the one thing the spec cannot, namely how much scroll remains; when the remainder is under a section header's height (140dp) there is no reason to keep it, so it animates to 0. Applied only once motion has settled, so it never fights the bring-into-view animation or a touch drag. Mid-list behaviour provably untouched: the remainder there is far past the threshold so the predicate never fires. `shouldSnapToTop` is pure and tested, including the measured 163px case, the mid-list refusals, negative values, and that a 0 threshold is a clean opt-out. Applied to the eight Column-based Settings surfaces (ScrollState hoisted at each so `verticalScroll` and the snap share one instance). Verified on device stepping UP one press at a time: "Video" now lands at y=286, exactly viewport-top 238 + the 24dp top padding, i.e. the scroll genuinely reached 0. |
| MB-315 | Native / Shared | **High** (was Medium) | **A long EPG import starves every other writer in the app.** (Originally: it held one SQLite write transaction across an entire XMLTV feed. Chunked commits landed 2026-08-21 and did NOT relieve the starvation — see below.) `BulkEpgWriter.Session` does `BEGIN IMMEDIATE` + `DELETE FROM epg_programmes` at `begin()` and only `COMMIT`s after the last streamed batch, which is deliberate — the replace has to be atomic or a mid-import failure would leave the guide half-empty. The cost is that on a large feed that transaction is open for a long time, and the DB is in WAL mode, so readers pass freely but **writers serialise**. Any concurrent write (a schedule state transition, a watch-history position, a favorite toggle) blocks for the remainder of the import. MB-254 was the acute symptom of this on the recording path; this row is the root cause, which MB-254's fix mitigates but does not remove. | **Fixed** | 2026-07-28 | Needs a decision, not a patch: either chunk the import into commit-and-re-begin batches (trades atomicity for latency — a crash mid-import leaves a partial guide), or write to a staging table and swap in one short transaction (costs disk + a rename step, keeps atomicity). The staging-table route looks right but is a real design change; not worth doing blind. **Re-rated Medium -> High on device evidence (2026-08-21).** Reproduced on a Fire TV with 195,847 programmes while verifying MB-351. The starved writer was not a background nicety — it was a **source sync**, frozen at `writing 0/963` for over 20 minutes with the UI showing progress that never moved and no error ever surfacing. logcat: `SQLiteConnectionPool: unable to grant a connection to thread 6347 (DefaultDispatcher-worker-2) with flags 0x2 for 1170.0271 seconds`, with a second worker at 1230 s. Both waiters were sync threads. **The holder was not directly observed** — it is identified by elimination: `BulkEpgWriter.Session` is the only `BEGIN` in the codebase that spans an unbounded amount of work, while every `BulkContentWriter` transaction is per-chunk and commits promptly (`prepareSource` COMMITs before returning). Strong, but inference rather than a captured stack. This entry previously listed the victims as a schedule transition, a watch-history position or a favorite toggle — all recoverable latency. **Add to that list: source sync and source deletion**, both of which need a write transaction and therefore hang outright. Restarting the app is the only recovery, and nothing in the UI tells the user that. A user would read this as "the app is broken", not "a write is queued". Also the likely explanation for a stale source seen with stamps but 0 content rows: `prepareSource` deleted its catalogue and the re-INSERT never got a write connection. **FIXED 2026-08-21 — chunked commits, not the staging table this entry proposed.** The staging route was reconsidered against a fact this entry pre-dated: `AndroidEpgImporter` already splits "Phase 1: downloads (NO transaction held)" from "Phase 2: parse + write", so the network was never inside the transaction and staging would only have moved the insert cost, not removed it — the final swap still re-inserts every row into a five-index table, so writers would still stall for tens of seconds, in exchange for a schema migration and double the disk. `BulkEpgWriter.Session` now commits every `COMMIT_EVERY_ROWS` (5,000) rows, capping the hold at one chunk. **The DELETE is also deferred to the first batch that carries rows, which makes the common failure SAFER than before:** every-source-failed used to wipe a good guide at `begin()` and only then discover it had no replacement. Atomicity is the acknowledged trade: a crash mid-import leaves a partial guide. Covered by a new `epg_import_state` marker in the EXISTING key/value `settings` table (no migration) — committed before any destruction, cleared only in the same transaction as the final rows — plus dropping `epg_last_refreshed` at the moment the guide stops being complete, so staleness logic re-refreshes even for callers that never read the marker. `lastImportWasInterrupted()` is wired into the importer so a partial guide is traceable in logs rather than guessed at. Second acknowledged regression: readers now see the rebuild in progress, where before they saw a complete-but-stale guide until one atomic swap. Judged much smaller than freezing every write in the app. 4 new tests; 4 negative controls each reddening exactly the expected test (remove the mid-import COMMIT; delete eagerly at begin(); stop clearing the marker; keep the stale freshness stamp). 701 shared tests pass, ktlint clean. **DEVICE TEST 2026-08-21 — the chunking did NOT fix the starvation. Status returned to Open.** Reproduced on the Fire TV during a real 201-second EPG write phase (215k rows, 3,110 channels): `SQLiteConnectionPool: unable to grant a connection to thread 6467 (DefaultDispatcher-worker-3) with flags 0x2 for 30.000002 seconds`. The competing writer (a resume-point persist — playback had been started accidentally, which is what finally produced a genuine concurrent writer after two attempts to manufacture one failed) began waiting at ~17:34:35 and was released at ~17:35:06, the moment the import committed. **It waited the entire remaining duration of the import, not one chunk.** If `COMMIT` / `BEGIN IMMEDIATE` were handing the connection to waiters, the worst case would be one chunk — about 5 s at 5,000 rows across 201 s — and the 30 s warning could never fire at all. Mechanism NOT established, and should not be guessed at in the fix: candidates are (a) the gap between `COMMIT` and the immediately following `BEGIN IMMEDIATE` is microseconds, so a waiting thread effectively never wins the race, and (b) Android's pool binds the primary (writable) connection to the transaction's thread and does not re-offer it at these boundaries. Note the pool dump alongside the warning reads `Connections: 0 active, 1 idle, 3 available`, which does not obviously fit (a) and needs explaining before anything else is changed. Next step is a measurement, not a patch: instrument the chunk boundary to log lock hand-off, or test an explicit yield/backoff after each `COMMIT`. **What the commit DID deliver, and what stands:** the deferred DELETE (a refresh where every source fails no longer wipes a good guide — a real and separately valuable fix), the `epg_import_state` marker, and dropping `epg_last_refreshed` at first destruction. All three are device-confirmed: the diagnostics panel read "never refreshed" for the whole 201 s import and flipped to "last refreshed just now" on commit. The chunked-commit machinery is correct in itself (unit-tested, rows survive a rollback only via a mid-import COMMIT) — it simply does not relieve a competing writer, which was the point. **ROOT CAUSE FOUND 2026-08-22, and it is not this bug's stated cause.** Instrumented the chunk boundary and captured the pool dump while a writer was starved: `executeForChangedRowCount started 62392ms ago - running, sql="DELETE FROM content_fts WHERE content_id IN (SELECT id FROM content WHERE source_id = ?)"`. Also observed `DELETE FROM content WHERE source_id = ?` at 47.8 s and **`COMMIT;` itself at 48.9 s**. Those are SINGLE STATEMENTS from `BulkContentWriter.prepareSource` — the catalogue sync — not the EPG import. **A single statement cannot be chunked**, which is conclusively why the commit above could not work. `content_fts` is an FTS4 virtual table and the correlated `IN (SELECT ...)` rewrites inverted-index entries across a 272,419-row catalogue inside the transaction every other writer needs. Second measurement, unexplained and blocking further work: **only ONE chunk commit fired for the entire session** where ~43 were expected. Either the import never got past the DELETE, or the chunk counter does not behave in production the way the unit test says. That must be answered before this code is touched again. **The fix commit `a247050` was REVERTED on 2026-08-22** — it does not address the real cause, only one chunk ever committed, and it had a 5-hour run that ended with the catalogue empty (see MB-353). The deferred DELETE and the interrupt marker from that commit are genuinely valuable and independent of the starvation; they should be re-landed on their own. Re-scoped next step: make `prepareSource`'s FTS cleanup cheap. It already deletes every row for the source, so an unqualified `DELETE FROM content_fts` (or drop + recreate the virtual table) avoids the correlated subquery entirely — needs care on multi-source installs, where the rows for OTHER sources must survive. **MEASURED 2026-08-22, and that re-scoped plan is WRONG — do not implement it.** Benchmarked both forms against an identical 60,000-row FTS4 table: predicated `DELETE ... WHERE content_id IN (SELECT ...)` **0.47 s** vs unqualified `DELETE FROM content_fts` **0.41 s**. No meaningful difference, so there is no `fts3DeleteAll` fast path to exploit and the query is NOT pathological — 272k rows on Fire TV eMMC simply is ~60 s of honest work. Had this been implemented on the hypothesis it would have been the SECOND wrong fix for this bug, and it would additionally have degraded search for other sources mid-sync. **What the bug actually is, restated:** replacing a large catalogue costs ~110 s of deletion (62 s FTS + 48 s content) plus a ~49 s COMMIT, and that work sits in ONE transaction, so every other writer waits it out. Nothing here is a mistake; it is volume. **The only lever measured to work is batching the deletion**, which single statements do not permit but a delete loop does: same fixture, 5,000-row batches with a COMMIT between them cost **+30% total time** (0.68 s → 0.89 s) and cut the longest continuous lock hold **6x** (0.68 s → 0.11 s). Smaller batches trade more total time for shorter holds. Scaled to this device that is roughly a 110 s stall becoming ~18 s at 5,000/batch, or ~4 s at 1,000/batch, against a sync that already runs 8-12 minutes. **Blocking trade-off, decided by the user 2026-08-22: batch at 1,000 rows.** Syncs run unattended in the background; stalls happen while someone is using the app, so sync time is the right currency to spend. **Fixed 2026-08-22.** `clearIfFirstWrite` now deletes in transactions of `CLEAR_BATCH_ROWS` (1,000) instead of one, capping the continuous write-lock hold at one batch. Structural consequence worth knowing: the clear moved OUT of the chunk writers' transactions (it has to commit partway, which is the entire point), so MB-353's delete-and-first-insert atomicity is deliberately given up. MB-353's actual promise is untouched — the clear still only runs from a chunk writer that has rows in hand — but a sync interrupted mid-clear now leaves a partially deleted catalogue, detectable only via the `sync_in_progress` marker. That marker is therefore no longer optional. Per batch: FTS rows first, then the `content` rows the FTS predicate looked them up by; reversing that orphans each batch's index entries. 3 tests added; 3 negative controls. **Two of the three initially failed to redden anything, which was the useful part:** (a) clearing without rows in hand passed because every existing 'sync delivers nothing' test never calls a chunk writer at all, though real providers do — a category returning 200 OK with zero items reaches `writeXxxChunk(emptyList())`; (b) reversing the two DELETEs passed because nothing asserted on FTS, and `searchFts` INNER JOINs `content`, so orphan index rows are invisible unless a channel survives BOTH syncs. Tests added for both; all three controls now redden exactly one test. 704 shared tests pass, ktlint clean, app compiles. **Device run 2026-08-22 — implementation confirmed at scale, starvation relief still UNPROVEN.** On the Fire TV the batched clear ran exactly as designed: `clear[yaman]: removed 272756 rows in 273 batches of 1000`, and the sync completed normally (272,871 items). Zero `SQLiteConnectionPool` warnings — but that proves nothing here, because **nothing was writing**. Playback was deliberately left running as the competing writer and it turns out `persistResumePoint` fires only on transitions (NewTarget / stop / release / lifecycle); there is no periodic ticker, so a playing video issues no writes. That is the THIRD time on this bug a clean run has been mistaken for evidence, which is why the clear now logs its START as well as its end — without a bounded window, "no warning appeared" and "nothing tried to write" are indistinguishable. **To actually verify:** watch for the `clear[...] starting` line, then force a write inside the window — stopping or starting playback triggers `persistResumePoint`, and a favourite toggle also writes. The original 30 s starvation was captured exactly that way, by an accidental PlayerActivity launch landing mid-window. |
| MB-254 | Native | Medium | **Scheduled-recording broadcast receivers did their DB work on the main thread, so a recording firing during an EPG refresh could ANR instead of recording.** `RecordingScheduleReceiver.onReceive` ran the whole fire path synchronously, justified by a KDoc claiming "SQLDelight reads / writes are fast — well within budget". True of the reads (WAL, so readers never wait) and false of the writes: WAL still serialises writers, and the EPG import holds one transaction across a whole feed (MB-315). The FIRST thing `startRecording` does is `transitionTo(FIRING)` — a write — and a failure there logs "aborting fire" and returns, so the recording never starts. Past the ~10 s broadcast budget that is an ANR. `RecordingScheduleBootReceiver` had the same shape and worse exposure: `reconcileAfterBoot` is a bulk UPDATE, BOOT_COMPLETED is the most I/O-contended moment on the device, and a post-update reboot runs SQLDelight's migrations on that same first touch — if it overran, `rescheduleAll` never ran and, since AlarmManager does not persist alarms across reboot, every future recording was silently lost. | **Fixed (code) · awaiting device verification** | 2026-07-25 | MK.30.5. Both receivers now `goAsync()` and run on `Dispatchers.IO`, so a congested write makes the recording start LATE (absorbed by the pre-padding) rather than not at all. Two ordering guarantees preserved explicitly: `PlaybackController` is main-thread-only so its reads and `play()` hop back via `withContext(Dispatchers.Main)`, and MB-208 requires the player switch to be *applied* before the FGS intent — `withContext` suspends until it is, so the switch-then-tee path still cannot mis-route to fresh-GET. `transitionTo(FIRING)` deliberately stays BEFORE the service start: it is the cancellation guard (MB-214), since `transitionTo` rejects illegal transitions, so starting first would record over a schedule the user just cancelled. Residual, accepted: if the write blocks long enough for the exact-alarm temporary allowlist to lapse, `startForegroundService` throws and MB-215's `tryStartOrFailSchedule` marks the schedule FAILED with a reason — visible and diagnosable, versus the previous silent ANR. Also traded: work pending in the goAsync continuation is lost if the process is killed, where the old synchronous path would have finished — but the ANR path lost it too, and a receiver processing a broadcast holds elevated priority. Boot-receiver failure remains recoverable via YancoApp's cold-start reconcile (already on IO). |
| MB-306 | Native | High | **After picking a subtitle pre-play, the player's SUBTITLES panel reported "Off" — then showed it correctly on a second try.** User-reported: "when choosing subtitle and it goes to the options menu, it keeps showing subtitles off … but then i tried again and it worked normal." The panel derived its entire selection state from `player.currentTracks`: `offSelected = disabled \|\| tracks.none { it.selected }`. A side-loaded subtitle is a `MediaItem.SubtitleConfiguration`, and ExoPlayer does not publish it as a track until it has **fetched and parsed the sidecar** — for a provider URL that is a network round-trip after `prepare()`. In that window `tracks` is empty, so "no selected track" and "subtitle still loading" were the same state to the panel, and it picked the wrong one. The retry worked because by then the sidecar had landed. Not a race in the staging path — the subtitle itself was applying correctly the whole time; only the readout was wrong. | **Fixed (code) · awaiting user visual check** | 2026-07-26 | MK.29.6. `_externalSubtitle` promoted from a private `Pair<Uri, String?>?` to a `StateFlow<ExternalSubtitle?>` the panel collects, so "loading" is now representable: while it is set and no track has surfaced, the panel renders the pending subtitle as the selection with a "Loading…" detail line, and `offSelected` excludes that case. It resolves itself live — `onTracksChanged` fires when the sidecar lands and the pending row is replaced by the real track. Four more fixes in the same pass, all found while reading the panel rather than reported: **(1)** `ExternalSubtitle` carries the picker's `label`, so `setLabel` no longer feeds the URI's last path segment into the track name — the panel used to list picks as `1234-Some.Movie.2019.1080p.srt`. **(2)** "Off" now calls a new `clearExternalSubtitle()`; previously it disabled the text renderer but left the subtitle attached to the MediaItem, so the next load (next episode, resume, zap back) silently re-applied it with `SELECTION_FLAG_DEFAULT`. **(3)** `PANEL_MAX_HEIGHT` was a flat `480.dp` while the panel sits `96.dp` off the bottom edge — on the 540 dp TV viewport that puts its top at **-36 dp**, so with a long track list (BluRay rips carry 10+) the first rows sat off-screen unreachable. Now derived from the actual viewport via `BoxWithConstraints`, clamped between a floor and the old value. Same failure shape as MB-300's settings rail: a dp figure that reads fine on a phone and overflows on TV. **(4)** `rememberTextTracks` / `rememberTextDisabled` seeded their state with an unkeyed `remember`, so after MK.9.4's FFmpeg-fallback player rebuild they showed the released player's state until the new one happened to emit an event; both keyed on `player` now. `OptionRow` gained an optional second line, which also un-truncates the online-search rows (release info was concatenated into the label with an em dash and pushed the language off the end of a 380 dp panel). |
| MB-305 | Native | Medium | **Every square brand slot was rendering the 16:9 wordmark lockup, and the launcher icon was an opaque rectangle.** User-reported: "change the logo on the top left from the Y and yancotv to the Y alone … if square then optimize the Y icon to look correctly placed and not stretched wrongly." Root cause is one line in `tools/GenIcons.java`: it center-cropped the source lockup to a square and scaled that into every output. A center crop of a badge+wordmark lockup is not the badge — it is a square of whatever sits mid-canvas, **background included**, which is why `ic_launcher_foreground.png` shipped as an opaque **168x224 dark rectangle** (measured: alpha bbox at threshold 8 and threshold 200 are identical, so the whole plate is solid). Every adaptive mask therefore clipped the *plate's* corners rather than the artwork, and the hex's own points sat flush against the plate edge. Same crop logic threw the wordmark away on the 16:9 TV banner, which had room for it. And since nothing emitted a badge-only asset at all, three square slots had to fit the wide lockup: the collapsed sidebar (92dp wide — measured header box 60x48dp, badge rendering ~15dp across), the About tab (`size(140.dp)`, so Fit rendered 140x78 and left 44% of the box empty), and `RecordingScheduleReceiver`'s notification `setSmallIcon` — where Android renders **alpha only, flat-tinted, in a square status-bar slot**, so the lockup became an unreadable white smear. | **Fixed (code) · awaiting user visual check** | 2026-07-26 | MK.29.5. New rule, encoded in the generator: **the asset follows the shape of the slot** — square/near-square gets the badge alone, wide gets badge + wordmark, and nothing is ever anisotropically scaled. New `ic_logo_mark.png` (drawable + drawable-xhdpi) is the hex alone on real alpha. Adaptive foreground rebuilt transparent at the 66/108 = 0.61 keyline so the adaptive `<background>` colour shows through and the mask cuts colour, not artwork; legacy flat icons composited on `yanco_bg` at 0.78; banner regenerated with the wordmark. Sidebar/About/notification switched to the mark; the Settings header stays on the lockup (its slot is `height(64.dp)` with free width — genuinely horizontal). **Extracting the badge needed care:** the wordmark's opening flourish passes *behind* the badge and re-emerges past its lower-right edge, so no vertical cut separates them — cropping at the badge's right tip still leaves clipped stubs. Alpha-masking against a mirrored silhouette was tried and rejected (the flourish runs through the badge's outer glow where both have coverage, leaving a diagonal hairline visible at icon size); the badge is symmetric about x=137, so the fix is to mirror the flourish-free left half, pixels and all. Cost: the interior leaf engraving becomes exactly symmetric — deliberate-looking on a heraldic badge, and `ic_logo` keeps the original asymmetry wherever the slot is wide. **Note the source:** `yancotv_logo.png` at the repo root is bigger (1536x1024) but is a flat render on an opaque grey backdrop — its alpha bbox is the entire canvas — so `drawable-xhdpi/ic_logo.png` is the only real alpha cut and remains the source. `GenIcons.java` rewritten to match, or the next run of it silently reverts all of the above. |
| MB-303 | Native | High | **Movie / series preview art was cropped to the middle 45% of the poster.** User-reported: "in movies and series the preview photo doesnt show the whole poster". The browse preview pane sized its art box by *row weight* (0.6) and filled it with `ContentScale.Crop`. Measured on the AFTMM via `uiautomator` at the reported state: the box is **444 x 303 dp — a 1.47:1 landscape frame** holding 2:3 portrait artwork (Xtream `stream_icon` / `cover_big` and TMDB `poster_path` are all 2:3). Crop scales to cover, so a 1000x1500 poster rendered 444x666 and the frame showed 303 dp of it: title block and credit block both cut off, on every VOD title. The detail page's own `Poster` had the correct `2f/3f` frame, which is why the bug only ever showed in browse. | **Fixed (code) · awaiting user visual check** | 2026-07-26 | MK.29.1. Preview frame split by type: LIVE keeps the wide box (video is 16:9 and the MiniPlayer shares that surface), VOD gets a height-driven `ShellDim.posterAspect` (2:3) frame — 202 dp wide at the measured 303 dp pane height — centred in a `posterSlotWeight` (0.30) slot, with the reclaimed 242 dp going to the meta column. `ContentScale.Fit`, not Crop, inside it: the frame is already poster-shaped, so Crop would only ever trim art that *isn't* 2:3 (the providers that serve a 16:9 grab under the same field) — precisely the case that needs letterboxing. Sized via `BoxWithConstraints` rather than `fillMaxHeight().aspectRatio(...)`: `fillMaxHeight` pins minHeight == maxHeight, and where the height-derived width doesn't fit (phone portrait wants a 333 dp frame from a ~110 dp slot) every branch of `aspectRatio`'s constraint search fails and it silently falls through to the raw constraints. Same `Fit` change applied to the detail-page poster. |
| MB-304 | Native | High | **Type ramp was too wide at both ends for a 540 dp-tall viewport.** User-reported: "the fonts are too large and not optimized correctly to its surrounding". Measured, not estimated: on the AFTMM (1920x1080 @ densityDpi 320 = **960 x 540 dp**, device confirmed at fontScale 1.0 — 2 lines of the 40/48 sp `DisplayM` measured 190 px against a predicted 192) the browse preview title wrapped to two lines occupying **95 dp of a 303 dp pane, 31% of the pane on the title alone**. At the other end 22 hardcoded sites sat at 10 sp (≈14.5 arcmin at 3 m on a 55" set, under the ~16 arcmin comfortable-reading floor). Largest:smallest ratio was **4.4x** (44/10). This is the "per-role TV type ramp" MB-300 explicitly left open. | **Fixed (code) · awaiting user visual check** | 2026-07-26 | MK.29.4. The ramp is **compressed, not scaled** — floor raised (Caption 11→12, Body 13→14, Label 14→15), ceiling cut (DisplayS 30→26, DisplayM 40→30, DisplayCinematic 44→34), ratio 4.4x → **2.8x**, which is the band TiviMate and the TV Material roles actually occupy. 149 raw literals across 31 files normalised onto the same ramp by pure numeric substitution (no structural edits, so every change is a one-token diff); `Typography.kt` excluded as the source of truth. This closes the loop MB-298 opened: a `fontScale` multiplier cannot fix it, because `Density(density, fontScale x k)` grows `sp` while freezing `dp`, and the gain needed to reach the floor *decreases* with size (11 sp wanted x1.55, 44 sp wanted x1.0) — the ramp needed compressing, the one thing a scalar cannot do. The stale MB-298 comment at the density seam was rewritten to say so. Geometry also recalibrated: `heroHeight` 520→330 dp (was **96% of the 540 dp viewport** — opening a movie showed a full screen of backdrop with title, plot and Play all below the fold; it reads plausibly on a phone, which is how it survived), `detailPosterWidth` 240→200, and the detail hero's bare `220.dp` content offset tokenised to `detailHeroContentOffset` = 140 dp. **Caveat for the visual check:** small text got *larger* by 1 sp — that is the deliberate half of "compression" and is the part most likely to want backing out. |
| MB-299 | Native | **High** | **EPG refresh races the catalog sync at startup, matching against an empty channel table and doing ~140 s of work for nothing — while adding a 77 MB download to the exact window that OOMs.** Device-captured on AFTMM 2026-07-25: the startup `EpgSyncWorker` fires at the same moment auto-sync-on-start begins rebuilding the catalog. `BulkContentWriter.prepareSource` has already DELETEd the source's content rows, so the importer logs `EPG stream: user has 0 live channels with tvg_id`, downloads 77,444,321 B, parses for 64 s and reports `scanned 226822, kept 0 (0 exact + 0 normalised), dropped 226822` — every single programme discarded because there was nothing to match. The coordinator then correctly kicks a SECOND refresh once the catalog lands (`user has 3970 live channels`), so the first run is pure waste. It is not merely wasted: that 77 MB fetch plus its parse overlaps the 279k-item catalog sync, which already peaks at ~313 MB of a 384 MB heap — this is a direct contributor to the MB-230 OOM the user hit by opening the app during a sync. | **Fixed · device-verified** | 2026-07-25 | Policy chosen: tag each refresh with WHY it was requested and only drop the deferrable ones. New `EpgSyncReason` in the work's input data — `AUTO` (startup one-shot + 6-hour periodic), `USER` (a "Refresh EPG" tap), `POST_SYNC` (the coordinator). `EpgSyncWorker.doWork` returns `Result.success()` early when `shouldSkipForActiveSync(reason, coordinator.state.value != null)` — i.e. only for `AUTO`. `success()` not `retry()` because retry burns `runAttemptCount` against `MAX_RETRIES = 3`, which a ~5-minute sync exhausts; dropping is safe because the coordinator re-kicks. Returning immediately also frees the `UNIQUE_ONESHOT` slot so the `POST_SYNC` kick isn't swallowed by `ExistingWorkPolicy.KEEP`. **The load-bearing subtlety:** `kickEpgRefresh()` is called from INSIDE the coordinator's `try`, while `_state` is still non-null — it only clears in the `finally` afterwards. A naive "skip whenever a sync is active" guard would therefore have skipped the coordinator's own post-sync kick and EPG would have stopped refreshing entirely. `POST_SYNC` exists solely to bypass that, and the unit test pins it. `USER` likewise never skips, so a tap can't silently no-op. Decision logic extracted as a pure `shouldSkipForActiveSync` with 6 tests (`EpgSyncReasonTest`), including unknown/missing reason decoding to `AUTO` for work enqueued by an older build. The skip is logged, not silent — "my EPG didn't refresh" is otherwise indistinguishable from a broken feed. **Device-verified on AFTMM:** `21:34:49.706 syncSource start` -> `21:34:50.739 EPG refresh skipped — catalog sync in progress (reason=AUTO)` -> `21:39:45.400 syncSource done (total=279588)` -> `21:39:45.481 syncCoordinator kicking EPG refresh` -> `21:39:45.896 EPG stream: user has 3970 live channels` (was `0 live channels` / `dropped 226822`). One 77 MB download instead of two, zero fatals. |
| MB-294 | Native | High | **Backup export crashed on Fire OS 6 / API 25** — `SettingsBackupTab.makeFilename()` used `java.time.LocalDateTime` (API 26) against minSdk 24 with no core-library desugaring, throwing NoClassDefFoundError the moment the user tapped Export. Same class as MB-241. Found by running `lintRelease` while wiring CI gates. | **Fixed** | 2026-07-25 | `f007ccc`. Replaced with `java.util.Calendar` (API 1), identical filename format, plus `Locale.US` so an Arabic locale can't emit Arabic-Indic digits into a filename. `BackupCoordinator`'s MediaStore NewApi hit was a FALSE positive (caller gates on SDK_INT >= Q; lint can't see through the call boundary) — annotated `@RequiresApi(Q)`. |
| MB-296 | Native | Medium | **Search force-opened the on-screen keyboard.** `SearchField` auto-requested focus onto the `BasicTextField`, and focusing a text field pops the IME — so opening Search (including via the KEYCODE_SEARCH hotkey and the voice deep-link) threw a full-screen keyboard over the results unasked, which on TV then needs BACK to clear. | **Fixed** | 2026-07-25 | `9502105`. Click-to-edit, the pattern already used by `SettingsClickToEditField` / `AddSourceDialog`: focus lands on the row (query rendered as text), keyboard opens only on OK/tap, BACK exits edit mode without closing the overlay. |
| MB-298 | Native | Medium | **TV text too small, and five player surfaces rendered outside `YancoTheme`.** At 1080p/densityDpi 320 the Fire TV viewport is 960x540dp, so 1sp = 2px; an 11sp caption subtends ~10 arcmin at 3 m, under the ~16 arcmin reading floor. Separately, the recording indicator, seek-flash, VOD chrome, VOD dock and channel-surf list called `setContent` bare — outside `YancoTheme` they used the DEFAULT palette (wrong colours on any non-default theme) and ignored the Appearance font-scale preference entirely. | **Fixed** | 2026-07-25 | `89b714e`. 1.30x `fontScale` multiplier at the `YancoTheme` density seam — lifts all ~209 hardcoded `sp` literals plus the ~96 `YancoType` sites at once, composes with the user font-scale pref, scales sp only so no `dp` layout moves; phone unaffected. Prerequisites in the same commit: two hardcoded `YancoTheme(isTv = true)` sites in PlayerActivity now use the real `isTvDevice()`, and the five bare surfaces are wrapped. Per-site sp bumps deliberately deferred — they compound with the multiplier and need an on-device look. |
| MB-302 | Native | Medium | **Pressing BACK to dismiss the search keyboard left NOTHING focused — dead D-pad.** After entering edit mode and pressing BACK the overlay correctly stayed open and dropped to browse mode, but no node carried `focused="true"` and it stayed that way (checked +1 s and +5 s). The next D-pad press triggered Compose's initial-focus fallback and landed on the app sidebar rather than the search row. **CORRECTION: this was a regression introduced by MB-301, NOT pre-existing.** The MB-301 commit message and the first version of this row both claimed pre-existing, on the reasoning that the change "only added a condition that is `true` on the BACK path". That reasoning was wrong — MB-301's new `onFocusChanged` fires *first* and leaves the condition `false`. Recorded here rather than quietly amended, because the wrong claim shipped in `0e04d82`. | **Fixed · device-verified** | 2026-07-25 | Root cause, from an on-device trace (`row focus -> false` at .629 then `EXIT edit: return=false` at .709): **on Fire TV the IME consumes BACK to dismiss itself, so the composable's `BackHandler` never runs on the first press.** The IME teardown drops field focus, MB-301's `onFocusChanged` ends edit mode, and the `returnFocusToRow` flag was still `false` — so the "hand focus back to the row" branch was skipped. The flag was defending against a case that cannot occur on this platform: while the IME is up it owns the D-pad, so the field cannot lose focus by navigation, only by the IME closing. Fix: delete the flag and **always** return focus to the row on leaving edit mode (which is what the pre-MB-301 code did). Same flag removed from `SettingsTextField`. **Earlier hypothesis, tested and REFUTED — do not retry:** stale `PlacedFocusAnchor.isPlaced`. With `fieldAnchor.reset()` added, instrumentation still showed `awaitAndRequest` returning in ~2 ms with focus not landing. `reset()` was kept as correct hygiene (it does stop a request firing at an unplaced node) but it was never the cause. The "spontaneous second edit cycle ~9.6 s later" noted in the first draft of this row was the same IME-driven exit, mis-read from an incomplete trace — not a separate phenomenon. **Verified:** press box -> `mInputShown=true`; BACK -> `false` with focus on the row `[260,48][1712,144]`; press again -> `true`; second BACK exits to the sidebar. Zero fatals. |
| MB-301 | Native | High | **The search box re-opened the keyboard on its own after you had dismissed it.** User-reported: "when i go back it keeps it selected when i move away from it and if i go back to it then it opens the keyboard without pressing anything." Two compounding defects in the click-to-edit field added by MB-296. **(1)** Nothing exited edit mode when the field lost focus — no `onFocusChanged` on the `BasicTextField` — so moving the selector away left `editing == true` and the row still rendered as a live text field. **(2)** `editing` was `rememberSaveable`, so it survived leaving and re-entering Search; on re-entry `LaunchedEffect(editing)` re-ran with `editing` still true and called `editFocus.requestFocus()`, and focusing a text field pops the IME — reintroducing the exact symptom MB-296 existed to remove. The same shape was present in `SettingsTextField`, the shared primitive behind every Settings text input, so it was not limited to Search. | **Fixed · device-verified** | 2026-07-25 | `editing` -> plain `remember` (edit mode is transient interaction state; the query text itself is hoisted and saved separately). Added an `onFocusChanged` that leaves edit mode on a genuine focused->unfocused transition only — guarded by a branch-scoped `hadFocus` flag, because `onFocusChanged` also reports the initial unfocused state before `requestFocus()` lands and acting on that would cancel edit mode the instant it opened. Added a `returnFocusToRow` flag so a DELIBERATE exit (initial entry, BACK, IME Search/Done) still pulls the selector back to the row, while an exit caused by the user navigating away does NOT snap focus back — without that split the field became impossible to leave. Same fix applied to `SettingsTextField.EditableFieldBody` via a new `onFocusLost` callback. **Verified on device with `uiautomator dump` + `dumpsys input_method`:** open Search -> `mInputShown=false`; press the box -> `mInputShown=true`, EditText present; BACK -> `false`; move selector away -> `false`; **return to the box -> `false`** (was: keyboard opened unasked). Note `KEYCODE_SEARCH` opens the Fire TV launcher's own search, not YancoTV's — navigate in-app via the sidebar when testing this. |
| MB-300 | Native | High | **Text overflow had no policy anywhere in the app, and the Settings content pane was half the width it should be.** Two independent defects, both surfaced by a 157-site per-surface measurement pass (2026-07-25). **(1)** `SettingsScreen.kt:413` set the inner Settings tab rail to a bare `380.dp`, transplanted 1:1 from `design_handoff_yancotv/designs/settings.html:109` — whose canvas declares `width=1920` at `:5`. The Fire TV viewport is 960x540dp, i.e. **half the design canvas in units**, so 19.8% of the mock became 39.6% of the screen. The content pane got 368dp and the breadcrumb inside it only 76.84dp of glyph budget, which is why "SOURCES" wrapped to four clipped lines; with the app rail expanded the pane fell to 200dp and the active chip's budget went negative. **(2)** 69 `Text` sites declared `maxLines` with no `overflow`, so they inherited Compose's `TextOverflow.Clip` default — information removed with no visible marker. That is why `CategoryRail` had been silently truncating provider group names for months with no bug report. Worst instances, all broken at font scale 1.0 before any change: `SourcesScreen.kt:334` wrapped from scale **0.686x** (never fitted at any shipped preset, including 90%); `SourcesScreen.kt:344` measured the source *count* at `maxWidth = 0` so the number rendered zero-width; `VodPlayerDock.kt:261` (44sp, no clamp) starved the transport row until play/pause measured to 0 on long titles; `VodPlayerChrome.kt:432` needed 564dp of a 444dp box with no scroll, so **RETRY and BACK were measured at height 0 — a user hitting a stream error had no on-screen way out.** | **Fixed (code) · awaiting user visual check** | 2026-07-25 | Rail 380 → `ShellDim.settingsRailWidth = 240.dp` (content pane 368 → 508dp, +38%; breadcrumb budget 76.84 → ~188dp, taking tab labels that overflow from 3/12 at 1.0x and 11/12 at 1.25x to 0/12 at both). `HexChip` hard `.height(30.dp)` → `heightIn(min = 34.dp)`, padding 22 → 14dp (the `HexCapsuleSoft` bevel only reaches x≈4dp inside the text band, so 22dp was over-provisioned by 18dp/side), plus `maxLines`/`softWrap = false`/`Ellipsis`. Sources header 22 → 20sp with a 2-line clamp; count folded into the overline (`"YOUR SOURCES · 4"`) which removes the zero-width bug at its source. `verticalScroll` + icon 160 → 96dp + headline 48 → 30sp on the error overlay. Ellipsis swept across all 69 sites. Also fixed two `&amp;` HTML entities sitting in Kotlin string literals (`SettingsShortcutsTab.kt:59`, `SettingsPlaybackTab.kt:171`) that rendered five visible characters on the two widest section titles in Settings, and a preset chip `Row` with no `horizontalScroll` whose 4th chip became unreachable by D-pad at 125%. **Note: `MB-298`'s 1.30x global `fontScale` multiplier was reverted (`040516e`) — it is the documented-wrong tool.** `Density(density, fontScale × k)` grows `sp` and freezes `dp`; and the multiplier needed to reach the readability floor is a *decreasing function of size* (11sp needs ×1.55, 44sp needs ×1.0), so no constant is right at both ends. `androidx.tv.material3.TypeScaleTokens` is numerically identical to phone Material 3 — Google's answer to "TV needs bigger text" is larger *roles*, never a global gain. Remaining: the per-role TV type ramp + the XML player-overlay surfaces, which sit outside the font-scale seam entirely (no `attachBaseContext`/`createConfigurationContext` anywhere in `app/src/main`) and therefore ignore both the multiplier and the shipped 90/100/110/125 preference. |
| MB-241 | Native / Shared | Critical | **YancoTV 1.3.7 crashes at startup on Fire OS 6 / Android 7.1 (API 25).** `AndroidPinHasher` initialized `SecretKeyFactory.getInstance("PBKDF2WithHmacSHA256")` in a companion object while constructing the app graph. Android only guarantees that named KDF from API 26, so API 24/25 throw `NoSuchAlgorithmException`, wrapped by `ExceptionInInitializerError`, before the first screen. The same unsupported call was latent in encrypted backup key derivation. Reproduced on AFTMM at `10.0.0.109`; the installed APK hash and release signature matched the public v1.3.7 asset before diagnosis. | **Fixed · installed + verified on AFTMM** | 2026-07-22 | MK.27.HF1. Central Android KDF prefers the platform `SecretKeyFactory` when present and falls back on API 24/25 to RFC-compatible PBKDF2 built from platform `HmacSHA256`; no PIN, backup, database, or wire formats change. Regression tests pin RFC 7914 vectors, Unicode parity with the platform provider, and a bounded work factor. `:shared:testDebugUnitTest`, `:app:testDebugUnitTest`, and minified `:app:assembleRelease` passed; APK package/version/ABI and signer verified before same-signature `adb install -r`; five API-25 cold launches produced distinct live PIDs with zero fatal or missing-algorithm log matches. |
| MB-13 | Mobile | High | Player takes two back presses to close; surface state desyncs between routes | **Fixed** | 2026-04-12 | Collapsed to one route + state-driven fullscreen (2026-04-20) — no modal to pop, back toggles `isFullscreen` then stops |
| MB-14 | Mobile / Native | High | HEVC-main10 / AC3 / EAC3 / DTS / TrueHD decode audio-only on ~30% of streams | **Fixed** | 2026-04-10 | MK.9.1–9.5 shipped 2026-04-25. FfmpegAudioRenderer wired with `EXTENSION_RENDERER_MODE_ON` (hw decoder preferred; FFmpeg only when MediaCodec rejects) — fixes the AC3/EAC3/DTS/TrueHD audio gap on Fire TV. ExperimentalFfmpegVideoRenderer was REMOVED 2026-04-25 (caused ANRs on common HEVC streams — software decode at 1080p borderline on Fire TV Stick class hardware); HEVC main10 dropped from scope. Hands-on regression closed 2026-04-25: user reports no remaining channels with broken audio path on the live source. The previously-broken set is functional; the FFmpeg path activates transparently per stream when MediaCodec rejects the audio format. |
| MB-118 | Native | Medium | Some channels render in fullscreen `PlayerActivity` (SurfaceView) but show black in `MiniPlayer` preview (TextureView) | **Fixed** | 2026-04-25 | Switched `MiniPlayer` from `AndroidView { TextureView }` to `AndroidExternalSurface` (Compose 1.6+ primitive that punches a SurfaceView into the Compose tree with proper z-ordering). Same surface type the fullscreen `PlayerActivity` already used (its `activity_player.xml` declares `app:surface_type="surface_view"`), so HEVC profiles that only output to SurfaceView render correctly in both paths. Surface lifecycle made symmetric: `clearVideoSurface(this)` on `surface.onDestroyed`, re-bind on `Lifecycle.ON_RESUME`, re-bind on `playerRebuilt` after a watchdog rebuild. Shipped `c96d0cc` 2026-04-27. Bug entry was stale on the register; closed by audit 2026-04-28 during the Stage 5 sweep. |
| MB-119 | Native | High | App died on 4K stream playback / fullscreen exit. Multiple symptoms: channel freeze + audio-continues, fullscreen-exit fatal exception | **Fixed** | 2026-04-25 | Three-stage fix landed 2026-04-25, all verified hands-on against Bien Sport 4K class streams (~10 Mbps HEVC + AVC variants) on AFTDCT31 (Fire TV). Stage 1 `2cf37db`: drop `ExperimentalFfmpegVideoRenderer` + switch `EXTENSION_RENDERER_MODE_PREFER` → `_ON` (was forcing software HEVC decode over hardware → mid-stream ANR). Stage 2 `301b5b5`: shrink load-control buffers from 5-Mbps tuned values to 4K-friendly (`maxBufferMs` 20s → 15s; `backBufferMs` 120s → 30s) — eased native-cache GC pressure. Stage 3 `fb927af`: explicit `controller.player.clearVideoSurface()` in `PlayerActivity.onStop()` before `playerView.player = null` — symmetric to `PlayerLauncher`'s pre-launch detach. Without it `ThreadedRenderer.finalize()` raced MediaCodec's pending GPU ops on the way out and `FinalizerWatchdogDaemon` killed the app after 10s (`TimeoutException: ThreadedRenderer.finalize() timed out … nDeleteProxy`). |
| MB-200 | Shared | Medium | M3U sync materialises the entire playlist text + a per-line `List<String>` before parsing. `SourceRepository.syncM3uUrl` calls `http.getText(...)` (one full-body string) and `parseM3u` does `cleaned.replace("\r\n", "\n").replace("\r", "\n").split('\n')` (one line list). For a ~50K-channel M3U (~10MB text) that's ~20MB transient + the parsed entry list — covered by `largeHeap=true` today but wasteful, and a perf headwind we'd want to clear before targeting larger providers. | **Fixed** | 2026-04-26 | Phase 4 / streaming-parser milestone. Plan: switch `HttpClient.getText` callers in this path to a streaming reader (`ktor`'s `bodyAsChannel().toInputStream()` or a `Source`-style reader), then port `parseM3u` to consume a `Sequence<String>` / `BufferedReader.lineSequence()`-style iterator instead of `split('\n')`. Mirror the change in `packages/core` TS port for parity. Deferred — not a regression, current memory profile is acceptable on Fire TV / phone targets. Surfaced 2026-04-26 by an outside review pass. **Fixed — discovered already-fixed on 2026-08-22.** Both M3U paths now stream: `syncM3uUrl` uses `http.getSource(...) { body -> parseM3uLines(body.m3uLineSequence(), logger) }` and `syncM3uFile` the same via `fileReader.readSource`, so the body is never materialised as one UTF-16 String and the parser consumes a line Sequence rather than a `split('
')` list. It was fixed as a SIDE EFFECT of the MB-230 heap work — the code comments cite MB-230, and no commit ever named MB-200, which is exactly why the reconciliation detector filed it under "Open, no fix commit" (presumed genuinely open). **Detector limitation worth remembering: a scan keyed on bug ids cannot see a fix that resolved a different bug in passing.** Only reading the code closed this one. **TS parity deliberately NOT done:** `packages/core/src/parsers/m3u*.ts` still does `split('
')`. That mirror was a parity note in the original plan, not the defect — the defect was a 255k-entry playlist peaking hundreds of MB against a Fire TV Stick's 384 MB heap. Desktop Electron has gigabytes, so a ~20 MB transient there buys nothing. Left as-is on purpose rather than churned for symmetry. |
| MB-201 | Native | Medium | Release builds reuse the debug keystore. `app/build.gradle.kts:83` has `signingConfig = signingConfigs.getByName("debug")` with a "Real signing config arrives in Stage 5.7 (distribution pipeline)" comment. Means `assembleRelease` produces an APK that's installable on test hardware but cannot be uploaded to Play / a sideload-distribution channel without re-signing. | **Fixed** | 2026-04-26 | Generated release keystore 2026-04-28: PKCS12 format, RSA 2048, 10 000-day validity, alias=yancotv, distinguished name `CN=YancoTV, OU=Mobile, O=YancoTV, L=Unknown, ST=Unknown, C=US`. Keystore lives at `packages/android/release.keystore` (gitignored); credentials threaded through `local.properties` (also gitignored) as `release.keystore.path` / `.password` / `release.key.alias` / `.password`. `app/build.gradle.kts` registers `signingConfigs.create("release")` only when all four values are present AND the keystore file exists; release `buildType` picks it up via the same conditional, falling back to debug signing with a Gradle log warning when not configured (so a fresh clone still builds + installs to test hardware). v1, v2, and v3 signing schemes all enabled for max install-target compatibility. Verified post-build with `keytool -printcert -jarfile app-release.apk` — owner = `CN=YancoTV…`, not the AOSP debug signer. Same keystore must sign every future release; password backup discipline is on the dev (password manager + encrypted off-box copy). Existing Fire TV install (currently debug-signed v1) cannot be `installRelease`-d directly because of the deliberate signature-mismatch refusal — that's the keystore working as intended; dev iteration stays on `installDebug`. |
| MB-202 | Tooling | Low | `ktlintCheck` exits successfully even when style violations exist — `ignoreFailures = true` in both `:app/build.gradle.kts:37` and `:shared/build.gradle.kts:14`. Means the lint command always reports green to CI, while real style debt accumulates in the report. The Windows generated-code filter has been fixed (commit `95bbfda`, 2026-04-26) so the report is now scoped to hand-written code only — the burn-down list is the real one. | **Fixed** | 2026-04-26 | Burn-down landed 2026-04-28. Created repo-level `.editorconfig` disabling three rules (`function-naming` for Compose's PascalCase convention, `backing-property-naming` for the StateFlow `_foo`/`foo` pattern, `filename` for multi-decl files) — rationale documented inline. Ran `./gradlew :app:ktlintFormat :shared:ktlintFormat` to auto-fix ~1100 violations across `chain-method-continuation`, `statement-wrapping`, `argument-list-wrapping`, `function-signature`, `class-signature`, `multiline-expression-wrapping`, `indent`, `import-ordering`, `no-unused-imports`. Manual fixups for the remainder: 5 `max-line-length` violations (description-copy in SettingsAboutTab/SettingsGeneralTab/SettingsPlaybackTab/SettingsShortcutsTab/GuideSyncPanel split into multi-line concatenations) + 5 `property-naming` violations (UrlBuilderTest/CatchupServiceTest underscore-style fixture properties renamed to camelCase). Flipped `ignoreFailures` to `false` in both modules. `:app:ktlintCheck` + `:shared:ktlintCheck` both green; CI now blocks new style debt instead of silently accumulating it. |
| MB-203 | Native | Low | `AndroidManifest.xml` sets `android:usesCleartextTraffic="true"`, so EVERY HTTP target is allowed cleartext — not just IPTV provider hosts. Real threat model: an attacker on the same Wi-Fi can MITM provider streams (already cleartext at the provider layer) but ALSO any future telemetry / Sentry / image-CDN HTTP traffic if someone adds an `http://` URL by mistake. The current Sentry / Coil paths are HTTPS-only by construction, so the practical leak surface today is limited to provider streams (which the user accepts because IPTV providers serve plain HTTP). | **Won't Fix · accepted threat-model** | 2026-04-26 | **Closed 2026-05-14 as documentation-only.** Architectural reality: Android's `network_security_config.xml` is static at build time; user-configured provider hosts only exist at runtime, so there is no OS-layer mechanism to allowlist them dynamically. Flipping the manifest to `usesCleartextTraffic="false"` would break every HTTP IPTV provider before our application code sees the request. The yancoxplorer / mobile-readiness scanner will keep reporting this on every rescan; the acceptance is recorded in [AGENTS.md](AGENTS.md) "Cleartext traffic (Android)" section and [docs/security/AUDIT_NOTES.md](docs/security/AUDIT_NOTES.md). **Defense-in-depth track** — an OkHttp-application-layer allowlist seeded from the `sources` table is genuine future work; tracked separately as `MK.SEC.A` / `MK.SEC.B` in [PRODUCTION_PLAN_NATIVE.md](PRODUCTION_PLAN_NATIVE.md) (not part of this MB-203 ticket). |
| MB-211 | Native | Low | Dead `recording_id` FK column on `recording_schedules`. Originally the schedule held a back-reference to its triggered recording; the MB-208 fix shifted to deterministic `RecordingScheduleScheduler.recordIdForSchedule(scheduleId)` derivation and the column has been NULL ever since. The FK constraint is latent — if a future commit revives the link-early pattern, the FK enforcement at UPDATE time will silently fail (write throws, schedule stuck in ARMED). | **Open · deferred (foot-gun closed)** | 2026-04-27 | Higher cost than first scoped: SQLite on min SDK 24 has no `DROP COLUMN` (3.35+ / API 33+) or `RENAME COLUMN` (3.25+ / API 30+), so removal requires the full table-rebuild dance (create new table → copy → drop → rename → recreate indexes). Real cost ~40 lines schema + updating 6 test cases + verifying the just-shipped backup format keeps round-tripping. Deferred — column being dead doesn't break anything today; it's clutter + a latent foot-gun. Pick up alongside the next major schema migration so the rebuild dance amortises. Surfaced 2026-04-27 by recording-subsystem audit. **Re-reviewed 2026-08-22 and the deferral stands — deliberately.** A table-rebuild migration whose only benefit is deleting an always-NULL column is exactly the change that risks data loss, and there is no other pending schema work for it to amortise against. Removal waits for the next major migration, as originally scoped. **The latent foot-gun half is closed at zero risk, though.** The schema header actively MISDESCRIBED the column as live ("shows the recording_id as a link to the recordings browser entry") — that stale comment was the actual hazard, since a future dev reading it would assume the column is in use. Both the header and the column line now state it is dead, why deriving via `RecordingScheduleScheduler.recordIdForSchedule(scheduleId)` is correct, and what breaks if the link-early pattern is revived (FK enforced at UPDATE, write throws, schedule stuck in ARMED). Comment-only — no schema change, no migration. |
| MB-212 | Native | Low | `RecordingScheduleReceiver.handleEnd()` reads the recording row to decide COMPLETED vs FAILED before `RecordingService.handleStop()` finishes flushing bytes asynchronously. In a sub-100ms stop window the schedule can lock as FAILED (`zero_bytes_captured` or `recording_never_started`) while the recording row eventually transitions to COMPLETED — schedule history disagrees with recording history. Both end-states are legal individually; the divergence is the bug. | **Fixed** | 2026-04-27 | Schedule transition for the row-exists path moved from receiver to `RecordingService.handleStop` so it runs INSIDE the same coroutine as `cancelAndJoin` + `output.close()` + `output.size()` + `markCompleted/markFailed` — single source of truth for the byte count + terminal state. The race window is closed because the schedule's transition reads bytes that have already been flushed. `scheduleId` is derived from the `recordId` via `RecordingScheduleScheduler.scheduleIdFromRecordId()` (inverse of the existing `recordIdForSchedule()` `sched-rec-` prefix convention) so no plumbing through `RecordInput` / Intent extras / `activeJobs` was needed — keeps the cross-process state surface minimal. Pure-function extraction `scheduleOutcomeFromBytes(bytes): (state, reason)` in `:shared/commonMain` pinned by `ScheduleOutcomeFromBytesTest` (4 cases: zero, negative, one byte, typical 1.2 GB). Inverse mapping pinned by `ScheduleIdFromRecordIdTest` (round-trip, manual recording, empty, prefix-only, case-sensitive, prefix-not-at-start — 7 cases). Receiver retains a single fallback path: when the recording row is missing entirely (service crashed before `markStarted`) it transitions the schedule to `FAILED("recording_never_started")` since the service won't reach the new transition code in that case. Race-tolerance: cancel-during-fire still works because `transitionTo` rejects terminal-to-terminal moves and both the receiver and the service wrap in `runCatching` — MB-214 already pinned that surface. |
| MB-213 | Native | Low | Live-tee vs fresh-GET URL-match routing logic in `RecordingService.handleStart` and `RecordingScheduleReceiver` is untested. The decision keys on string equality between `playbackController.currentItem.value?.streamUrl` and `recordInput.streamUrl`. No tests cover trailing-slash, query-param, case-difference, or null/empty edge cases — a regression here would silently route every recording to fresh-GET (1-stream cap conflicts) or every recording to live-tee (headless freezes). | **Fixed** | 2026-04-27 | Extracted the routing decision into `RecordingRouting.decide(playingUrl, requestedUrl): RecordingPath` (sealed `LiveTee` / `FreshGet`). Production `RecordingService.handleStart` calls the same function — same string-equality semantics, behaviour preserved. Added a tightened blank-input guard along the way (both URLs must be non-blank). `RecordingRoutingTest` pins the table: same URL → LiveTee; different URL → FreshGet; null/blank/empty → FreshGet; trailing-slash mismatch → FreshGet (deliberate non-normalisation, pinned); query-param mismatch → FreshGet; case difference → FreshGet. |
| MB-214 | Native | Low | `RecordingScheduleRepository.transitionTo()` enforces `isLegalTransition()` but no tests cover concurrent transitions: end alarm firing while user manually cancels the schedule, schedule deletion racing the end alarm, transitionTo against a missing row. Race conditions today degrade gracefully (legal-state guard catches most), but a future refactor could introduce a window where two transitions race. | **Fixed** | 2026-04-27 | Added three race-coverage tests in `RecordingScheduleRepositoryTest`: `endAlarmAfterCancelIsRejected` (terminal→COMPLETED throws), `secondTerminalTransitionFromFiringIsRejected` (whoever-wins-wins, loser throws cleanly), `deletedScheduleEndAlarmThrows` (post-delete transition throws IllegalStateException not NPE so receiver runCatching catches it). Pre-existing `cannotMoveTerminalRowAtAll` + `transitionThrowsOnUnknownId` cover the rest of the surface. |
| MB-215 | Native | Low | The MB-208 fix wraps `RecordingService.start()` in `runCatching` so a `ForegroundServiceStartNotAllowedException` (Fire TV background-restriction edge) transitions the schedule to FAILED instead of leaving it stuck in FIRING. No test verifies the transition actually happens, the reason string is meaningful, or the recording row stays absent. Future maintainers may simplify the runCatching away under the assumption it's defensive paranoia. | **Fixed** | 2026-04-27 | Extracted the wrap-and-fail logic into top-level `tryStartOrFailSchedule(scheduleId, tag, startService, transitionFailed)` so a JVM unit test can pin the failure path without standing up a Receiver/Service/Context. `TryStartOrFailScheduleTest` covers: success path doesn't call transitionFailed; thrown call routes to transitionFailed with `"service_start_failed: <message>"`; null-message exception falls back to class name. Production receiver call site updated to use the helper. |
| MB-216 | Native | Medium | No end-to-end test for the orphan-recovery flow after app kill mid-recording. The two halves (`RecordingsRepository.sweepOrphans` reaping RECORDING rows, `RecordingScheduleRepository.reconcileAfterBoot` reaping FIRING schedules) are individually tested but the integration — crash mid-recording → reboot → both sweeps run → UI reflects history correctly — is not covered. Highest probability of latent regression as Stage 3.1 features evolve. | **Fixed** | 2026-04-27 | Added `OrphanRecoveryIntegrationTest` under `:shared:androidUnitTest`. Two cases: (1) crash-mid-recording happy path: seeds a RECORDING row + FIRING schedule with timestamps backdated via crash-time clock, runs sweepOrphans then reconcileAfterBoot at reboot-time clock, asserts both rows transition (RECORDING→FAILED with `orphaned_by_app_kill`, FIRING schedule reaped via the orphan path); (2) negative case: fresh row + ARMED schedule still in window, both sweeps must be no-ops. Pinning that the production order in YancoApp.onCreate + RecordingScheduleBootReceiver doesn't double-count. |
| MB-217 | Native | Medium | Imported COMPLETED recordings from a MK.19.8 backup whose `file_path` URI doesn't resolve on the target device (file moved, SAF tree URI from a different device, MediaStore entry from a different volume) appear in the Recordings browser but fail with a cryptic `MediaCodec` / `IOException` when the user hits Play. The recording row is technically valid; the file is missing. | **Fixed** | 2026-04-27 | `BackupImporter` constructor now takes an injectable `recordingFileExists: (fileUri) → Boolean` callback (default `{ true }` preserves prior behaviour; Android caller wires `DocumentFile`/`ContentResolver`-based check). `importRecordings()` calls it for every COMPLETED row pre-insert; missing files land as `status = "failed"` + `error = "file_not_found_post_restore"`. Recordings exported in non-terminal states (RECORDING / FAILED / CANCELLED) skip the check — already orphans, browser handles non-playable status. Reused the existing `failed` enum value so no schema migration. Test: `importRecordings_missingFileUri_landsAsFailed` pins the contract. |
| MB-218 | Native | Low | `RecordingOutput` (`FileBackedOutput`, `MediaStoreRecordingOutput`, `DocumentBackedOutput`) requires the caller to manually invoke `onFinalize()` AFTER `cancelAndJoin()`. Easy to forget; an oversight leaves MediaStore files in `IS_PENDING=1` state (invisible in Gallery), or a SAF FileDescriptor leaked. Today there's exactly one caller (`RecordingService.handleStop`) and it does call onFinalize, but the manual lifecycle is fragile. | **Fixed** | 2026-04-27 | `RecordingOutput` now extends `AutoCloseable`; the previous `onFinalize()` is renamed `close()` (default no-op for File/SAF, MediaStore overrides to flip `IS_PENDING=0`). Single call site updated: `RecordingService.handleStop` now calls `output?.close()` instead of `output?.onFinalize()`. Same semantic, but anyone reading the code now sees the standard `Closeable` surface and can use `output.use { }` going forward. Doc references updated. |
| MB-219 | Native | High | `RecordingService.handleStop` runs `cancelAndJoin → output.close → output.size → markCompleted` in sequence. Process death anywhere between `cancelAndJoin` and `markCompleted` leaves the file flushed to disk but the row in `RECORDING`. On the next boot, `RecordingsRepository.sweepOrphans` blindly transitioned every stale `RECORDING` row to `FAILED("orphaned_by_app_kill")` — losing a perfectly-playable recording behind a "Failed" badge. Real-world likelihood is non-trivial on Fire TV: the device aggressively reaps background processes, and the stop window can span hundreds of milliseconds when MediaStore's `IS_PENDING=0` flip queries the resolver. User reports "scheduled recording may be broken" on 2026-04-27 are consistent with this failure mode (recording happens, file lands on disk, row appears as FAILED next session, user assumes data is gone). | **Fixed** | 2026-04-27 | `RecordingsRepository` constructor now takes an injectable `fileBytesIfExists: ((fileUri) → Long?)?` (default `null`, preserving prior behaviour for KMP / test targets). `sweepOrphans` calls the hook for every stale `RECORDING` row: a non-null byte count ≥ `MIN_RECOVERED_BYTES` (64 KB — filters HTTP error bodies and header-only captures) salvages the row to `COMPLETED` with the on-disk byte count + a wall-clock duration derived from `(now - started_at)`; smaller / missing files fall through to the existing `FAILED("orphaned_by_app_kill")` path. Android wires the hook to `recordingFileBytesResolver(context)` in `AppModules.kt` — handles `content://` (MediaStore / SAF via `ContentResolver` + `OpenableColumns.SIZE`), `file://`, and bare absolute paths via `File.length()`. Tests in `RecordingsRepositoryTest`: `sweepOrphans_fileHookFindsBytes_marksCompletedNotFailed`, `sweepOrphans_fileHookReturnsBelowFloor_marksFailedNotRecovered`, `sweepOrphans_fileHookReturnsNull_marksFailed`, `sweepOrphans_noFileHookConfigured_legacyAlwaysFailedBehaviorPreserved`. Hands-on Fire TV verification of the recovery itself requires forcing a process kill mid-flush (`adb shell am kill com.yancotv.android` between recorder finish and DB commit) — deferred to next user-driven session. |
| MB-220 | Native / Shared | **Critical** | Every source sync silently wiped the user's favorites + watch_history for that source. Schema declares `favorites.content_id` and `watch_history.content_id` as `REFERENCES content(id) ON DELETE CASCADE`. With foreign_keys=ON (production default — `DatabaseFactory.android.kt#onOpen` calls `setForeignKeyConstraintsEnabled(true)`), the `DELETE FROM content WHERE source_id = ?` inside `BulkContentWriter.prepareSource()` cascades and silently wipes every dependent row. `ContentIds.*` are deterministic, so the chunked re-INSERT recreates the same content_ids — but the favorites + history rows are already gone. **User-visible symptom:** after any source sync, the home screen drops Continue Watching, Favorites, On Now, Up Next, and Hero rails (all of which read from favorites or watch_history). Reproduced 2026-04-28 on user's device — favorites table empty, watch_history empty, source last-synced minutes earlier. Confirmed via direct SQL test: insert favorite → DELETE FROM content WHERE source_id = ? → favorites count = 0. Latent for months; not caught by any test. | **Fixed** | 2026-04-28 | `acc86e9`. Toggle `PRAGMA foreign_keys = OFF` across the prepare → chunks → finish window in `BulkContentWriter` so the cascade doesn't fire on sync's delete-then-recreate. `finishSource` sweeps genuinely-stale dependents (favorites pointing at content the provider rotated out) via `DELETE FROM favorites WHERE content_id NOT IN (SELECT id FROM content)` + same for `watch_history`, then re-enables FK. `abortSource` re-enables FK on the error path. If the process crashes mid-sync, `DatabaseFactory.android.kt#onOpen` re-arms FK on next launch. PRAGMA foreign_keys is a no-op inside a transaction (per SQLite docs), so all toggles happen outside BEGIN/COMMIT. Two new regression tests in `BulkContentWriterTest`: `prepareSource preserves favorites and watch_history when content is recreated` (re-syncs same channels, asserts user data survives), `finishSource sweeps orphan favorites and watch_history when content removed` (re-syncs without ch2, asserts ch2 cleaned up while ch1 stays). |
| MB-221 | Native | Medium | Main app sidebar (HomeScreen) feels laggy on focus — when collapsed and the user navigates back to it, expansion doesn't fire immediately. Two compounding causes: (a) `HomeScreen.kt:184-199` `LaunchedEffect(section)` issues a hard `delay(120)` before flipping `panelFocus` and calling `mainContentFocus.requestFocus()` — the user perceives ~120 ms of "not responding" before any visible motion starts; (b) `AppSidebar.kt:100-105` width animation uses `animateDpAsState(spring(0.85f, 320f))` which settles in ~280-320 ms and carries a small overshoot that reads as a wobble at 10 ft on a 168 dp delta (92 → 260); (c) `AppSidebar.kt:415-419` wraps every row label in `AnimatedVisibility(expandHorizontally/shrinkHorizontally)` — that's ~9 simultaneous layout-shifting animations during sidebar open which contend with the parent width animation on Fire TV's modest GPU; (d) `AppSidebar.kt:325-338` adds two `animateColorAsState` (sidebar-fg) plus an `animateFloatAsState` accent-bar spring per row, ~18 concurrent value animations during a width transition. Total perceived: ~370 ms gap before the sidebar feels open, where ~250 ms is fixable. Surfaced 2026-04-28 by motion auditor. | **Fixed** | 2026-04-28 | `94bb577`. MK.22.A. (a) Dropped the `delay(120)` preamble — replaced with one `withFrameNanos { }` for layout-pass settle. (b) Replaced spring with `tween(180, FastOutSlowInEasing)` — width-only animation, no need for physics. (c) Replaced per-row `AnimatedVisibility(expandHorizontally + fadeIn / shrinkHorizontally + fadeOut)` with a single shared `labelAlpha` derived from the parent width animation, applied via `Modifier.alpha`. (d) Dropped per-row `animateColorAsState` (foreground = ternary) and `animateFloatAsState(spring 420)` for the accent bar (now hard switch on focus/selection). Removed ~9 layout-shifting animations + ~18 value animations from the sidebar-open path. User confirmed "yes its better" on hands-on Fire TV verification 2026-04-28. |
| MB-222 | Native | Medium | `OnNowTile` ([HomeContent.kt:897](packages/android/app/src/main/java/com/yancotv/android/ui/shell/HomeContent.kt:897)) captures `val nowSec = remember { System.currentTimeMillis() / 1000 }` ONCE at first composition and never refreshes it. Every "On Now" tile shows a programme-progress percentage based on a frozen baseline — bars never advance while the user sits on Home. Existing pattern in `GuideScreen.kt:330-335` already lifts `nowSec` to a single ticking `LaunchedEffect` (30 s cadence) and passes down via parameter — that's the right shape; OnNowTile just doesn't follow it. Surfaced 2026-04-28 by motion auditor. | **Fixed** | 2026-04-28 | `94bb577`. MK.22.A. Lifted `nowSec` out of the per-tile composable into a single `mutableStateOf` at the `HomeContent` root, ticked every 30 s via a `LaunchedEffect(Unit) { while (true) { nowSec.value = ...; delay(30_000L) } }`. Threaded through `OnNowRail` → `OnNowTile` via parameter. Mirrors `GuideScreen.kt:330-335`'s shape. |
| MB-223 | Tooling / Shared | Medium | 10 `:shared:androidUnitTest` test files broke at compile time when commit `f157250` ("feat(android): per-source 'auto-sync on app start' toggle") added the `auto_sync_on_start INTEGER NOT NULL DEFAULT 0` column to `sources` via `9.sqm` and updated the SQLDelight `insert` query to require all 21 columns explicitly. Test helpers in `BulkContentWriterTest`, `ContentWriterTest`, `EpgRepositoryTest`, `BulkEpgWriterTest`, `FavoritesRepositoryTest`, `WatchHistoryRepositoryTest`, `SourcesBackupTest`, `BackupRoundTripTest`, `ContentRepositoryTest`, `MigrationTest` all called `db.sourcesQueries.insert(...)` without `auto_sync_on_start` — the generated Kotlin signature requires it (no SQLDelight-side default), so every one was a compile error. Suite was unrunnable from `f157250` (2026-04-27) until 2026-04-28; this hid the latent MB-220 CASCADE bug because nobody could write or run a sync regression test. CI presumably never caught this either — flag for follow-up. | **Fixed** | 2026-04-28 | `acc86e9`. Patched all 10 sites with `auto_sync_on_start = false` (the schema's `DEFAULT 0` semantically). Suite compiles and `:shared:testDebugUnitTest` is green. Follow-up MB-224 filed for the no-CI gap. |
| MB-225 | Native | Low | **`PlaybackController` two-tap launch guard is unpinned.** The `native-android-mk` skill checklist says every launch site must guard `controller.currentId == target.id` before calling `play(...)` again — without it, a second tap on the same channel/movie recreates the `MediaItem`, drops the buffer, and rebuffers from scratch (visible as a black flash + 1–3 s spinner). No test pins this contract. A future refactor that "simplifies" the guard out would silently regress two-tap UX on every shipped tile. | **Fixed** | 2026-04-28 | MK.24 Sprint E (slice 24.E.3). Sprint E read-the-file-first gate confirmed the guard exists at **both** layers: call-site-level (per skill checklist) AND controller-level as defense-in-depth (`PlaybackController.play` line 470 + `play(episode)` line 503 check `_currentItem.value?.id == target.id` and early-return without `loadCurrent()`). Extracted `playLaunchDecision(list, startIndex, currentId)` and `episodeLaunchDecision(episode, currentId)` as pure top-level internal functions returning `Reject` / `SameTarget` / `NewTarget` — analog to MK.23.C.1's `resumePointDecision`. Production `play()` and `play(episode)` now `when`-branch on the decision. Test: `PlayLaunchDecisionTest.kt` (~14 cases): empty list, out-of-range, series container, blank URL, null currentId, same/different currentId, multi-item list with various startIndex values, episode same/different id, episode currentId pointing at series-id-not-episode-id (guards against confused state). Manual cascade-nav smoke per skill checklist still covers call-site-level guard. |
| MB-226 | Native / Shared | Low | **`reminders.programme_id` design pin missing.** `reminders.programme_id` is plain `TEXT` (no FK) — deliberate, because reminders need to survive provider-flip programme-id churn (`recording_schedules.programme_id` has the FK with `ON DELETE SET NULL`, but reminders specifically should NOT cascade or null on EPG re-sync). No comment or test pins this design choice. A future "tidy up" refactor that adds the FK to reminders would silently delete or null user reminders on every EPG sync. | **Fixed** | 2026-04-28 | MK.24 Sprint E (slice 24.E.4). Sprint E read-the-file-first gate confirmed: `BulkEpgWriter` doesn't touch reminders by construction (no FK = no cascade), and `selectByProgrammeId` / `deleteByProgrammeId` are explicit user-action queries, not implicit triggers. **A test that can only fail when the schema is already broken is low-value.** Shipped a load-bearing schema comment in `Reminders.sq` documenting the design pin (why it's not FK, how UI falls back when the programme row is gone, links MK.24.E.4 + MB-226). Future "tidy up" refactor that adds the FK now has to delete an explicit "DELIBERATELY NOT FK" warning in the same diff — same protection a test would give, lower cost. |
| MB-227 | Native / Shared | Medium | **Per-migration isolation is missing for `3.sqm` … `8.sqm`.** Today the test suite covers v3 → current as one bundle (`Stage2MigrationTest`) and v9 → v10 in isolation (`MigrationTest`). The middle hops (v3→v4, v4→v5, v5→v6, v6→v7, v7→v8) are not isolated — a regression in `5.sqm` could be silently masked by a downstream fixup in `6.sqm` or by an existing post-migration data assertion. | **Fixed** | 2026-04-28 | `bf12d65`. MK.24 Sprint G.1. Added 5 per-hop isolation tests to `MigrationTest.kt`: `migrationV3ToV4AddsRecordingSchedulesAndRecordingsFormat` (3.sqm), `migrationV4ToV5SeedsDefaultListAndBackfillsLegacyFavoritesListId` (4.sqm — load-bearing UPDATE backfill, seeds 2 legacy favorites so a "first row only" bug is caught), `migrationV5ToV6AddsSourcesRefererAsNullForExistingRows` (5.sqm), `migrationV6ToV7AddsSourcesEpgPriorityDefaultingToZero` (6.sqm), `migrationV7ToV8CreatesBackupMetadataTable` (7.sqm). Each test uses a hand-crafted source-version schema fixture (only tables that hop touches + FK targets); no shared cumulative helpers so each test is independently maintainable. Some tests read post-migration state via direct driver SQL because SQLDelight's generated query API expects the CURRENT (v10) shape and fails on intermediate-version tables (e.g. `recording_schedules.selectAll` expects `series_key` from 8.sqm). Note: 8.sqm itself isn't isolated here — `series_key` is the only column it adds and was already implicitly covered by `Stage2MigrationTest` reaching the current schema; if a regression there matters, file as a follow-up. |
| MB-233 | Native | High | **Casting from the phone strands the user in a stuck, touch-dead player options menu over a paused video.** After picking a cast target ("Play on TV" → a discovered TV, or "Cast to Chromecast"), the per-category panel closed but the ROOT options menu (Audio / Subtitles / Aspect / Speed / Sleep / …) stayed up and was non-responsive to touch — tapping a row just re-opened that category; the only exit was the phone's system Back. Root cause: both cast paths called `onPickOption()` → `state.closePanel()`, which for a top-level category (`PLAY_ON_TV`) sets `activePanel=null` but leaves `menuVisible=true`, so the root menu re-renders (`PlayerOptionsMenu` is gated on `visible && activePanel==null`); neither path called the host's `onDismiss()` (=`hideOptionsV2`). Separately the local player was left paused (Chromecast) / kept double-playing (LAN never paused). Surfaced 2026-06-15 during MK.26 phone testing. | **Fixed** | 2026-06-15 | `PlayOnTvPanelContent` now takes the host's full-dismiss `onDismiss` instead of `onPickOption`. LAN `send()` runs the POST first (the menu must stay composed or its `rememberCoroutineScope` cancels mid-send), then dismisses the menu and — on `Accepted` — `finish()`es the sender player so the phone returns to the app instead of a paused frame; Chromecast dismisses the menu before showing the system chooser. Built + installed. Deeper touch-dismiss affordances + the Chromecast end-state are tracked under MK.27. |
| MB-232 | Native | High | **Any Settings tab with a `SettingsSlider` crashes on a PHONE** — `java.lang.IllegalArgumentException: Padding must be non-negative` at `SettingsPrimitives.kt:397`. The knob is positioned by `.padding(start = knobX)` where `knobX = (maxOffset.value * progress).dp` and `maxOffset = maxWidth - knobSize` comes from a `BoxWithConstraints`. During the first/transient measure pass `maxWidth` can be `0` (or `< knobSize`), so `maxOffset` goes negative → negative `knobX` → `padding` throws and takes down the whole tab. Bit the **Network** tab (Connect/Read-timeout sliders) on the phone's narrower layout — which is what blocked the user from reaching the "Play on TV" pairing-code field (mis-attributed at first to the cast feature). Latent on TV (the wide layout never produced a sub-knob width). Surfaced 2026-06-15 during MK.26 phone testing. | **Fixed** | 2026-06-15 | One-line clamp in the shared slider primitive: `val maxOffset = (maxWidth - knobSize).coerceAtLeast(0.dp)` — the knob pins to the start until the real width resolves; fixes every slider. **Verified on-device:** phone crash buffer clean after the fix, Settings → Network opens normally. ktlint-clean. |
| MB-231 | Native | High | **`PlayerActivity` is D-pad-first and barely usable on a PHONE: no touch path to the controls, no on-screen Cast button, no on-screen Back.** Three compounding gaps surfaced 2026-06-15 during MK.26.A.3 phone testing: (1) the VOD dock opened **only** on `KEYCODE_DPAD_CENTER` — there was no touch handler, so tapping a movie/series did nothing; (2) the "Play on TV" / cast affordance was buried (live had no touchable way into `showOptionsV2` at all; VOD only via a tiny dock "MENU" chip), so the user found "no cast button anywhere"; (3) the player had no on-screen Back — exit relied on the system back gesture only. | **Fixed** | 2026-06-15 | Three-part phone control layer in `PlayerActivity` + `activity_player.xml`. **(1) Touch:** a `GestureDetector` on the PlayerView (`controllerHideOnTouch=false`) — single tap routes through `onPlayerSingleTap()`, mirroring the D-pad CENTER path (LIVE toggles the Media3 controller, VOD toggles the Compose dock). **(2) Phone chrome:** on-screen **Back** (`ic_player_back` → `finish()`), **More** (`ic_player_more` → options popup), **Cast** (`ic_player_cast` → `showOptionsV2(PLAY_ON_TV)`), placed ABOVE the VOD dock in z-order, gated by `updatePlayerChrome()` (phone only, while the live controller OR the VOD dock is visible; hidden on TV via `isTvDevice()`, which was confirmed `false` on the phone via `dumpsys uimode`). `focusable=false` so none enter D-pad traversal; zap bar nudged right (phone only) to clear the Back button. **Verified on-device (HT74J0206349, landscape):** movie tap → dock + Back/Cast/More all `V` in the view hierarchy; Cast → "PLAY ON TV" panel showing "Cast to Chromecast"; live tap → same chrome `V`; Back button → player finishes to MainActivity. ktlint-clean. |
| MB-229 | Native | **High** | **Scheduled recording shows "recording" AND "failed" at the same time** — symptom of MB-230 (heap-pressure / OOM), NOT a recording-subsystem race. Live device investigation 2026-04-28 captured a YancoTV process pinned at 376/384 MB heap (98% full), 101 consecutive GC events freeing 0 bytes, threads blocked 12+ seconds on `WaitForGcToComplete`, `am dumpheap` returning a 0-byte file because the process couldn't respond, recording_schedules + recordings tables both 0 rows despite the `RecordingService` running foreground for 12+ minutes with a "Recording 1 channel" notification, output file at 0 bytes. The "recording AND failed" UI is two Compose surfaces showing inconsistent cached state because their reactive flows can't update — the underlying issue is memory exhaustion. | **Fixed** (dual-state gone; 0-byte output split to MB-355) | 2026-04-28 | Stop trying to fix MB-229 directly. Once MB-230 (the heap pressure root cause) is fixed, retest a scheduled recording end-to-end on Fire TV. **UNBLOCKED 2026-08-22 — MB-230 is closed, so the precondition this entry was waiting on is met and the retest below is now the next action, not a blocked one.** If the dual-state UI persists with a healthy heap, escalate MB-229 to a real recording-subsystem race and pursue the original 4-hypothesis path (a/b/c/d in earlier filing). Most likely outcome: MB-229 closes by transitive fix when MB-230 lands. **RETEST DONE 2026-08-22 — that is exactly what happened, and the entry's diagnosis was half right.** Scheduled a real recording on a Fire TV with a healthy heap (Java heap 68 MB, versus the 376/384 MB pinned state this bug was filed against): Brentford vs Tottenham, 15:13-18:00 on beIN SPORTS 1, scheduled at 14:49 and fired at 15:12:30. **The dual state did NOT reproduce.** Every surface showed one coherent status throughout: `Scheduled` before the fire, then `Recording · ends 6:00 PM` with a REC badge, then `Stopped` after I stopped it, with SCHEDULE HISTORY reading `Cancelled`. At no point did any surface say "failed" while another said "recording". The `recording_schedules` row was written immediately on scheduling ("UPCOMING · 1"), where the original capture had **0 rows** despite the service running foreground for 12+ minutes — that was the memory-exhaustion signature and it is gone. **But the 0-byte output DID reproduce, on a healthy heap** — so that half of this entry's diagnosis was wrong. It was filed as a symptom of MB-230's heap pressure; it is not. Split out as **MB-355** rather than left buried here, because it is a distinct and more serious defect: the app reports "Recording" for four minutes while writing nothing. Closing MB-229 for the symptom it actually describes. Forensic data preserved at `D:/tmp/mb-229-capture/` (314 MB DB pre + post recording attempt, PID-filtered logcat showing GC thrash). |
| MB-230 | Native | **Critical** | **YancoTV process exhausts heap (376/384 MB, 98%) and enters runaway GC after sustained use**, causing every reactive flow + DB write + file write to silently stall. Captured 2026-04-28 on Fire TV (AFTDCT31, API 28, 384 MB heap budget) with the user's actual install state: 1 source, 255,843 content rows, 191,564 EPG programmes, 1 favorite, 1 watch_history. Process had been alive several hours when the symptom hit. 22 seconds of PID-filtered logcat shows 101 `Alloc concurrent copying GC freed 0(0B)` events at 376MB/384MB pinned, plus `WaitForGcToComplete blocked Alloc on HeapTrim` waits escalating from 2s → 12s. Force-stop recovered the device. **Failure mode is silent:** no OOM crash, no Sentry event, no user-visible error — the app just stops making progress while the Compose UI shows whatever cached state was current at the moment of the stall. | **Fixed** | 2026-04-28 | MK.24 Sprint I (refocused). **Investigation 2026-04-28 ruled out the obvious culprits:** EPG sync uses `AndroidEpgImporter` (streams to temp file + XmlPullParser + 500-row flush — well-designed); content/EPG queries all use `*Paged` variants (no naive `selectAll` on giant tables); idle Java heap is stable at 12 MB for 90s (no background-timer leak); per-screen Java heap stable 18-22 MB across Home/Live/Movies/Series/Guide/Search/Settings (no single-screen retention bug). **Confirmed gaps:** `onTrimMemory(RUNNING_CRITICAL)` releases only -0.5 MB Java with Graphics + Native unchanged (= no `onTrimMemory` hook in `YancoApp` + Coil bitmap cache doesn't honor pressure signals); `OkHttpClient()` constructed fresh in `YancoImageLoader.kt:41` separate from `PlaybackController`'s OkHttp (= duplicate connection pool, dispatcher, DNS cache, TLS session cache). **Diagnosis:** "1000 cuts" leak — slow per-navigation accumulation + Coil cache that doesn't release on pressure + duplicate OkHttp infrastructure compound past the 384 MB heap budget over a long session. **Fix shipped (Sprint I.7, 2026-04-28, commits `f9d966f` + `a71edfe`):** F1 `onTrimMemory` hook in YancoApp clearing Coil cache on `RUNNING_LOW`/`CRITICAL` — **verified on Fire TV: -3 MB Java drop on `RUNNING_CRITICAL` (vs pre-fix -0.5 MB), 6× the reclaim**; F2 shared OkHttpClient between Coil + AndroidEpgImporter (eliminates duplicate connection pool + dispatcher + DNS + TLS); F3 heap-watermark Sentry probe (60s poll, WATCH breadcrumb >75%, ALERT event >90%) — armed for first wild occurrence. **Soak verification pending:** F3 will fire in the wild if heap pressure recurs over a long session; that becomes the regression check. **MB-229 retest** waits on this — schedule a recording after 1+ hour of normal use, confirm UI shows a single coherent state. **Deferred to Sprint I follow-ups (24.I.X.1–6):** high-heap hprof dominator analysis (needs reproduction of 376 MB state — F3 unlocks this), RGB_565 bitmap config for logos (visual review needed), EPG-sync retention deep-dive (speculative, no direct evidence), MB-229 recording retest (blocked on this), Ktor connection-pool audit (separate from F2's Coil/PlaybackController dedup), Compose recomposition retention audit. Forensic data preserved at `D:/tmp/mb-229-capture/`. **ROOT CAUSE FOUND 2026-07-25 (`37d4973`), and it is not "1000 cuts" — it is one uncapped allocation the 2026-04-28 investigation never reached.** That pass ruled out EPG sync (correctly — `AndroidEpgImporter` streams) and confirmed all *queries* were paged, but never audited the **M3U parse path**, which is the most common source type. `syncM3uUrl` called `http.getText` (whole playlist buffered to one String, no `maxResponseBytes`), then `parseM3u` did `substring` → `replace` → `split`, ~4-5 full UTF-16 copies of the playlist. Fixed by streaming the parse (`M3uLineSource`). **SECOND ROOT CAUSE FOUND + FIXED 2026-07-25, in the XTREAM path — this is the one the user actually hit.** The M3U fix did not help an Xtream source, and v1.4.0 still OOM'd on every launch on AFTMM (279,588-item provider). Two independent defects, both in `AndroidKtorHttpClient.getSource`, the very class written to *prevent* this: **(1)** `streamToFile` used Ktor's `channel.copyTo(fos, limit)` believing it streamed through a small internal buffer. It does not when `limit` is unbounded — it resolves the whole body first, so the catalog arrived as ONE contiguous array before a byte reached disk. Device-captured: `W/art: Throwing OutOfMemoryError "Failed to allocate a 72213488 byte allocation"` — 72,213,488 B is the `get_vod_streams` body (177,364 items). The temp-file spool was defeated by its own copy step. **This retro-explains why making the three catalog fetches sequential changed nothing** (peak 318 → 313 MB, 65% slower): the largest single array is one catalog's whole body, which concurrency does not affect — so that earlier negative result was measuring around this bug, not disproving a memory theory. **(2)** With (1) fixed it still OOM'd, now on *small* allocations against a full heap. `performGet` ran before any throttle, so all three catalog requests were issued at once and two had nobody draining them while the third spooled; Ktor buffers whatever the server has sent but the consumer has not read, and eMMC is slower than wifi. Measured: heap was 114 MB/384 MB at the moment the *smallest* catalog (20 MB) finished spooling, before any decode. Fixed with a 64 KB bounded copy loop + a process-wide `Semaphore(1)` covering **request + drain** (an earlier version wrapping only the copy did nothing, which is what proved the request itself had to be gated). Decode stays outside the permit so catalogs still pipeline. **Verified end-to-end on AFTMM 2026-07-25:** `syncSource done — live=55676 vod=177364 series=46548 (total=279588) in 276288ms`, zero fatals, peak Java heap **133 MB PSS / 198 MB used vs 313-318 MB before**, steady ~14 MB through the decode+write phase. Sync time 276 s vs the 267 s baseline (~3%; the reverted sequential experiment was 442 s). `streamLive` is deliberately outside the semaphore — a recording holds its channel for hours and would starve every other request. **CLOSED 2026-08-22 by reconciliation.** The entry had been sitting Open for a month on one outstanding condition — a device soak — which had in fact already happened and is recorded in this file's own header: a 272,419-item sync (53,167 live / 174,000 VOD / 45,252 series) ran flat at 12-13 MB of a 24 MB heap with `0(0B) LOS objects` and zero OOM, harsher than the conditions the bug originally reproduced under. Re-confirmed repeatedly on 2026-08-21/22: four full 272k syncs across the MB-353 and MB-315 work, no OOM and no runaway GC in any of them. Nothing was left to do; the status simply was never updated, which is exactly the drift this pass exists to remove.
","
")` → `replace("
","
")` → `split('
')` — each step a **complete additional copy**, all reachable at once, in UTF-16. A playlist of N bytes on the wire peaked at roughly 8-10N of live heap; at 255,843 entries that is several hundred MB against a 384 MB budget, which is precisely the captured 376/384 MB pin. The parser's own KDoc claimed it "processes line-by-line without loading the entire file into memory" — the implementation did the opposite, which is why the earlier audit trusted it. Fix: `parseM3uLines(Sequence<String>)` core + lazy line sequences; `M3U_URL` now uses `http.getSource` (the memory-bounded temp-file path `AndroidKtorHttpClient` was written for — its own KDoc says it "tips peak heap back to ~1MB"), `M3U_FILE` streams via the new `FileContentReader.readSource`. Line-ending/BOM parity pinned from both directions by the 18 existing `M3uParserTest` cases plus a new `M3uLineSourceTest` (String-vs-Source equality across 14 line-ending shapes, 50k-entry streaming parse, and a laziness assertion that the first line costs <10% of the playlist). Two accumulation paths fixed alongside (`085d9ef`): **MB-286** Guide pagination appended channels forever with no ceiling (now capped at 1000 channels / 50k programmes — the second bound matters because a 14+14-day EPG window is ~670 programmes per channel, i.e. ~400 MB at 1000 channels), and **MB-287** `TvContextActionState` pinned a disposed composition for the life of the process (now released via `DisposableEffect`). **MB-288** (`049efb7`) surfaced while capping: guide pagination died permanently whenever a scroll cancelled an in-flight page, because `loadingMore` was never reset on cancellation. **Still unverified:** no device was reachable this session — all of the above is compile- + unit-test-verified only. The 1h Fire TV soak remains the gate, and the F3 heap-watermark Sentry probe is still the wild-detection net. Deferred follow-ups from Sprint I.X (hprof dominator analysis, RGB_565 logos, Ktor pool audit, Compose retention audit) stay open but are lower-priority now that a several-hundred-MB cause is closed. |
| MB-228 | Native / Shared | Medium | **DB driver corruption-recovery path has no integration test.** Stage 1.5 shipped `SourcesBackup` + `DatabaseFactory.android.kt` recovery (delete + recreate + restore on open failure) and unit tests cover the backup/restore round trip in isolation. The integration — corrupt DB on disk → cleans + recreates + restores → produced DB is queryable — was not covered. | **Fixed** | 2026-04-28 | `92bfafc`. MK.24 Sprint G.2. Read-the-file gate confirmed `DatabaseFactory.android.kt` is heavily Android-bound (`Context.getDatabasePath`, `AndroidSqliteDriver`, `androidx.sqlite.db.SupportSQLiteDatabase`) — pure-JVM end-to-end of the production factory needs Robolectric (rejected). Shipped pure-function extraction instead: `decideRecoveryAction(saved, currentSchemaVersion): RecoveryAction` (sealed FreshOnly / Restore / RefuseRestore) + `dbArtifactPaths(dbFile): List<File>` (sidecar enumeration). Production `recoverWithFreshDb` now `when`-branches on the pure decision; `deleteDbArtifacts` delegates to `dbArtifactPaths`. Tests in `DatabaseRecoveryTest.kt`: 5 decision-table cases (null / current / older / newer-refuse / empty), 2 sidecar enumeration cases, 2 integration cases — the load-bearing one writes garbage bytes to a real file, attempts JdbcSqliteDriver open (fails as expected), runs the cleanup, opens fresh at same path, insert+select round-trips. Plus a clean-disk no-op test catching a `File("")`-style bug where paths resolve to the working directory. **Out of scope (deferred):** end-to-end through the production Android `Context`-bound `DatabaseFactory.create()` — requires Robolectric. The decision-table + integration tests pin all the load-bearing logic; the production wiring is now a thin adapter calling them. Surfaced 2026-04-28 by Sprint C/D verification audit. |
| MB-224 | Tooling | Medium | **No CI runs the test suite.** No `.github/workflows/`, no other CI configuration anywhere in the repo. That's why MB-223's broken-test-suite state went undetected for 24 hours — any compile-time test breakage is invisible until somebody locally runs `./gradlew :shared:testDebugUnitTest` or `:app:testDebugUnitTest`. Manually running them is what surfaced MB-223 during MK.23 Sprint C work; without that, MB-220's CASCADE wipe regression test was unwriteable. Same gap protects every other latent bug: a pre-existing test could silently start failing and we'd never know. | **Fixed** | 2026-04-28 | `.github/workflows/android-tests.yml` runs `:shared:testDebugUnitTest :app:testDebugUnitTest :app:assembleDebug` on every push to `master` + every PR targeting master. Java 17 (Temurin), Android SDK platforms;android-35, Gradle cache keyed on wrapper-properties + every `build.gradle.kts` + `libs.versions.toml`. `concurrency` cancels in-progress runs on rapid pushes. 30-minute timeout per job. Test reports uploaded as artifacts on failure (14-day retention). Workflow runs without secrets — `local.properties` is intentionally absent on CI; Sentry DSN handling treats missing file as Sentry-off (init becomes a no-op when `SENTRY_DSN` is empty). Side-fix: `gradlew` was checked in mode `100644` (no exec bit) — set to `100755` via `git update-index --chmod=+x`, plus a `chmod +x` step in the workflow as belt-and-suspenders. Windows-only guard on `:shared:verifyCommonMainYancoDbMigration` does not fire on Linux CI, so SQLDelight's build-time migration verification runs — exactly the safety net we want enforced. **Out of scope (file as future MK):** instrumented tests (none today), release builds (debug keystore per MB-201), iOS klib compile (separate macOS workflow once iOS targets land). |
| MB-209 | Native | High | Scheduled recordings fired while the app was closed / TV in standby would freeze ~15–45 seconds in. Root cause: receiver's pre-fire path routed every "no overlap" case through switch+tee — which calls `playbackController.play(...)` to kick off ExoPlayer and then taps the bytes via `RecordingDataSink`. With no `PlayerActivity` alive there's no video Surface attached; ExoPlayer's load loop pulls bytes only until its internal buffer fills (BALANCED preset = 15s, STABLE = 45s) and then pauses loading. The recording captured the first ~15–45s and froze. | **Fixed** | 2026-04-27 | RecordingScheduleReceiver pre-fire now distinguishes "different channel playing" from "nothing playing" via `controller.currentItem.value == null`. Headless / standby case (`currentUrl == null`) dispatches `RecordingService.start` WITHOUT calling `playbackController.play(...)` — the service's URL-match check then routes to fresh-GET (`MpegTsRecorder` / `HlsRecorder`), which opens its own HTTP connection and writes straight to disk, no Surface, no decoder, full duration. Live-tee still wins when the user is actively watching (avoids the 1-stream IPTV cap on a foreground stream); switch+tee still wins when a different channel is in flight in the foreground. Foreground service holds an implicit CPU partial wake lock so the recording continues across standby; Wi-Fi typically stays on for Alexa wake-word on Fire TV. (Future hardening: explicit `WifiManager.createWifiLock(WIFI_MODE_FULL_HIGH_PERF)` for devices that aggressively sleep Wi-Fi.) |
| MB-208 | Native | High | Scheduled recordings could end up with the schedule transitioned to COMPLETED but no recording row in the DB (the "Done" + Delete-only history-row case the user hit on Apr 26 21:15, "Rennes vs Nantes"). Three failure modes converged on the same symptom: (a) the receiver's pre-fire path called `RecordingService.start` without runCatching, so a `ForegroundServiceStartNotAllowedException` (Fire TV background-restriction edge — alarm exemption isn't universally honoured) thrown by `startForegroundService` killed the receiver after the schedule was already transitioned to FIRING; the end alarm later unconditionally transitioned to COMPLETED; (b) the service's coroutine could fail before `resolveOutputOrFail` reached `markStarted` (process death mid-window, cancellation), leaving no row; (c) the switch+tee path deferred `playbackController.play(...)` through `getMainExecutor.execute { ... }`, which sometimes let the FGS intent's `handleStart` read the OLD `currentItem.value` first → routed to fresh-GET → opened a SECOND HTTP connection to the same channel → 1-stream cap kicked in → recording failed. In all three cases `handleEnd` optimistically transitioned to COMPLETED with no recording row behind it. Captured via diagnostic logcat: schedule=COMPLETED, rowCount=0, linked=null, scheduleRecId=null. | **Fixed** | 2026-04-27 | RecordingScheduleReceiver: (1) wrap `RecordingService.start` in runCatching; on throw transition the schedule to FAILED with `service_start_failed: <message>` instead of leaving it in FIRING. (2) handleEnd now reads the recording row by derived recordId after dispatching stop and transitions the schedule based on row presence + bytes: missing row → FAILED (`recording_never_started`); zero bytes → FAILED (`zero_bytes_captured`); has bytes → COMPLETED. (3) drop the `getMainExecutor.execute` deferral on the switch+tee path — receiver onReceive runs on main already, so `playbackController.play(...)` is called synchronously, guaranteeing `controller.currentItem.value` is the scheduled URL by the time the FGS intent's `handleStart` reads it (live-tee path wins, no second HTTP connection). Existing "No file" badge (commit 6fcba37) still covers any historical orphan rows. (commit pending) |
| MB-206 | Native | High | Live-tee recordings (the channel the user is watching) saved bytes to disk but ExoPlayer refused to play the resulting file: `ERROR_CODE_PARSING_CONTAINER_MALFORMED` ("Loading finished before preparation is complete. {contentIsMalformed=true, dataType=1}"). Root cause: the user can press "Record this channel" mid-broadcast, so the bytes ExoPlayer is feeding through `RecordingDataSink` at that instant typically begin mid-TS-payload with the most recent PAT/PMT already past. Writing them verbatim produced a file whose first 188-byte boundary held random payload bytes; `TsExtractor.sniff()` couldn't find a sync byte + valid section header sequence and gave up before preparation finished. | **Fixed** | 2026-04-27 | RecordingDataSink now buffers writes until it's seen a TS-aligned packet pair where the first carries PID 0 (PAT) AND the buffer also contains a packet for the PMT PID parsed from that PAT. From that point on, bytes flush starting at the PAT packet so the file begins on a 188-byte boundary with a valid program-table head; subsequent writes go straight to disk with no buffering. Buffer is capped at 1 MB — pathological streams that never emit PAT bail to "write everything" so the user always sees bytes in the Recordings list. (commit pending) |
| MB-205 | Native | Medium | Terminal-action `OptionRow`s inside the redesigned player options panels (MENU → Record → **Stop recording**, MENU → Favorites → **Add/Remove**, MENU → External player → app rows) didn't respond to D-pad CENTER on Fire TV. Recording-tab Stop worked (same `RecordingService.stop` path), so service-side was fine — the click never reached `onPick`. | **Fixed** | 2026-04-27 | Diagnostic logcat capture (2026-04-27 session) revealed the root cause: the `inflight` flow inside `RecordPanelContent` starts with `emptyList()` so the first frame composes "Record this channel" and `awaitAndRequest` focuses it; the flow then emits the real list, `active` becomes non-null, Compose swaps in "Stop recording" — but the `LaunchedEffect(currentItem?.id)` doesn't re-fire because the channel id didn't change. The two rows are different OptionRow call sites (separate composable identities), so focus didn't transfer. CENTER then fell through to the activity-level swallow guard. Fix: use TWO separate `PlacedFocusAnchor` instances (one per branch) and key the LaunchedEffect on `(currentItem?.id, active != null)` so the focus request re-runs when the visible row swaps. |
| MB-204 | Native | High | Recording allocation failures evaporated silently. `RecordingService.resolveOutputOrFail` ran the storage resolver BEFORE inserting the recordings row; on failure it tried to `markFailed` a row that didn't exist yet, the call threw, `runCatching` swallowed the throw, and the launch returned `null`. The user saw: nothing in the Recordings list, no foreground notification, AND the player options sheet's Record tab kept showing "Record" instead of "Stop recording" because `activeJobs` stayed empty. Real-world repro 2026-04-26: user on PUBLIC mode (post-MK.14.X audit-revised default) + Fire TV API 28 + WRITE_EXTERNAL_STORAGE not granted -> resolver throws -> failure invisible. Surfaced because of MB-204's interaction with the migration default. | **Fixed** | 2026-04-26 | Restructured `resolveOutputOrFail` to insert the row INSIDE its try/catch: on success it transitions through `markStarted` with the resolved file_path; on failure it inserts a placeholder row with empty file_path then immediately `markFailed`s with `reason = file_allocation_failed: <message>`. Both paths leave the user with a visible row (RECORDING -> COMPLETED/FAILED, or directly FAILED) so allocation failures are no longer silent. Removed the duplicate `markStarted` calls from `handleStartLiveTee` and `handleStartFreshGet` — the single canonical insert is now inside `resolveOutputOrFail`. The structural fix went in alongside the user-facing fallback in `RecordingStorageResolver.resolvePublic` (commit `2a49257`); together they ensure recordings either succeed silently to the preferred location, fall back silently to app-private, or fail visibly with a row the user can see. (2026-04-26) |
| MB-15 | Mobile | Medium | First-frame blocked by hydration gate on cold boot | **Deferred to native rewrite** | 2026-04-14 | N/A — Compose boots direct from SQLDelight (MK.4) |
| MB-16 | Mobile | High | SearchScreen crashes during fast typing | **Fixed** | 2026-04-14 | FlatList virtualization (2026-04-19) — full rebuild as SearchOverlay in M4R |
| MB-17 | Mobile | Medium | Navigation sluggish across the whole app | **Deferred to native rewrite** | 2026-04-12 | N/A — Compose navigation (MK.4) |
| MB-18 | Desktop | Critical | Electron boot `ERR_UNSUPPORTED_DIR_IMPORT` / `ERR_MODULE_NOT_FOUND` on `@yancotv/core` internal imports | **Fixed** | 2026-04-19 | `.js`-extension commit (`e454da4`, 2026-04-19) type-checked but didn't run on Electron's bundled Node 24; real fix builds `@yancotv/core` to `dist/` via tsc and points `main`/`exports` at the emitted JS (2026-04-20) |
| DB-01 | Desktop | High | Overlay Back button stops playback but main window stays in theater mode → blank screen, no sidebar/content | **Fixed** | 2026-04-20 | `src/renderer/stores/player-store.ts` state-change handler now treats mpv `status: 'stopped'` the same as `'idle'` and exits theater mode (2026-04-20) |
| MB-19 | Mobile | Critical | PhoneLayout crams 260-wide LeftRail into 56px horizontal strip — categories + Sources button unreachable | **Deferred to native rewrite** | 2026-04-19 | N/A — Compose adaptive layouts (MK.4) |
| MB-20 | Mobile | High | `Platform.isTV` misdetects on some Fire TV / GTV boxes → those devices fall into broken PhoneLayout | **Deferred to native rewrite** | 2026-04-19 | N/A — `UiModeManager.UI_MODE_TYPE_TELEVISION` in Kotlin |
| MB-21 | Mobile | High | No SafeAreaView / status bar inset on HomeShell — top content covered on notched phones | **Deferred to native rewrite** | 2026-04-19 | N/A — Compose `WindowInsets` (MK.4) |
| MB-22 | Mobile | Medium | `ContentPanel.tsx:56` dead conditional (`category.kind === 'type' ? category.type : category.type` — both branches identical) | **Deferred — RN frozen** | 2026-04-19 | Won't fix; code deleted when RN package is archived |
| MB-23 | Mobile | Medium | No `hasTVPreferredFocus` on first mount — D-pad does nothing until click | **Deferred to native rewrite** | 2026-04-19 | N/A — Compose focus system (MK.4) |
| MB-24 | Mobile | High | Visual design doesn't match desktop — flat tiles, no hex logo frames, no quality badges, no category filter column, sidebar only has 4 content types (not full global nav) | **Deferred to native rewrite** | 2026-04-19 | Rebuilt in Compose (MK.4.7 theme + MK.5.3 badges) |
| MB-25 | Mobile | Critical | Native `ReactExoplayerView` / `TextureView` measures `0` height on Fire TV, causing black video while audio plays | **Fixed** | 2026-04-20 | M4R.Player (`09150e9`, 2026-04-20) — react-native-video removed entirely; replaced by native Android `PlayerActivity` hosting Media3 ExoPlayer directly. RN bridge no longer in the playback path. Picture renders on Fire TV v7a + phone arm64 |
| MB-26 | Mobile | High | Fullscreen route renders controls only and depends on underlying persistent player; collapsed host leaves black fullscreen playback | **Fixed** | 2026-04-20 | M4R.Player (`09150e9`) — `FullscreenPlayer` RN route deleted; playback is a native AppCompat Activity launched via Intent. No RN fullscreen host exists |
| MB-27 | Mobile | High | `transparentModal` fullscreen overlay plus persistent player underneath makes video surface layout/composition fragile on TV | **Fixed** | 2026-04-20 | M4R.Player (`09150e9`) — no `transparentModal`, no persistent RN `<Video>`. Native Activity owns the whole Window while playing |
| MB-28 | Mobile | High | `react-native-video` `viewType={TEXTURE}` is ineffective because local Android `updateSurfaceView(viewType)` is a no-op | **Fixed** | 2026-04-20 | M4R.Player (`09150e9`) — react-native-video dropped; native `PlayerView` uses stock Media3 SurfaceView |
| MB-29 | Mobile | Medium | Patched `exo_player_view_texture.xml` uses invalid Android XML namespace `res-android` instead of `res/android` | **Fixed** | 2026-04-20 | M4R.Player (`09150e9`) — `patches/react-native-video@6.19.1.patch` deleted, `patchedDependencies` removed from root `package.json`, `react-native-video` removed from mobile deps |
| MB-30 | Mobile | High | No bundled Media3 FFmpeg extension / `jniLibs`; DTS, TrueHD, and other non-platform codecs still fail on Fire TV-class devices | **Deferred to native rewrite** | 2026-04-20 | MK.9 (FFmpeg extension registered on native Activity's DefaultRenderersFactory) |
| MB-31 | Mobile | Medium | Release APK installed on Fire TV as `armeabi-v7a`, so future native decoder extensions must ship matching 32-bit libraries | **Deferred to native rewrite** | 2026-04-20 | MK.9 (per-ABI splits in Android Gradle) |
| MB-32 | Mobile | Medium | IPTV stream type detection relies mostly on URL suffixes, missing extensionless/query-based Xtream/HLS/MPEG-TS streams | **Deferred to native rewrite** | 2026-04-20 | MK.1.4 / MK.6 (stream detection in Kotlin parser + Media3) |
| MB-33 | Mobile | Medium | Player has no visible unsupported-video-track state, so decoder failures and render-surface failures both look like generic black playback | **Deferred to native rewrite** | 2026-04-20 | MK.6 (Compose error UI over `PlayerView`) |
| MB-34 | Mobile | Low | Player diagnostics are mostly dev-only/Sentry breadcrumbs, leaving release ADB logs without enough codec/layout detail | **Deferred to native rewrite** | 2026-04-20 | MK.12.5 (Firebase Crashlytics) |
| MB-35 | Android | High | `FavoritesScreen.onRemove` calls `favorites.remove(row.id)` directly from a Compose click handler — SQLite write on main thread (3 call sites: live/movies/series) | **Fixed** | 2026-04-21 | Extracted `removeFavorite` helper: optimistic main-thread drop from the `SnapshotStateList`, delete dispatched to `Dispatchers.IO` via `rememberCoroutineScope()` (2026-04-21) |
| MB-36 | Android | High | `InfoPanel.FavoriteButton.onToggle` calls `favorites.toggle(item.id)` synchronously in the click lambda — main-thread DB write | **Fixed** | 2026-04-21 | Optimistic flip on main; `favorites.toggle` runs on `Dispatchers.IO` via `rememberCoroutineScope()`. StateFlow migration deferred to MB-45/46 batch (2026-04-21) |
| MB-37 | Android | High | `PlaybackController.loadCurrent()` calls `history.positionFor(item.id)` synchronously — blocks caller thread (often main) on SQLite read before every play | **Fixed** | 2026-04-21 | Resume lookup now runs in `scope.launch { withContext(Dispatchers.IO) { ... } }`; main-thread continuation guards against mid-flight zap with an `_currentItem.id` identity check (2026-04-21) |
| MB-38 | Android | High | `PlaybackController.persistResumePoint()` calls `history.upsert(...)` synchronously from `PlayerActivity.onPause` — main-thread DB write on every pause | **Fixed** | 2026-04-21 | Position capture stays on main (MediaController requirement); upsert dispatched to `Dispatchers.IO` via `scope.launch` (2026-04-21) |
| MB-39 | Shared | High | `FavoritesRepository.toggle` stores `added_at = clock() / 1000L` (seconds) while the rest of the schema uses milliseconds — unit inconsistency | **Fixed** | 2026-04-21 | Repo now stores raw `clock()` ms; `Favorites.sq` comments the unit. Pre-release — existing dev rows will be inconsistent; next schema bump can wipe (2026-04-21) |
| MB-40 | Shared | High | `WatchHistoryRepository.upsert` stores `watched_at = clock() / 1000L` (seconds) — same ms/sec inconsistency as MB-39 | **Fixed** | 2026-04-21 | Same fix as MB-39. `position_seconds` / `duration_seconds` remain seconds (media offsets, not wall clock) and that's now documented in `WatchHistory.sq` (2026-04-21) |
| MB-41 | Shared | High | `WatchHistoryRepository.positionFor` falls back to `rows.firstOrNull()` when no content-level row exists — returns an episode's position for a series container lookup, causing the player to seek to a random episode's offset | **Fixed** | 2026-04-21 | Dropped the episode fallback — returns null when no `episode_id IS NULL` row exists (2026-04-21) |
| MB-42 | Android | High | `PlaybackController.stop()` clears the queue without calling `persistResumePoint` first — BACK out of fullscreen player loses current VOD progress | **Fixed** | 2026-04-21 | `stop()` now calls `persistResumePoint()` before clearing `_queue`/`_index`/`_currentItem` (2026-04-21) |
| MB-43 | Android | High | `PlaybackController.step()` (next/previous zap) loads the next item without persisting the outgoing one's resume — movie zapping discards progress | **Fixed** | 2026-04-21 | `step()` calls `persistResumePoint()` before mutating `_index` (2026-04-21) |
| MB-44 | Android | Medium | Only `PlayerActivity.onPause` invokes `persistResumePoint`; `MainActivity.onStop` does not — pressing Home during mini-preview VOD playback loses the resume point | **Fixed** | 2026-04-21 | `MainActivity.onStop` injects `PlaybackController` via Koin and calls `persistResumePoint()` (no-op for live) (2026-04-21) |
| MB-45 | Android | Medium | `InfoPanel.isFav` is only refreshed when `item?.id` changes — unstarring the same item from FavoritesScreen leaves the star button stale until focus moves | **Fixed** | 2026-04-21 | Added `FavoritesRepository.isFavoriteFlow(id)` backed by SQLDelight `asFlow().mapToOne(Dispatchers.IO)`; InfoPanel collects it in a dedicated `LaunchedEffect(item?.id)` (2026-04-21) |
| MB-46 | Android | Medium | `FavoritesScreen.LaunchedEffect(Unit)` runs once per composition — favorite changes from InfoPanel in another section require navigating away and back to refresh | **Fixed** | 2026-04-21 | Added `FavoritesRepository.allFlow()` backed by SQLDelight `asFlow().mapToList(Dispatchers.IO)`; FavoritesScreen now `collectAsState`s it and drops the imperative `LaunchedEffect` + `SnapshotStateList` (2026-04-21) |
| MB-47 | Android | Medium | `SearchScreen.query` uses `remember` not `rememberSaveable` — rotation, process death, or navigating away and back loses the typed text | **Fixed** | 2026-04-21 | Switched `query` to `rememberSaveable`; `results`/`searching` stay in `remember` since rerunning the debounced FTS query on restore is cheap (2026-04-21) |
| MB-48 | Android | Medium | `FavoritesScreen` movie row activation skips the `alreadyPlaying` guard — tapping the currently-playing movie rebuffers instead of fullscreening | **Fixed** | 2026-04-21 | Movie `onActivate` now mirrors the live branch: `alreadyPlaying = controller.currentId == row.id`, skip `play()` if true, and phone path always launches fullscreen (2026-04-21) |
| MB-49 | Android | Medium | Same as MB-48 for the series branch | **Fixed** | 2026-04-21 | Same fix as MB-48 applied to the series `onActivate` (2026-04-21) |
| MB-50 | Android | Low | `FavoritesScreen.UnstarButton` renders `"\u2605 Remove"` — ★ is the FILLED-star glyph, semantically "is favorited"; remove action should use ☆ (`\u2606`) or an explicit X icon | **Fixed** | 2026-04-21 | Swapped to `\u2606 Remove`; filled-star stays reserved for the "currently favorited" state in `InfoPanel` (2026-04-21) |
| MB-51 | Android | Low | `FavoritesScreen.FavoriteRow` `AsyncImage` has `contentDescription = null` — TalkBack / TV reader announces nothing for the logo | **Fixed** | 2026-04-21 | `contentDescription = item.title` on the row logo (2026-04-21) |
| MB-52 | Android | Low | `FavoritesScreen` LazyColumn loses scroll position when a section becomes empty and is re-added (e.g. unstarring the last movie then re-starring one) — sticky-header items get re-keyed | **Fixed** | 2026-04-21 | Hoisted `rememberLazyListState()` and passed as `state` to `LazyColumn`; combined with the stable `"header-<type>"` + `"<type>:${it.id}"` keys, scroll survives partitions shrinking/re-expanding (2026-04-21) |
| MB-53 | Android | Low | `SearchScreen.BasicTextField` has no `KeyboardOptions` — soft keyboard shows IME action "Enter" instead of "Search", and hardware keyboards don't get a submit affordance | **Fixed** | 2026-04-21 | Added `KeyboardOptions(imeAction = ImeAction.Search)`; `KeyboardActions.onSearch` just hides the IME — the 220ms debounce already drives the FTS call (2026-04-21) |
| MB-54 | Android | Low | `SearchScreen` does not request focus on entry — TV users must D-pad-navigate from sidebar into the text field before typing | **Fixed** | 2026-04-21 | `FocusRequester` + `LaunchedEffect(Unit) { requester.requestFocus() }` on the BasicTextField (2026-04-21) |
| MB-55 | Android | Low | `SearchScreen.SearchRow` `AsyncImage` has `contentDescription = null` — same accessibility gap as MB-51 | **Fixed** | 2026-04-21 | `contentDescription = item.title` on the row logo (2026-04-21) |
| MB-56 | Android | Low | `SearchScreen` empty-state subtitle `"Looking through ${query.trim()}."` reads awkwardly for short queries ("Looking through a.") | **Fixed** | 2026-04-21 | Copy changed to `Searching for "$query"…` (2026-04-21) |
| MB-57 | Android | Low | `SearchScreen.SearchField` placeholder renders via a stacked `Text` over `BasicTextField` relying on z-order — brittle vs. `BasicTextField`'s `decorationBox` slot | **Fixed** | 2026-04-21 | Moved placeholder into `BasicTextField.decorationBox`; baseline now tracks the field (2026-04-21) |
| MB-58 | Android | Medium | `InfoPanel` renders `"Source: ${item.sourceId}"` — shows the opaque UUID/slug instead of the source's display name | **Fixed** | 2026-04-21 | Injected `SourceRepository` and resolved `getById(sourceId)?.name` inside the existing IO `LaunchedEffect`; falls back to "Unknown source" (2026-04-21) |
| MB-59 | Android | Low | `InfoPanel.FavoriteButton` has no TalkBack semantics — announces as generic "clickable" | **Fixed** | 2026-04-21 | `Modifier.semantics { contentDescription = if (starred) "Remove from favourites" else "Add to favourites" }` on the button Row (2026-04-21) |
| MB-60 | Android | Medium | `InfoPanel`'s `LaunchedEffect(item?.id)` has no try/catch — a corrupt row in `favorites` / `watch_history` crashes the entire right pane | **Fixed** | 2026-04-21 | Whole IO fan-out wrapped in try/catch; renders a "Some details couldn't load." hint instead of crashing, logs via logcat (2026-04-21) |
| MB-61 | Android | Medium | MK.8.3 spec calls for a pinned "Favorites" group at the top of the category rail; shipped implementation is only a standalone sidebar destination | **Fixed** | 2026-04-21 | Added `FAVORITES_GROUP` constant + pinned "☆ Favorites" row at the top of `CategoryFilterPanel`; `ContentPanel` branches to `FavoritesRepository.allFlow()` for that group, keeping the paged `content` query for all others. Sidebar destination retained as a discoverable entry point (2026-04-21) |
| MB-62 | Android | Medium | MK.8.5 spec calls for a global search overlay + KEYCODE_SEARCH remote hotkey + Ctrl-K phone shortcut; shipped implementation is only a sidebar destination | **Fixed** | 2026-04-21 | Added `SearchOverlayState` (app-scoped `MutableStateFlow<Boolean>`) + overlay layer in `HomeScreen` that hosts `SearchScreen` over a scrim; `MainActivity.onKeyDown` intercepts `KEYCODE_SEARCH` and Ctrl-K, calls `SearchOverlayState.show()`, returns true to consume. `BackHandler` dismisses (2026-04-21) |
| MB-63 | Android | Low | `FavoritesScreen` recomputes `items.filter { it.type == ... }` on every recomposition for three sections — O(3N) per frame when focused | **Fixed** | 2026-04-21 | Wrapped each type filter in `remember { derivedStateOf { ... } }` so they only re-partition when the backing `items` list actually changes (2026-04-21) |
| MB-64 | Android | Medium | `HomeContent.kt:367` (`HomeHero`) — `MutableInteractionSource` re-used via `remember {}` but child chrome reads `focused` state driving full frame re-layout; hero recomposes visibly on every focus flicker | **Fixed** | 2026-04-22 | LaunchedEffect no longer takes `focused` as key (rememberUpdatedState); HeroFrame/HeroCta receive `interaction` so only the CTA sub-tree recomposes on focus (`5e053fe`, 2026-04-22) |
| MB-65 | Android | High | `BrowseShell.kt:274` — stale `focusedItem` reference when `items` list reallocates after a type/group switch; selector paints on wrong card or nothing until the user presses D-pad | **Fixed** | 2026-04-22 | Track selection by id via `visibleNow.none { it.id == focusedItem?.id }` check; LaunchedEffect(items.size) resyncs against parental-filtered list (`cd7973f`, 2026-04-22) |
| MB-66 | Android | Low | `HomeContent.kt:627` (`PosterRail`) — `Modifier.wheelItemTransform(...)` re-instantiated per tile on every recompose, costing frames during scroll | **Fixed** | 2026-04-22 | `remember(index) { Modifier.wheelItemTransform(...) }` in ContentRail + all three HomeContent rails; graphicsLayer Modifier allocated once per stable slot key (2026-04-22) |
| MB-67 | Android | High | `ContentRail.kt:114` — `firstItemFocus` FocusRequester attached inline in `itemsIndexed`; races with layout so first focus ring doesn't paint until user presses D-pad | **Fixed** | 2026-04-22 | ContentRail now takes `firstItemAnchor: PlacedFocusAnchor`; focused card marks anchor via `onPlaced`; BrowseShell uses `anchor.awaitAndRequest()` replacing the delay-ladder (2026-04-22) |
| MB-68 | Android | Medium | `FeatureHero.kt:155` — `AnimatedContent(targetState = focused.logoUrl)` crossfade runs on every focus change even when URL is unchanged, causing backdrop flicker during rail scrolling | **Fixed** | 2026-04-22 | HeroBackdrop debounces URL 300ms via LaunchedEffect(focused?.logoUrl); Coil only loads and AnimatedContent only transitions after the user rests on a channel (`1e75be1`, 2026-04-22) |
| MB-69 | Android | Medium | `BrowseShell.kt:380` — tail-prefetch fires on every focus frame near the threshold, hammering SQLDelight repeatedly and producing scroll stalls | **Fixed** | 2026-04-22 | Added `delay(100L)` before the DB fetch inside the `LaunchedEffect`; LaunchedEffect cancel semantics debounce rapid focus changes (`b6be032`, 2026-04-22) |
| MB-70 | Android | Medium | `BrowseShell.kt:349` — parental `hiddenIds` filter is applied to `visible` inline *after* `FeatureHero` reads `focusedItem`, so the hero briefly shows a hidden item on group entry | **Fixed** | 2026-04-22 | LaunchedEffect(items.size) now uses `applyParentalFilters` to derive the initial focus index from the filtered list — hero never paints a hidden card (`cd7973f`, 2026-04-22) |
| MB-71 | Android | High | `HomeContent.kt:249` — `continueWatching.indexOfFirst { ... }` runs on a `mutableStateListOf` that can be mutated by a parallel `LaunchedEffect`; potential IOOB during activation | **Fixed** | 2026-04-22 | All four `onPlay` lambdas snapshot to `.toList()` before `indexOfFirst` so idx and list slice are from the same snapshot (`3a550ff`, 2026-04-22) |
| MB-72 | Android | Low | `CategoryChipBar.kt:88` — `LaunchedEffect(selectedIndex) { listState.scrollToItem(...) }` fires unconditionally, even when `selectedIndex` hasn't changed meaningfully; brief freeze on group pick | **Fixed** | 2026-04-22 | Guard on `selectedIndex != prevScrolledIndex` tracked in remember; scrollToItem only fires on real index changes (`22740e0`, 2026-04-22) |
| MB-73 | Android | High | `CategoryChipBar.kt:113` — selected chip attaches its FocusRequester only when `isSelected == true`; attachment races composition so the "selected" chip doesn't show its ring until the user presses D-pad | **Fixed** | 2026-04-22 | Chip now self-requests focus via LaunchedEffect(focusRequester) when it gains the requester — focus ring paints on selection without external caller racing composition (`22740e0`, 2026-04-22) |
| MB-74 | Android | High | `HomeContent.kt:202` — `heroSlides` `derivedStateOf` keyed on `continueWatching`, `resumeByContent`, `onNow` — each state changes independently, so `buildHeroSlides` runs on every EPG tick | **Fixed** | 2026-04-22 | Replaced with heroSlidesKey derivedStateOf (cwFirstId + top-2 On Now titles + resume size) + remember(heroSlidesKey); EPG ticks no longer rebuild the carousel (`5e053fe`, 2026-04-22) |
| MB-75 | Android | Medium | `BrowseShell.kt:287` — `initialFocusIndex()` uses `controller.currentId` which goes stale when playback is paused mid-session; user returns from fullscreen and focus lands on the wrong card | **Fixed** | 2026-04-22 | Changed to `controller.currentItem.value?.id` so the currently-loaded (paused or active) item wins (`cd7973f`, 2026-04-22) |
| MB-76 | Android | High | `SearchScreen.kt:192` (search field) — `LaunchedEffect(Unit) { requester.requestFocus() }` runs before the text field is placed; field stays dark until D-pad wake-up | **Fixed** | 2026-04-22 | SearchField migrated to `rememberPlacedFocusAnchor()` + `awaitAndRequest()`; focus waits for `onPlaced` before firing (`d14671a`, 2026-04-22) |
| MB-77 | Android | Medium | `BrowseShell.kt` — `nowSeconds` was re-assigned in both the items-change `snapshotFlow` collect and the EPG tick loop; tile progress ring redraws on every pagination event | **Fixed** | 2026-04-22 | Removed `nowSeconds = ...` from the snapshotFlow collect; tick loop is the sole owner (`6c8416b`, 2026-04-22) |
| MB-78 | Android | High | `BrowseShell.kt:321` — EPG `while(true)` loop continues firing DB queries when a detail/search overlay is up, wasting CPU | **Fixed** | 2026-04-22 | Loop body skips with `continue` when `restoreFocusOnWindowRegain` is false (overlay visible); cancels naturally when `type` changes (`b6be032`, 2026-04-22) |
| MB-79 | Android | Medium | `FeatureHero.kt:337` (`LiveHeroMeta`/`VodHeroMeta`) — `chips = buildList { ... }` runs on every recomposition of the hero copy, even when `type`/`group`/`sourceName` are unchanged | **Fixed** | 2026-04-22 | Both MetaChipRow calls wrapped in `remember(...)` keyed on their relevant inputs (`6c8416b`, 2026-04-22) |
| MB-80 | Android | Low | `ContentRail.kt:115` — double-coerce expression `focusedIndex.coerceAtLeast(0).coerceAtMost((items.size - 1).coerceAtLeast(0))` is unreadable and off-by-one risky when items is empty | **Fixed** | 2026-04-22 | Extracted `val safeFocusedIndex = focusedIndex.coerceIn(...)` before itemsIndexed; readable single-expression, correct on empty lists (`febe3e2`, 2026-04-22) |
| MB-81 | Android | Low | `FeatureHero.kt:298` (`HeroProgress`) — `span` recomputed on every `nowSeconds` tick; `Brush.horizontalGradient` reallocated on every recompose | **Fixed** | 2026-04-22 | `span` memoized with `remember(start, end)`; gradient extracted as stable `private val heroProgressBrush`; same pattern applied to `ProgressStripe` in HomeContent (2026-04-22) |
| MB-82 | Android | High | `GuideScreen.kt:161` — `.distinctBy { it.tvgId }` is only applied on initial load; subsequent pagination pages can reintroduce duplicates, throwing LazyColumn "Key already used" crash | **Fixed** | 2026-04-22 | Added `distinctBy { it.tvgId }` to each incoming page before the existing-id filter, closing the intra-page duplicate window (`3a550ff`, 2026-04-22) |
| MB-83 | Android | Medium | `HomeContent.kt:135` — `remember(favoriteList, hiddenIds)` uses a `Set` collection key; any Set identity change (even with equal contents) re-runs the filter | **Fixed** | 2026-04-22 | Changed to `remember(favoriteList.size, hiddenIds)` to avoid O(N) list-equality walk on every recomposition (`6c8416b`, 2026-04-22) |
| MB-84 | Android | High | `ContentDetailScreen.kt:141` — play button CTA never `requestFocus()`s after detail loads; user lands on detail page and sees no selector until D-pad | **Fixed** | 2026-04-22 | Migrated `playFocus` from `FocusRequester` to `PlacedFocusAnchor`; `awaitAndRequest()` waits for `onPlaced` — deterministic, no delay race (`d14671a`, 2026-04-22) |
| MB-85 | Android | Medium | `BrowseShell.kt:462` — focus-restore retry loop with hardcoded `[80, 250, 500]ms` delays can burn 830ms total before giving up, which feels like "return from fullscreen is slow" | **Fixed** | 2026-04-22 | Retry delays tightened to `[50, 150, 400]ms`; bails on first successful attach (`cd7973f`, 2026-04-22) |
| MB-86 | Android | Low | `FeatureHero.kt:423` — Play button has `Icon(..., contentDescription = null)` nested under a parent with its own `Modifier.semantics { contentDescription = ... }`; TalkBack may read redundantly | **Fixed** | 2026-04-22 | PrimaryCta + TonalCta Row semantics use `mergeDescendants = true` so TalkBack treats each button as one atomic node; inner Icon null description no longer creates a spurious child traversal stop (`1e75be1`, 2026-04-22) |
| MB-87 | Android | Medium | `ContentRail.kt:210` — long-press dismiss path fires but `selfRequester.requestFocus()` is only wired on `onClick`; after menu closes the rail card goes dark | **Fixed** | 2026-04-22 | BrowseShell's `ChannelActionsMenu.onDismiss` launches `firstItemFocus.requestFocus()` after 80ms so the focused rail card regains its ring once the menu leaves composition (`d14671a`, 2026-04-22) |
| MB-88 | Android | Low | `CategoryChipBar.kt:77` — `remember(groups, selected, showFavorites)` runs `.indexOf()` on every `showFavorites` toggle even though index doesn't depend on that flag | **Fixed** | 2026-04-22 | Split into `groupPos = remember(groups, selected) { indexOf }` and `selectedIndex = remember(groupPos, selected, showFavorites) { offset calc }`; indexOf only reruns when groups/selected change (`22740e0`, 2026-04-22) |
| MB-89 | Android | Medium | `HomeScreen.kt:141` — `LaunchedEffect(section) { mainContentFocus.requestFocus() }` re-fires on same-section recompositions (e.g. favorites flow updates), yanking focus back to main content unexpectedly | **Fixed** | 2026-04-22 | Added `lastFocusedSection` guard; focus request only fires on actual section transitions (`febe3e2`, 2026-04-22) |
| MB-90 | Android | Low | `SearchScreen.kt:131` — `BasicTextField` `.focusRequester(requester)` order vs `.focusable()` may leave cursor un-drawn on first gain of focus on some Fire TV builds | **Fixed** | 2026-04-22 | Resolved by MB-76: SearchField uses PlacedFocusAnchor (focusRequester before focusable, awaits placement). BasicTextField is self-focusable; no explicit `.focusable()` needed. Ordering correct by construction (`d14671a`, 2026-04-22) |
| MB-91 | Android | Medium | `BrowseShell.kt:311` — EPG batch `snapshotFlow` included `focusedItem?.tvgId` in the pair, causing `distinctUntilChanged` to emit on every channel focus change even when the tvgId list was identical | **Fixed** | 2026-04-22 | Removed focused item's tvgId from the snapshotFlow; `distinctUntilChanged` now compares only the tvgId list (`b6be032`, 2026-04-22) |
| MB-92 | Android | Low | `FeatureHero.kt:177` — `fg by animateColorAsState(...)` on the favorite button animates on every `selected \|\| isFavorite` change with no spring spec, feeling mushy on fast toggles | **Fixed** | 2026-04-22 | PrimaryCta bg and TonalCta bg now use `animateColorAsState(spring(StiffnessMediumLow))` — focus crossfades instead of snapping (`1e75be1`, 2026-04-22) |
| MB-93 | Android | Low | `HomeContent.kt:126` — `continueWatching` / `favorites` / `recentlyAdded` are `mutableStateListOf` mutated from multiple `LaunchedEffect`s; no data race per se in Compose, but sequencing is non-obvious and makes mid-load reads surprising | **Fixed** | 2026-04-22 | All clear+addAll pairs wrapped in `Snapshot.withMutableSnapshot`; no recomposition fires between clear and addAll — rails never flash empty during refresh (`5e053fe`, 2026-04-22) |
| MB-94 | Android | High | Leaving Live TV for Movies/Series keeps the last live channel playing in the preview/hero, so the new tab inherits live playback instead of showing its own poster or preview | **Fixed** | 2026-04-22 | `HomeScreen` stops live preview on any non-LiveTV section, and `BrowseShell` only gives `MiniPlayer` ownership to the focused card; covered by `BrowseShellLogicTest` (2026-04-22) |
| MB-95 | Android | Critical | Heavy churn through channels, tabs, and fullscreen player can crash; player attach/detach churn and uncancelled EPG/paging jobs confirmed | **Fixed** | 2026-04-22 | EPG `LaunchedEffect` key changed from `(type, items.size)` to `(type)` — no more restart on every paginated page; `snapshotFlow` inside handles item additions. MiniPlayer `clearVideoTextureView` on dispose (prev session) eliminates attach/detach crash path (2026-04-22) |
| MB-96 | Android | Medium | First entry into Live TV, Movies, or Series briefly renders the empty-state copy ("no videos") before the initial SQL/page load completes | **Fixed** | 2026-04-22 | Added `hasLoaded(type, group)` flag in BrowseShell; empty state only renders after first page finishes loading (`febe3e2`, 2026-04-22) |
| MB-97 | Android | Medium | BrowseShell `visible` list recomputed on every recomposition (EPG ticks, nowSeconds, sourceName changes) — O(n) parental filter running needlessly | **Fixed** | 2026-04-22 | `visible` wrapped in `remember { derivedStateOf { applyParentalFilters(...) } }`; Compose now tracks only items/hiddenIds/hideAdultContent as snapshot dependencies (2026-04-22) |
| MB-98 | Android | High | Series category selector disappears after opening a series detail and returning; the selected chip does not paint until pressing OK, and selector behavior needs a full cross-screen audit | **Fixed** | 2026-04-22 | Browse entry focus now attaches to the selected chip leaf instead of a non-focusable wrapper; category/rail lazy `focusRestorer` removed to avoid stale pinned focus state, and chip index math counts the separator row (2026-04-22) |
| MB-99 | Android | High | Backing out of a series episode leaves audio playing when navigating to other tabs; triggering a preview in another tab crashes the app (MiniPlayer dangling TextureView) | **Fixed** | 2026-04-22 | Three-part fix: (1) HomeScreen stops ALL playback (not just LIVE) on section change; (2) BrowseShell stops on group/type change; (3) MiniPlayer `onDispose` calls `clearVideoTextureView` to prevent dangling surface reference (2026-04-22) |
| MB-100 | Android | Critical | Movie → Series Episode → Live TV crashes app with `SQLiteConstraintException: FOREIGN KEY constraint failed` from `WatchHistoryRepository.upsert` during `PlaybackController.persistResumePoint` | **Fixed** | 2026-04-23 | Episode sessions wrote `watch_history.content_id = episode.id`, but episodes live in their own table — FK to `content(id)` failed. Fix: track `_currentEpisode` in PlaybackController; persistResumePoint writes `contentId = seriesId, episodeId = ep.id`. Both FKs satisfied; per-episode resume rows are unique (`wh:<seriesId>:<episodeId>`). Surfaced now because MB-99's stop-on-section-change made the persist path fire more aggressively (2026-04-23) |
| MB-101 | Android | Critical | Any DB write failure during `WatchHistoryRepository.upsert` crashes the entire process — no try/catch around the persist coroutine | **Fixed** | 2026-04-23 | `upsert` now wraps the SQLDelight call in try/catch; logs via Kermit and swallows. `CancellationException` is re-thrown so coroutine cancellation still propagates. Resume points are best-effort telemetry, not a load-bearing invariant (2026-04-23) |
| MB-102 | Android | High | Episodes were never persisted to the `episodes` table — `watch_history.episode_id` would FK-fail against `episodes(id)` even after MB-100's fix | **Fixed** | 2026-04-23 | Added `upsert` query to `Episodes.sq`; `ContentDetailService.load` now persists every fetched episode after a series detail loads. Idempotent batch with per-row `runCatching` so a malformed episode doesn't block the rest (2026-04-23) |
| MB-103 | Android | Medium | Tapping a Series row in Favorites opened an empty fullscreen player — `controller.play(series, …)` no-ops on series (toPlayable returns null) but `PlayerLauncher.launch` still fired | **Fixed** | 2026-04-23 | `FavoritesScreen` now takes `onOpenDetail` and routes Series taps through HomeScreen's detail overlay, mirroring the Movies/Series branch in `HomeScreen.onBrowseActivate`. Live and Movie rows still go straight to the player (2026-04-23) |
| MB-104 | Android | Medium | Switching from Live TV to Movies/Series after browsing channels showed stale channel list under the new category chips | **Fixed** | 2026-04-23 | `derivedStateOf { applyParentalFilters(items, …) }` was keyed on nothing, so its lambda kept observing the original `items` SnapshotStateList; `items = remember(type) { … }` allocated a NEW list on type change but the derivedStateOf never rebound. Re-keyed `remember(type) { derivedStateOf { … } }` so it tracks the current items reference (2026-04-23) |
| MB-105 | Android | Low | Misleading comment in `HomeScreen.onPlayEpisode` claimed "uses the real episode id as the content_id in watch_history" — that's the broken invariant from MB-100 | **Fixed** | 2026-04-23 | Comment updated to reflect the post-fix behavior: content_id = seriesId, episode_id = ep.id (2026-04-23) |
| MB-106 | Android | Medium | BACK / detail-close / `onExitToSidebar` requested focus on the sidebar wrapper Column; in Compose 1.7 the descendant focus search was unreliable, so the active row's `MutableInteractionSource` never flipped — no focused gradient or ring until the user nudged the D-pad | **Fixed** | 2026-04-25 | Two passes: v1 attached requester to row's outer Box (still wrong target node); v2 binds requester directly to the focusable inner Row via `.let { base -> if (focusRequester != null) base.focusRequester(focusRequester) else base }.focusable(interactionSource = …)`, with the binding rule extracted to `bindActiveRowFocus(section, current, requester)` and pinned by `AppSidebarFocusBindingTest` (`be3d413`, hardened 2026-04-25) |
| MB-107 | Android | Medium | Parental tab content overflowed the viewport with no scroll — toggles + hidden-channels list got clipped, no way to reach them with the D-pad | **Fixed** | 2026-04-22 | Wrapped tab body in `verticalScroll(rememberScrollState())` outer + bounded `heightIn(max = …)` inner LazyColumn for the hidden list; D-pad scroll now reaches every row (`29fe656`, 2026-04-22) |
| MB-107a | Android | Medium | Settings toggle rows gave no visible focus indicator on Fire TV — focus reached the row but the Material3 Switch's halo was invisible against the raised background, so the user couldn't tell which toggle they were on | **Fixed** | 2026-04-22 | Extracted shared `SettingsToggleRow` composable: Row owns input via `.clickable(interactionSource)`, Switch is display-only (`onCheckedChange = null`), focus state drives an accent border (`width = if (focused) 2.dp else 1.dp, color = palette.FocusRing`). Same pattern inlined into `GroupRow` in SettingsGroupsTab (`8e77a37`, 2026-04-22) |
| MB-108 | Android | High | Settings sidebar tab CENTER didn't auto-focus the content pane — pressing CENTER on a different tab swapped the body but left focus on the sidebar; user had to press RIGHT manually after every tab change | **Fixed** | 2026-04-25 | v1 used `focusGroup + focusRestorer + PlacedFocusAnchor` on the content pane — sometimes lost focus entirely (bounced back to sidebar) because the fresh `key(current)` mount had no last-focused child for restorer to land on. v2 simulates D-pad RIGHT via `LocalFocusManager.moveFocus(FocusDirection.Right)` after `withFrameNanos` (mimics the manual press exactly). Hardened: now waits TWO frames (composition + layout pass), retries once if `moveFocus` returns false, logs a warning to Logcat (`SettingsScreen` tag) on persistent failure so Fire TV regressions are diagnosable (`be3d413`, hardened 2026-04-25) |
| MB-109 | Android | High | Empty / read-only Settings tabs (About, Shortcuts, all 5 placeholder bodies) had ZERO focusable children — `moveFocus(Right)` from the sub-sidebar always failed, focus stayed on the sidebar, the user saw the body update but the app silently refused to enter the pane. Real layer-level root cause of the symptom MB-108 was patching at the wrong layer | **Fixed** | 2026-04-25 | Added `FocusableSpacer` primitive (0-dp `.focusable()` Spacer) to `ui/focus/PlacedFocusAnchor.kt`; dropped one as the first child of `SettingsPlaceholder`, `SettingsAboutTab`, and `SettingsShortcutsTab`. CENTER-into-tab now reliably lands focus inside the pane on every tab; LEFT returns to sidebar. Network/Parental/EPG/General/Playback/Groups already had real focusable controls and didn't need the trap (2026-04-25) |
| MB-110 | Android | Medium | Settings selector chips (`Chip` / `ResizeChip` / `TypeChip` — three duplicated definitions across General / Playback / Groups tabs) had no `.focusable()`, no shared `MutableInteractionSource`, and no focus-aware visual state. On Fire TV the chip row was unreachable via D-pad in some focus paths, and even when focus did land there was no border/colour change to show it | **Fixed** | 2026-04-25 | Extracted shared `SettingsChip` composable (focusable via `.clickable(interactionSource, indication = null)` so it self-registers as a focus target; visible 2dp accent border on focus; pure `chipColors(palette, selected, focused)` helper + `SettingsChipColors` data class for unit-testable colour rules — `focused` border wins over `selected`, `selected` background wins, foreground `TextPrimary` for focus-or-select). Replaced all three call sites; deleted the three private composables. Locked in by `SettingsChipColorsTest` (6 cases covering each rule) (2026-04-25) |
| MB-111 | Android | Medium | `HomeScreen` non-browse content panes (Home / Settings / Guide / Favorites / Search) wrapped their screens in `Box(modifier = Modifier.weight(1f).focusRequester(mainContentFocus))` with no `.focusable()` and no `.focusGroup()`. Compose 1.7's descendant-focus search through a bare wrapper-Box requester is unreliable when the first focusable child isn't placed yet — `mainContentFocus.requestFocus()` could silently no-op. Same class of bug that MB-106 v1 hit on the sidebar | **Fixed** | 2026-04-25 | Added `.focusGroup()` to all 5 wrapper Boxes so each becomes a deterministic descendant-focus search root; the requester now reliably forwards focus to the first focusable child once placed. Single shared comment above the Settings branch documents the pattern for the other four (2026-04-25) |
| MB-113 | Android | High | Sidebar focus indicator looked like "two shapes" — chunky-chamfered tile in the collapsed (92dp) state, elegant long pill in the expanded (260dp) state — even after MB-112 made the FRAME exclusive to focus. Two coupled root causes: (1) `CutCornerCardSmall` uses an *absolute* 16dp cut, which is 23 % of width on the collapsed row and only 7 % on the expanded row — same shape primitive, very different visual character. (2) `Column.focusRestorer()` was layered on top of the explicit `activeRowFocus` binding from MB-106; on BACK / LEFT into the sidebar both ran, and during the COLLAPSED→EXPANDED width animation the layout was in flux long enough that the restorer's "land on last focused descendant" and `activeRowFocus.requestFocus()` could land out of order, leaving the row focused but the `interactionSource` lagging until the user nudged the D-pad — same "detector won't show until OK" symptom MB-106 first surfaced | **Fixed** | 2026-04-25 | (1) Switched `SidebarRow` clip + border from `YancoShapes.CutCornerCardSmall` to `RoundedCornerShape(10.dp)`. Cut-corner aesthetic is preserved everywhere else (chips, cards, heroes) — only the sidebar row, which morphs width, opts out. (2) Removed `Modifier.focusRestorer()` from the sidebar `Column`. The restorer was redundant: the explicit `activeRowFocus` already binds to the *current section's* row by construction (via `bindActiveRowFocus`), and "last focused" inside the sidebar is identical to "current section" because the only way to leave the sidebar with a different row last-focused is to click that row, which simultaneously updates `current`. Stripping the restorer eliminates the race; one explicit binding now drives every focus return (2026-04-25) |
| MB-112 | Android | High | Sidebar / category pills / Settings chips painted nearly-identical accent borders for both `focused` (FocusRing) and `selected` (Accent at low alpha) states. At Fire TV viewing distance (~3 m) a thin coloured outline reads the same regardless of which accent token it uses — user could not tell whether the cursor was on the row or the row was simply the breadcrumb of where they came from ("the category and the live tv will be showing a selection which is proper... but they are the same color"). Also: per-tab `FocusableSpacer()` from MB-109 was dropped at the top of every Settings tab body's `Arrangement.spacedBy(...)` Column, which added the spacedBy gap between the 0-dp spacer and the first heading — every tab's content pushed down 12-16dp from the breadcrumb | **Fixed** | 2026-04-25 | Two-part fix: (1) **Frame = focus, fill = selection**, applied uniformly. `AppSidebar.SidebarRow`: `border = if (focused) 2.dp FocusRing else 0.dp Transparent`; selected row keeps its left-edge accent rail + gradient wash but loses the border. `CategoryChipBar.Chip`: same — focus owns the 2dp ring, selection has only the soft Accent gradient + the leading pip. `SettingsChip.chipColors()`: `border = if (focused) palette.FocusRing else Color.Transparent` — was previously `selected -> Accent.copy(alpha=0.55f)`. (2) **Hoisted `FocusableSpacer`** out of the per-tab Columns up to `SettingsScreen.ContentPane` Box where it sits as a sibling of `key(current) { TabContent(tab) }` — the Box overlays children rather than spacing them, so the trap is layout-neutral and one trap covers all 14 tabs. Locked in by `SettingsChipColorsTest.onlyFocusedPaintsBorder` (truth-table guard) + the renamed `selectedIdleHasTintedBackgroundAndNoBorder` so future "small visual polish" PRs can't put an accent ring back on idle-selected (2026-04-25) |
| MB-114 | Android | High | Closing the in-process detail / search overlay or the parental-PIN gate from a coverflow section did not return focus to the coverflow — focus dropped to the page root (no visible cursor) or stayed on a stale node. `HomeScreen` requested focus on `mainContentFocus` after the overlay closed, but that requester was only bound to the non-browse content panes' wrapper Boxes; in browse sections the requester wasn't bound to any node, so `requestFocus()` silently no-op'd. Same root cause class as MB-111 / MB-106 (requester pointed at the wrong node), but for in-process overlay closes specifically — the existing `windowInfo.isWindowFocused` handler in CoverflowSectionScreen catches Activity-level transitions (player launch / IME / external dialog) but not Compose-internal overlays, which never flip the OS window-focus bit | **Fixed** | 2026-04-25 | Receiver-side fix in `CoverflowSectionScreen`: added a `LaunchedEffect(restoreFocusOnWindowRegain)` that detects a `false → true` transition of the flag and calls `firstItemAnchor.awaitAndRequest()`. `restoreFocusOnWindowRegain` is the existing HomeScreen-driven flag (true when no overlay is mounted: `detailItem == null && !searchOverlayVisible && pendingPlay == null`) — the flag flipping back to true is exactly "an overlay just closed, give focus back." Initial-mount safe via `prevRestore` seeded from the current flag value (no fire on first invocation, no race with the orb's own bring-focus-to-orb-on-mount path). Only fires for in-process overlay closes; OS-level window-focus regains are still handled by the existing `windowInfo` handler. Side effects audited: only browse sections mount this composable; detail / search / PIN-gated play all originate from coverflow user actions so returning focus to the orb is the expected destination (`CoverflowSectionScreen.kt`, 2026-04-25) |
| MB-115 | Android | Low | `ContentDetailScreen` Play-button focus restoration on window-focus regain used the delay-ladder anti-pattern (`for (delayMs in longArrayOf(80L, 250L, 500L)) { delay(delayMs); requestFocus() }`) — flagged in the `native-android-mk` skill checklist as race-prone since it requests focus on fixed timeouts rather than after `onPlaced`. In practice the Play button was usually focused by the third tick, but the fallback chain is exactly the kind of "silently failed in production" pattern PlacedFocusAnchor was introduced to replace | **Fixed** | 2026-04-25 | `playAnchor` (a `PlacedFocusAnchor`) was already in scope at the top of the screen; replaced the delay-ladder with `playAnchor.awaitAndRequest()` inside the `snapshotFlow { windowInfo.isWindowFocused }` collector. Strictly more deterministic — fires after the next `onPlaced` callback rather than blind-firing at 80/250/500 ms. No behavioral change on the happy path; eliminates the "first two ticks no-op" warning noise on slower Fire TV hardware (`ContentDetailScreen.kt`, 2026-04-25) |
| MB-116 | Android | High | Settings tabs that are read-only by design (`About`, `Shortcuts`) and the placeholder bodies (`Storage`, `Notifications`, `Recordings`, `EPG`, `Subtitles`, `Appearance`) silently failed D-pad scrolling. Each Column had `verticalScroll(rememberScrollState())` but no focusable child, so the focus from the sub-sidebar's `moveFocus(Right)` landed on `ContentPane`'s sibling `FocusableSpacer()` (per MB-109's hoisted-trap fix) — that spacer is OUTSIDE the scrolling Column, so D-pad UP/DOWN had no scroll target and the lower half of every page was unreachable on TV. User report: "about and shortcuts when pressing on them, it doesnt take me to the card to scroll in it and read it" | **Fixed** | 2026-04-25 | New `Modifier.dpadVerticalScroll(scrollState)` helper in `ui/focus/DpadScroll.kt`: makes the modified node focusable AND intercepts D-pad UP/DOWN via `onPreviewKeyEvent`, calling `ScrollState.animateScrollBy(±240dp)` per press. Returns `false` from the handler when scroll is at its boundary so focus can still escape — D-pad LEFT or UP-from-top still walks back to the sub-sidebar. Applied to `SettingsAboutTab`, `SettingsShortcutsTab`, and `SettingsPlaceholder` (one helper covers all three plus the 6 placeholder tabs that share `SettingsPlaceholder`). Lives next to `PlacedFocusAnchor` / `FocusableSpacer` because it's the same family of TV-focus primitives (2026-04-25) |
| MB-117 | Android | High | Every text input across Settings (`Network` User-Agent + Connect/Read timeouts, `Parental` PIN field, `Playback` audio/subtitle language fields) was a plain Material3 `OutlinedTextField`, which auto-pops the IME the instant focus lands on it via D-pad. On Fire TV that means flicking past a field with the remote opens the system keyboard with no way to dismiss without entering text — actively user-hostile. The `AddSourceDialog`'s private `ClickToEditField` already had the right pattern (focusable read-only row → CENTER flips `editing = true` → embedded `BasicTextField` then requests focus → IME opens; Done/Enter/Back collapses back) but it was not extracted, so the rest of Settings shipped the broken Material3 default. User report: "whenever the indicator goes to a writing box. the keyboard appear automatically. i want the behavior in every writing box in settings to behave that only if im on it and press ok then the keyboard appears" | **Fixed** | 2026-04-25 | Extracted `SettingsClickToEditField` to `ui/settings/SettingsTextField.kt` with two render modes: **Card** (default — wrapped in PrefCard chrome with title + optional description) for tab-level fields, and **Bare** (no chrome — `bare = true`) for fields inside dialogs. Supports `keyboardType: KeyboardType` (Text / Number / NumberPassword / Ascii / Uri / Password) and `transformation: VisualTransformation` (None / `PasswordVisualTransformation`) so every existing call site swaps in 1:1. Migrated all 6 broken sites (`SettingsNetworkTab` ×3, `SettingsParentalTab` ×1, `SettingsPlaybackTab` ×2) plus the 6 dialog sites in `AddSourceDialog` (which now uses the shared composable in bare mode instead of its private copy). The IME is now physically impossible to open without an explicit OK press on the field. `LaunchedEffect(editing) { if (editing) runCatching { editFocus.requestFocus() } }` is the gate — it only requests focus after `editing = true`, which only flips on the read-only row's onClick (2026-04-25) |

## How to use this register

- **Adding a bug:** append a row with the next MB-NN (mobile) or DB-NN (desktop) ID. Keep summary to one line; details go in the fix commit.
- **Fixing a bug:** flip Status to **Fixed**, add commit ref to the Fix target column, and remove the row on the next doc pass once the fix has stabilized.
- **Dumping a refactor-wide set of bugs:** don't list them here. Create `docs/incidents/YYYY-MM-DD-<topic>.md` and link it from the Status column (e.g. `Open — see [incident](docs/incidents/...)`).

## Cross-references

- [docs/incidents/2026-04-16-html5-player-refactor.md](docs/incidents/2026-04-16-html5-player-refactor.md) — desktop HTML5 refactor bug dump (bugs 1–29, archived)
- [PRODUCTION_PLAN_ANDROID.md](PRODUCTION_PLAN_ANDROID.md) § Reboot Notice — M4R rebuilds the mobile shell; MB-13/15/17/19–23 are all scoped there
- [CLAUDE.md § Native Android — Lessons from MK.8 Audit (2026-04-21)](CLAUDE.md#native-android--lessons-from-mk8-audit-2026-04-21) — rules distilled from MB-35 through MB-63. Apply before every future MK.* commit.
