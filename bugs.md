# YancoTV — Bug Register

Living register of **open** bugs across desktop and mobile. Closed bugs move to the changelog; incident reports (deep post-mortems or one-off refactor bug dumps) go to [docs/incidents/](docs/incidents/).

**Last updated:** 2026-04-27 (MB-205 closed via diagnostic logcat capture; MB-206 filed + fixed — live-tee recordings unplayable because the file head began mid-TS-packet without PAT/PMT)
**Format:** ID · Platform · Severity · One-line · Status · First seen · Fix target

> **RN app frozen 2026-04-20.** All open `MB-*` bugs targeting React Native milestones are **Deferred to native rewrite** — they'll be resolved by construction in the Kotlin/KMP app. See [PRODUCTION_PLAN_NATIVE.md](PRODUCTION_PLAN_NATIVE.md). RN bugs remain in the register for historical reference; do not attempt RN fixes.

| ID | Platform | Sev | Summary | Status | First seen | Fix target |
|---|---|---|---|---|---|---|
| MB-13 | Mobile | High | Player takes two back presses to close; surface state desyncs between routes | **Fixed** | 2026-04-12 | Collapsed to one route + state-driven fullscreen (2026-04-20) — no modal to pop, back toggles `isFullscreen` then stops |
| MB-14 | Mobile / Native | High | HEVC-main10 / AC3 / EAC3 / DTS / TrueHD decode audio-only on ~30% of streams | **Fixed** | 2026-04-10 | MK.9.1–9.5 shipped 2026-04-25. FfmpegAudioRenderer wired with `EXTENSION_RENDERER_MODE_ON` (hw decoder preferred; FFmpeg only when MediaCodec rejects) — fixes the AC3/EAC3/DTS/TrueHD audio gap on Fire TV. ExperimentalFfmpegVideoRenderer was REMOVED 2026-04-25 (caused ANRs on common HEVC streams — software decode at 1080p borderline on Fire TV Stick class hardware); HEVC main10 dropped from scope. Hands-on regression closed 2026-04-25: user reports no remaining channels with broken audio path on the live source. The previously-broken set is functional; the FFmpeg path activates transparently per stream when MediaCodec rejects the audio format. |
| MB-118 | Native | Medium | Some channels render in fullscreen `PlayerActivity` (SurfaceView) but show black in `MiniPlayer` preview (TextureView) | **Open** | 2026-04-25 | Hardware decoder TextureView compatibility issue. Some HEVC profiles only output to a SurfaceView. Surfaced during MB-14 regression. Plausible fix: switch MiniPlayer from TextureView to SurfaceView with proper Compose interop, OR add a fallback path that lets the user launch fullscreen even from a black mini-preview. |
| MB-119 | Native | High | App died on 4K stream playback / fullscreen exit. Multiple symptoms: channel freeze + audio-continues, fullscreen-exit fatal exception | **Fixed** | 2026-04-25 | Three-stage fix landed 2026-04-25, all verified hands-on against Bien Sport 4K class streams (~10 Mbps HEVC + AVC variants) on AFTDCT31 (Fire TV). Stage 1 `2cf37db`: drop `ExperimentalFfmpegVideoRenderer` + switch `EXTENSION_RENDERER_MODE_PREFER` → `_ON` (was forcing software HEVC decode over hardware → mid-stream ANR). Stage 2 `301b5b5`: shrink load-control buffers from 5-Mbps tuned values to 4K-friendly (`maxBufferMs` 20s → 15s; `backBufferMs` 120s → 30s) — eased native-cache GC pressure. Stage 3 `fb927af`: explicit `controller.player.clearVideoSurface()` in `PlayerActivity.onStop()` before `playerView.player = null` — symmetric to `PlayerLauncher`'s pre-launch detach. Without it `ThreadedRenderer.finalize()` raced MediaCodec's pending GPU ops on the way out and `FinalizerWatchdogDaemon` killed the app after 10s (`TimeoutException: ThreadedRenderer.finalize() timed out … nDeleteProxy`). |
| MB-200 | Shared | Medium | M3U sync materialises the entire playlist text + a per-line `List<String>` before parsing. `SourceRepository.syncM3uUrl` calls `http.getText(...)` (one full-body string) and `parseM3u` does `cleaned.replace("\r\n", "\n").replace("\r", "\n").split('\n')` (one line list). For a ~50K-channel M3U (~10MB text) that's ~20MB transient + the parsed entry list — covered by `largeHeap=true` today but wasteful, and a perf headwind we'd want to clear before targeting larger providers. | **Open · deferred** | 2026-04-26 | Phase 4 / streaming-parser milestone. Plan: switch `HttpClient.getText` callers in this path to a streaming reader (`ktor`'s `bodyAsChannel().toInputStream()` or a `Source`-style reader), then port `parseM3u` to consume a `Sequence<String>` / `BufferedReader.lineSequence()`-style iterator instead of `split('\n')`. Mirror the change in `packages/core` TS port for parity. Deferred — not a regression, current memory profile is acceptable on Fire TV / phone targets. Surfaced 2026-04-26 by an outside review pass. |
| MB-201 | Native | Medium | Release builds reuse the debug keystore. `app/build.gradle.kts:83` has `signingConfig = signingConfigs.getByName("debug")` with a "Real signing config arrives in Stage 5.7 (distribution pipeline)" comment. Means `assembleRelease` produces an APK that's installable on test hardware but cannot be uploaded to Play / a sideload-distribution channel without re-signing. | **Open · deferred** | 2026-04-26 | Stage 5.7 / distribution pipeline. Plan: generate a release keystore (out-of-tree, gitignored), thread its path + alias + passwords through `local.properties` → `BuildConfig`, wire a `signingConfigs.create("release")` block, point release `signingConfig` at it. Local-properties-driven so a fresh clone still builds (release config falls back to debug when keystore absent). Surfaced 2026-04-26 by an outside review pass. Already documented in code comment; this entry just tracks it on the bug register. |
| MB-202 | Tooling | Low | `ktlintCheck` exits successfully even when style violations exist — `ignoreFailures = true` in both `:app/build.gradle.kts:37` and `:shared/build.gradle.kts:14`. Means the lint command always reports green to CI, while real style debt accumulates in the report. The Windows generated-code filter has been fixed (commit `95bbfda`, 2026-04-26) so the report is now scoped to hand-written code only — the burn-down list is the real one. | **Open · deferred** | 2026-04-26 | D.x burn-down per the existing comment ("flip to false in a later D.x once ktlintCheck is empty"). Plan: triage the current report (mostly `Icons.kt` newline-after-`;` for the multi-icon density, `Theme.kt` capitalised function name, `Typography.kt` filename rule), fix in style-only commits, then flip `ignoreFailures` to `false`. Surfaced 2026-04-26 by an outside review pass. |
| MB-203 | Native | Low | `AndroidManifest.xml:75` has `android:usesCleartextTraffic="true"`, so EVERY HTTP target is allowed cleartext — not just IPTV provider hosts. Real threat model: an attacker on the same Wi-Fi can MITM provider streams (already cleartext at the provider layer) but ALSO any future telemetry / Sentry / image-CDN HTTP traffic if someone adds an `http://` URL by mistake. The current Sentry / Coil paths are HTTPS-only by construction, so the practical leak surface today is limited to provider streams (which the user accepts because IPTV providers serve plain HTTP). | **Open · accepted** | 2026-04-26 | Stage 5.x narrowing. Plan: `network_security_config.xml` that defaults to cleartext-deny, with a `<base-config cleartextTrafficPermitted="false">` + a runtime allow-list for hosts the user has added as sources. Requires a `dynamicAllowList` mechanism since provider hosts are user-configured at runtime — Android's static XML doesn't support that, so the practical implementation is a per-source `OkHttp` + a default `CertificatePinner.DEFAULT` for everything else. Deferred — this is a defense-in-depth hardening, not a present leak. Surfaced 2026-04-26 by an outside review pass. Threat model documented in `AGENTS.md` "Cleartext traffic" note. |
| MB-206 | Native | High | Live-tee recordings (the channel the user is watching) saved bytes to disk but ExoPlayer refused to play the resulting file: `ERROR_CODE_PARSING_CONTAINER_MALFORMED` ("Loading finished before preparation is complete. {contentIsMalformed=true, dataType=1}"). Root cause: the user can press "Record this channel" mid-broadcast, so the bytes ExoPlayer is feeding through `RecordingDataSink` at that instant typically begin mid-TS-payload with the most recent PAT/PMT already past. Writing them verbatim produced a file whose first 188-byte boundary held random payload bytes; `TsExtractor.sniff()` couldn't find a sync byte + valid section header sequence and gave up before preparation finished. | **Fixed** | 2026-04-27 | RecordingDataSink now buffers writes until it's seen a TS-aligned packet pair where the first carries PID 0 (PAT) AND the buffer also contains a packet for the PMT PID parsed from that PAT. From that point on, bytes flush starting at the PAT packet so the file begins on a 188-byte boundary with a valid program-table head; subsequent writes go straight to disk with no buffering. Buffer is capped at 1 MB — pathological streams that never emit PAT bail to "write everything" so the user always sees bytes in the Recordings list. (commit pending) |
| MB-205 | Native | Medium | Terminal-action `OptionRow`s inside the redesigned player options panels (MENU → Record → **Stop recording**, MENU → Favorites → **Add/Remove**, MENU → External player → app rows) didn't respond to D-pad CENTER on Fire TV. Recording-tab Stop worked (same `RecordingService.stop` path), so service-side was fine — the click never reached `onPick`. | **Fixed** | 2026-04-27 | Diagnostic logcat capture (2026-04-27 session) revealed the root cause: the `inflight` flow inside `RecordPanelContent` starts with `emptyList()` so the first frame composes "Record this channel" and `awaitAndRequest` focuses it; the flow then emits the real list, `active` becomes non-null, Compose swaps in "Stop recording" — but the `LaunchedEffect(currentItem?.id)` doesn't re-fire because the channel id didn't change. The two rows are different OptionRow call sites (separate composable identities), so focus didn't transfer. CENTER then fell through to the activity-level swallow guard. Fix: use TWO separate `PlacedFocusAnchor` instances (one per branch) and key the LaunchedEffect on `(currentItem?.id, active != null)` so the focus request re-runs when the visible row swaps. |
| MB-204 | Native | High | Recording allocation failures evaporated silently. `RecordingService.resolveOutputOrFail` ran the storage resolver BEFORE inserting the recordings row; on failure it tried to `markFailed` a row that didn't exist yet, the call threw, `runCatching` swallowed the throw, and the launch returned `null`. The user saw: nothing in the Recordings list, no foreground notification, AND the player options sheet's Record tab kept showing "Record" instead of "Stop recording" because `activeJobs` stayed empty. Real-world repro 2026-04-26: user on PUBLIC mode (post-MK.14.X audit-revised default) + Fire TV API 28 + WRITE_EXTERNAL_STORAGE not granted -> resolver throws -> failure invisible. Surfaced because of MB-204's interaction with the migration default. | **Fixed** | 2026-04-26 | Restructured `resolveOutputOrFail` to insert the row INSIDE its try/catch: on success it transitions through `markStarted` with the resolved file_path; on failure it inserts a placeholder row with empty file_path then immediately `markFailed`s with `reason = file_allocation_failed: <message>`. Both paths leave the user with a visible row (RECORDING -> COMPLETED/FAILED, or directly FAILED) so allocation failures are no longer silent. Removed the duplicate `markStarted` calls from `handleStartLiveTee` and `handleStartFreshGet` — the single canonical insert is now inside `resolveOutputOrFail`. The structural fix went in alongside the user-facing fallback in `RecordingStorageResolver.resolvePublic` (commit `2a49257`); together they ensure recordings either succeed silently to the preferred location, fall back silently to app-private, or fail visibly with a row the user can see. (2026-04-26) |
| MB-15 | Mobile | Medium | First-frame blocked by hydration gate on cold boot | **Deferred to native rewrite** | 2026-04-14 | N/A — Compose boots direct from SQLDelight (MK.4) |
| MB-16 | Mobile | High | SearchScreen crashes during fast typing | **Fixed** | 2026-04-14 | FlatList virtualization (2026-04-19) — full rebuild as SearchOverlay in M4R |
| MB-17 | Mobile | Medium | Navigation sluggish across the whole app | **Deferred to native rewrite** | 2026-04-12 | N/A — Compose navigation (MK.4) |
| MB-18 | Desktop | Critical | Electron boot `ERR_UNSUPPORTED_DIR_IMPORT` / `ERR_MODULE_NOT_FOUND` on `@yancotv/core` internal imports | **Fixed** | 2026-04-19 | `.js`-extension commit (`e454da4`, 2026-04-19) type-checked but didn't run on Electron's bundled Node 24; real fix builds `@yancotv/core` to `dist/` via tsc and points `main`/`exports` at the emitted JS (2026-04-20) |
| DB-01 | Desktop | High | Overlay Back button stops playback but main window stays in theater mode → blank screen, no sidebar/content | **Fixed** | 2026-04-20 | `src/renderer/stores/player-store.ts` state-change handler now treats mpv `status: 'stopped'` the same as `'idle'` and exits theater mode (2026-04-20) |
| MB-19 | Mobile | Critical | PhoneLayout crams 260-wide LeftRail into 56px horizontal strip — categories + Sources button unreachable | **Deferred to native rewrite** | 2026-04-19 | N/A — Compose adaptive layouts (MK.4) |
| MB-20 | Mobile | High | `Platform.isTV` misdetects on some Fire TV / GTV boxes → those devices fall into broken PhoneLayout | **Deferred to native rewrite** | 2026-04-19 | N/A — `UiModeManager.UI_MODE_TYPE_TELEVISION` in Kotlin |
| MB-21 | Mobile | High | No SafeAreaView / status bar inset on HomeShell — top content covered on notched phones | **Deferred to native rewrite** | 2026-04-19 | N/A — Compose `WindowInsets` (MK.4) |
| MB-22 | Mobile | Medium | `ContentPanel.tsx:56` dead conditional (`category.kind === 'type' ? category.type : category.type` — both branches identical) | **Deferred — RN frozen** | 2026-04-19 | Won't fix; code deleted when RN package is archived |
| MB-23 | Mobile | Medium | No `hasTVPreferredFocus` on first mount — D-pad does nothing until click | **Deferred to native rewrite** | 2026-04-19 | N/A — Compose focus system (MK.4) |
| MB-24 | Mobile | High | Visual design doesn't match desktop — flat tiles, no hex logo frames, no quality badges, no category filter column, sidebar only has 4 content types (not full global nav) | **Deferred to native rewrite** | 2026-04-19 | Rebuilt in Compose (MK.4.7 theme + MK.5.3 badges) |
| MB-25 | Mobile | Critical | Native `ReactExoplayerView` / `TextureView` measures `0` height on Fire TV, causing black video while audio plays | **Fixed** | 2026-04-20 | M4R.Player (`09150e9`, 2026-04-20) — react-native-video removed entirely; replaced by native Android `PlayerActivity` hosting Media3 ExoPlayer directly. RN bridge no longer in the playback path. Picture renders on Fire TV v7a + phone arm64 |
| MB-26 | Mobile | High | Fullscreen route renders controls only and depends on underlying persistent player; collapsed host leaves black fullscreen playback | **Fixed** | 2026-04-20 | M4R.Player (`09150e9`) — `FullscreenPlayer` RN route deleted; playback is a native AppCompat Activity launched via Intent. No RN fullscreen host exists |
| MB-27 | Mobile | High | `transparentModal` fullscreen overlay plus persistent player underneath makes video surface layout/composition fragile on TV | **Fixed** | 2026-04-20 | M4R.Player (`09150e9`) — no `transparentModal`, no persistent RN `<Video>`. Native Activity owns the whole Window while playing |
| MB-28 | Mobile | High | `react-native-video` `viewType={TEXTURE}` is ineffective because local Android `updateSurfaceView(viewType)` is a no-op | **Fixed** | 2026-04-20 | M4R.Player (`09150e9`) — react-native-video dropped; native `PlayerView` uses stock Media3 SurfaceView |
| MB-29 | Mobile | Medium | Patched `exo_player_view_texture.xml` uses invalid Android XML namespace `res-android` instead of `res/android` | **Fixed** | 2026-04-20 | M4R.Player (`09150e9`) — `patches/react-native-video@6.19.1.patch` deleted, `patchedDependencies` removed from root `package.json`, `react-native-video` removed from mobile deps |
| MB-30 | Mobile | High | No bundled Media3 FFmpeg extension / `jniLibs`; DTS, TrueHD, and other non-platform codecs still fail on Fire TV-class devices | **Deferred to native rewrite** | 2026-04-20 | MK.9 (FFmpeg extension registered on native Activity's DefaultRenderersFactory) |
| MB-31 | Mobile | Medium | Release APK installed on Fire TV as `armeabi-v7a`, so future native decoder extensions must ship matching 32-bit libraries | **Deferred to native rewrite** | 2026-04-20 | MK.9 (per-ABI splits in Android Gradle) |
| MB-32 | Mobile | Medium | IPTV stream type detection relies mostly on URL suffixes, missing extensionless/query-based Xtream/HLS/MPEG-TS streams | **Deferred to native rewrite** | 2026-04-20 | MK.1.4 / MK.6 (stream detection in Kotlin parser + Media3) |
| MB-33 | Mobile | Medium | Player has no visible unsupported-video-track state, so decoder failures and render-surface failures both look like generic black playback | **Deferred to native rewrite** | 2026-04-20 | MK.6 (Compose error UI over `PlayerView`) |
| MB-34 | Mobile | Low | Player diagnostics are mostly dev-only/Sentry breadcrumbs, leaving release ADB logs without enough codec/layout detail | **Deferred to native rewrite** | 2026-04-20 | MK.12.5 (Firebase Crashlytics) |
| MB-35 | Android | High | `FavoritesScreen.onRemove` calls `favorites.remove(row.id)` directly from a Compose click handler — SQLite write on main thread (3 call sites: live/movies/series) | **Fixed** | 2026-04-21 | Extracted `removeFavorite` helper: optimistic main-thread drop from the `SnapshotStateList`, delete dispatched to `Dispatchers.IO` via `rememberCoroutineScope()` (2026-04-21) |
| MB-36 | Android | High | `InfoPanel.FavoriteButton.onToggle` calls `favorites.toggle(item.id)` synchronously in the click lambda — main-thread DB write | **Fixed** | 2026-04-21 | Optimistic flip on main; `favorites.toggle` runs on `Dispatchers.IO` via `rememberCoroutineScope()`. StateFlow migration deferred to MB-45/46 batch (2026-04-21) |
| MB-37 | Android | High | `PlaybackController.loadCurrent()` calls `history.positionFor(item.id)` synchronously — blocks caller thread (often main) on SQLite read before every play | **Fixed** | 2026-04-21 | Resume lookup now runs in `scope.launch { withContext(Dispatchers.IO) { ... } }`; main-thread continuation guards against mid-flight zap with an `_currentItem.id` identity check (2026-04-21) |
| MB-38 | Android | High | `PlaybackController.persistResumePoint()` calls `history.upsert(...)` synchronously from `PlayerActivity.onPause` — main-thread DB write on every pause | **Fixed** | 2026-04-21 | Position capture stays on main (MediaController requirement); upsert dispatched to `Dispatchers.IO` via `scope.launch` (2026-04-21) |
| MB-39 | Shared | High | `FavoritesRepository.toggle` stores `added_at = clock() / 1000L` (seconds) while the rest of the schema uses milliseconds — unit inconsistency | **Fixed** | 2026-04-21 | Repo now stores raw `clock()` ms; `Favorites.sq` comments the unit. Pre-release — existing dev rows will be inconsistent; next schema bump can wipe (2026-04-21) |
| MB-40 | Shared | High | `WatchHistoryRepository.upsert` stores `watched_at = clock() / 1000L` (seconds) — same ms/sec inconsistency as MB-39 | **Fixed** | 2026-04-21 | Same fix as MB-39. `position_seconds` / `duration_seconds` remain seconds (media offsets, not wall clock) and that's now documented in `WatchHistory.sq` (2026-04-21) |
| MB-41 | Shared | High | `WatchHistoryRepository.positionFor` falls back to `rows.firstOrNull()` when no content-level row exists — returns an episode's position for a series container lookup, causing the player to seek to a random episode's offset | **Fixed** | 2026-04-21 | Dropped the episode fallback — returns null when no `episode_id IS NULL` row exists (2026-04-21) |
| MB-42 | Android | High | `PlaybackController.stop()` clears the queue without calling `persistResumePoint` first — BACK out of fullscreen player loses current VOD progress | **Fixed** | 2026-04-21 | `stop()` now calls `persistResumePoint()` before clearing `_queue`/`_index`/`_currentItem` (2026-04-21) |
| MB-43 | Android | High | `PlaybackController.step()` (next/previous zap) loads the next item without persisting the outgoing one's resume — movie zapping discards progress | **Fixed** | 2026-04-21 | `step()` calls `persistResumePoint()` before mutating `_index` (2026-04-21) |
| MB-44 | Android | Medium | Only `PlayerActivity.onPause` invokes `persistResumePoint`; `MainActivity.onStop` does not — pressing Home during mini-preview VOD playback loses the resume point | **Fixed** | 2026-04-21 | `MainActivity.onStop` injects `PlaybackController` via Koin and calls `persistResumePoint()` (no-op for live) (2026-04-21) |
| MB-45 | Android | Medium | `InfoPanel.isFav` is only refreshed when `item?.id` changes — unstarring the same item from FavoritesScreen leaves the star button stale until focus moves | **Fixed** | 2026-04-21 | Added `FavoritesRepository.isFavoriteFlow(id)` backed by SQLDelight `asFlow().mapToOne(Dispatchers.IO)`; InfoPanel collects it in a dedicated `LaunchedEffect(item?.id)` (2026-04-21) |
| MB-46 | Android | Medium | `FavoritesScreen.LaunchedEffect(Unit)` runs once per composition — favorite changes from InfoPanel in another section require navigating away and back to refresh | **Fixed** | 2026-04-21 | Added `FavoritesRepository.allFlow()` backed by SQLDelight `asFlow().mapToList(Dispatchers.IO)`; FavoritesScreen now `collectAsState`s it and drops the imperative `LaunchedEffect` + `SnapshotStateList` (2026-04-21) |
| MB-47 | Android | Medium | `SearchScreen.query` uses `remember` not `rememberSaveable` — rotation, process death, or navigating away and back loses the typed text | **Fixed** | 2026-04-21 | Switched `query` to `rememberSaveable`; `results`/`searching` stay in `remember` since rerunning the debounced FTS query on restore is cheap (2026-04-21) |
| MB-48 | Android | Medium | `FavoritesScreen` movie row activation skips the `alreadyPlaying` guard — tapping the currently-playing movie rebuffers instead of fullscreening | **Fixed** | 2026-04-21 | Movie `onActivate` now mirrors the live branch: `alreadyPlaying = controller.currentId == row.id`, skip `play()` if true, and phone path always launches fullscreen (2026-04-21) |
| MB-49 | Android | Medium | Same as MB-48 for the series branch | **Fixed** | 2026-04-21 | Same fix as MB-48 applied to the series `onActivate` (2026-04-21) |
| MB-50 | Android | Low | `FavoritesScreen.UnstarButton` renders `"\u2605 Remove"` — ★ is the FILLED-star glyph, semantically "is favorited"; remove action should use ☆ (`\u2606`) or an explicit X icon | **Fixed** | 2026-04-21 | Swapped to `\u2606 Remove`; filled-star stays reserved for the "currently favorited" state in `InfoPanel` (2026-04-21) |
| MB-51 | Android | Low | `FavoritesScreen.FavoriteRow` `AsyncImage` has `contentDescription = null` — TalkBack / TV reader announces nothing for the logo | **Fixed** | 2026-04-21 | `contentDescription = item.title` on the row logo (2026-04-21) |
| MB-52 | Android | Low | `FavoritesScreen` LazyColumn loses scroll position when a section becomes empty and is re-added (e.g. unstarring the last movie then re-starring one) — sticky-header items get re-keyed | **Fixed** | 2026-04-21 | Hoisted `rememberLazyListState()` and passed as `state` to `LazyColumn`; combined with the stable `"header-<type>"` + `"<type>:${it.id}"` keys, scroll survives partitions shrinking/re-expanding (2026-04-21) |
| MB-53 | Android | Low | `SearchScreen.BasicTextField` has no `KeyboardOptions` — soft keyboard shows IME action "Enter" instead of "Search", and hardware keyboards don't get a submit affordance | **Fixed** | 2026-04-21 | Added `KeyboardOptions(imeAction = ImeAction.Search)`; `KeyboardActions.onSearch` just hides the IME — the 220ms debounce already drives the FTS call (2026-04-21) |
| MB-54 | Android | Low | `SearchScreen` does not request focus on entry — TV users must D-pad-navigate from sidebar into the text field before typing | **Fixed** | 2026-04-21 | `FocusRequester` + `LaunchedEffect(Unit) { requester.requestFocus() }` on the BasicTextField (2026-04-21) |
| MB-55 | Android | Low | `SearchScreen.SearchRow` `AsyncImage` has `contentDescription = null` — same accessibility gap as MB-51 | **Fixed** | 2026-04-21 | `contentDescription = item.title` on the row logo (2026-04-21) |
| MB-56 | Android | Low | `SearchScreen` empty-state subtitle `"Looking through ${query.trim()}."` reads awkwardly for short queries ("Looking through a.") | **Fixed** | 2026-04-21 | Copy changed to `Searching for "$query"…` (2026-04-21) |
| MB-57 | Android | Low | `SearchScreen.SearchField` placeholder renders via a stacked `Text` over `BasicTextField` relying on z-order — brittle vs. `BasicTextField`'s `decorationBox` slot | **Fixed** | 2026-04-21 | Moved placeholder into `BasicTextField.decorationBox`; baseline now tracks the field (2026-04-21) |
| MB-58 | Android | Medium | `InfoPanel` renders `"Source: ${item.sourceId}"` — shows the opaque UUID/slug instead of the source's display name | **Fixed** | 2026-04-21 | Injected `SourceRepository` and resolved `getById(sourceId)?.name` inside the existing IO `LaunchedEffect`; falls back to "Unknown source" (2026-04-21) |
| MB-59 | Android | Low | `InfoPanel.FavoriteButton` has no TalkBack semantics — announces as generic "clickable" | **Fixed** | 2026-04-21 | `Modifier.semantics { contentDescription = if (starred) "Remove from favourites" else "Add to favourites" }` on the button Row (2026-04-21) |
| MB-60 | Android | Medium | `InfoPanel`'s `LaunchedEffect(item?.id)` has no try/catch — a corrupt row in `favorites` / `watch_history` crashes the entire right pane | **Fixed** | 2026-04-21 | Whole IO fan-out wrapped in try/catch; renders a "Some details couldn't load." hint instead of crashing, logs via logcat (2026-04-21) |
| MB-61 | Android | Medium | MK.8.3 spec calls for a pinned "Favorites" group at the top of the category rail; shipped implementation is only a standalone sidebar destination | **Fixed** | 2026-04-21 | Added `FAVORITES_GROUP` constant + pinned "☆ Favorites" row at the top of `CategoryFilterPanel`; `ContentPanel` branches to `FavoritesRepository.allFlow()` for that group, keeping the paged `content` query for all others. Sidebar destination retained as a discoverable entry point (2026-04-21) |
| MB-62 | Android | Medium | MK.8.5 spec calls for a global search overlay + KEYCODE_SEARCH remote hotkey + Ctrl-K phone shortcut; shipped implementation is only a sidebar destination | **Fixed** | 2026-04-21 | Added `SearchOverlayState` (app-scoped `MutableStateFlow<Boolean>`) + overlay layer in `HomeScreen` that hosts `SearchScreen` over a scrim; `MainActivity.onKeyDown` intercepts `KEYCODE_SEARCH` and Ctrl-K, calls `SearchOverlayState.show()`, returns true to consume. `BackHandler` dismisses (2026-04-21) |
| MB-63 | Android | Low | `FavoritesScreen` recomputes `items.filter { it.type == ... }` on every recomposition for three sections — O(3N) per frame when focused | **Fixed** | 2026-04-21 | Wrapped each type filter in `remember { derivedStateOf { ... } }` so they only re-partition when the backing `items` list actually changes (2026-04-21) |
| MB-64 | Android | Medium | `HomeContent.kt:367` (`HomeHero`) — `MutableInteractionSource` re-used via `remember {}` but child chrome reads `focused` state driving full frame re-layout; hero recomposes visibly on every focus flicker | **Fixed** | 2026-04-22 | LaunchedEffect no longer takes `focused` as key (rememberUpdatedState); HeroFrame/HeroCta receive `interaction` so only the CTA sub-tree recomposes on focus (`5e053fe`, 2026-04-22) |
| MB-65 | Android | High | `BrowseShell.kt:274` — stale `focusedItem` reference when `items` list reallocates after a type/group switch; selector paints on wrong card or nothing until the user presses D-pad | **Fixed** | 2026-04-22 | Track selection by id via `visibleNow.none { it.id == focusedItem?.id }` check; LaunchedEffect(items.size) resyncs against parental-filtered list (`cd7973f`, 2026-04-22) |
| MB-66 | Android | Low | `HomeContent.kt:627` (`PosterRail`) — `Modifier.wheelItemTransform(...)` re-instantiated per tile on every recompose, costing frames during scroll | **Fixed** | 2026-04-22 | `remember(index) { Modifier.wheelItemTransform(...) }` in ContentRail + all three HomeContent rails; graphicsLayer Modifier allocated once per stable slot key (2026-04-22) |
| MB-67 | Android | High | `ContentRail.kt:114` — `firstItemFocus` FocusRequester attached inline in `itemsIndexed`; races with layout so first focus ring doesn't paint until user presses D-pad | **Fixed** | 2026-04-22 | ContentRail now takes `firstItemAnchor: PlacedFocusAnchor`; focused card marks anchor via `onPlaced`; BrowseShell uses `anchor.awaitAndRequest()` replacing the delay-ladder (2026-04-22) |
| MB-68 | Android | Medium | `FeatureHero.kt:155` — `AnimatedContent(targetState = focused.logoUrl)` crossfade runs on every focus change even when URL is unchanged, causing backdrop flicker during rail scrolling | **Fixed** | 2026-04-22 | HeroBackdrop debounces URL 300ms via LaunchedEffect(focused?.logoUrl); Coil only loads and AnimatedContent only transitions after the user rests on a channel (`1e75be1`, 2026-04-22) |
| MB-69 | Android | Medium | `BrowseShell.kt:380` — tail-prefetch fires on every focus frame near the threshold, hammering SQLDelight repeatedly and producing scroll stalls | **Fixed** | 2026-04-22 | Added `delay(100L)` before the DB fetch inside the `LaunchedEffect`; LaunchedEffect cancel semantics debounce rapid focus changes (`b6be032`, 2026-04-22) |
| MB-70 | Android | Medium | `BrowseShell.kt:349` — parental `hiddenIds` filter is applied to `visible` inline *after* `FeatureHero` reads `focusedItem`, so the hero briefly shows a hidden item on group entry | **Fixed** | 2026-04-22 | LaunchedEffect(items.size) now uses `applyParentalFilters` to derive the initial focus index from the filtered list — hero never paints a hidden card (`cd7973f`, 2026-04-22) |
| MB-71 | Android | High | `HomeContent.kt:249` — `continueWatching.indexOfFirst { ... }` runs on a `mutableStateListOf` that can be mutated by a parallel `LaunchedEffect`; potential IOOB during activation | **Fixed** | 2026-04-22 | All four `onPlay` lambdas snapshot to `.toList()` before `indexOfFirst` so idx and list slice are from the same snapshot (`3a550ff`, 2026-04-22) |
| MB-72 | Android | Low | `CategoryChipBar.kt:88` — `LaunchedEffect(selectedIndex) { listState.scrollToItem(...) }` fires unconditionally, even when `selectedIndex` hasn't changed meaningfully; brief freeze on group pick | **Fixed** | 2026-04-22 | Guard on `selectedIndex != prevScrolledIndex` tracked in remember; scrollToItem only fires on real index changes (`22740e0`, 2026-04-22) |
| MB-73 | Android | High | `CategoryChipBar.kt:113` — selected chip attaches its FocusRequester only when `isSelected == true`; attachment races composition so the "selected" chip doesn't show its ring until the user presses D-pad | **Fixed** | 2026-04-22 | Chip now self-requests focus via LaunchedEffect(focusRequester) when it gains the requester — focus ring paints on selection without external caller racing composition (`22740e0`, 2026-04-22) |
| MB-74 | Android | High | `HomeContent.kt:202` — `heroSlides` `derivedStateOf` keyed on `continueWatching`, `resumeByContent`, `onNow` — each state changes independently, so `buildHeroSlides` runs on every EPG tick | **Fixed** | 2026-04-22 | Replaced with heroSlidesKey derivedStateOf (cwFirstId + top-2 On Now titles + resume size) + remember(heroSlidesKey); EPG ticks no longer rebuild the carousel (`5e053fe`, 2026-04-22) |
| MB-75 | Android | Medium | `BrowseShell.kt:287` — `initialFocusIndex()` uses `controller.currentId` which goes stale when playback is paused mid-session; user returns from fullscreen and focus lands on the wrong card | **Fixed** | 2026-04-22 | Changed to `controller.currentItem.value?.id` so the currently-loaded (paused or active) item wins (`cd7973f`, 2026-04-22) |
| MB-76 | Android | High | `SearchScreen.kt:192` (search field) — `LaunchedEffect(Unit) { requester.requestFocus() }` runs before the text field is placed; field stays dark until D-pad wake-up | **Fixed** | 2026-04-22 | SearchField migrated to `rememberPlacedFocusAnchor()` + `awaitAndRequest()`; focus waits for `onPlaced` before firing (`d14671a`, 2026-04-22) |
| MB-77 | Android | Medium | `BrowseShell.kt` — `nowSeconds` was re-assigned in both the items-change `snapshotFlow` collect and the EPG tick loop; tile progress ring redraws on every pagination event | **Fixed** | 2026-04-22 | Removed `nowSeconds = ...` from the snapshotFlow collect; tick loop is the sole owner (`6c8416b`, 2026-04-22) |
| MB-78 | Android | High | `BrowseShell.kt:321` — EPG `while(true)` loop continues firing DB queries when a detail/search overlay is up, wasting CPU | **Fixed** | 2026-04-22 | Loop body skips with `continue` when `restoreFocusOnWindowRegain` is false (overlay visible); cancels naturally when `type` changes (`b6be032`, 2026-04-22) |
| MB-79 | Android | Medium | `FeatureHero.kt:337` (`LiveHeroMeta`/`VodHeroMeta`) — `chips = buildList { ... }` runs on every recomposition of the hero copy, even when `type`/`group`/`sourceName` are unchanged | **Fixed** | 2026-04-22 | Both MetaChipRow calls wrapped in `remember(...)` keyed on their relevant inputs (`6c8416b`, 2026-04-22) |
| MB-80 | Android | Low | `ContentRail.kt:115` — double-coerce expression `focusedIndex.coerceAtLeast(0).coerceAtMost((items.size - 1).coerceAtLeast(0))` is unreadable and off-by-one risky when items is empty | **Fixed** | 2026-04-22 | Extracted `val safeFocusedIndex = focusedIndex.coerceIn(...)` before itemsIndexed; readable single-expression, correct on empty lists (`febe3e2`, 2026-04-22) |
| MB-81 | Android | Low | `FeatureHero.kt:298` (`HeroProgress`) — `span` recomputed on every `nowSeconds` tick; `Brush.horizontalGradient` reallocated on every recompose | **Fixed** | 2026-04-22 | `span` memoized with `remember(start, end)`; gradient extracted as stable `private val heroProgressBrush`; same pattern applied to `ProgressStripe` in HomeContent (2026-04-22) |
| MB-82 | Android | High | `GuideScreen.kt:161` — `.distinctBy { it.tvgId }` is only applied on initial load; subsequent pagination pages can reintroduce duplicates, throwing LazyColumn "Key already used" crash | **Fixed** | 2026-04-22 | Added `distinctBy { it.tvgId }` to each incoming page before the existing-id filter, closing the intra-page duplicate window (`3a550ff`, 2026-04-22) |
| MB-83 | Android | Medium | `HomeContent.kt:135` — `remember(favoriteList, hiddenIds)` uses a `Set` collection key; any Set identity change (even with equal contents) re-runs the filter | **Fixed** | 2026-04-22 | Changed to `remember(favoriteList.size, hiddenIds)` to avoid O(N) list-equality walk on every recomposition (`6c8416b`, 2026-04-22) |
| MB-84 | Android | High | `ContentDetailScreen.kt:141` — play button CTA never `requestFocus()`s after detail loads; user lands on detail page and sees no selector until D-pad | **Fixed** | 2026-04-22 | Migrated `playFocus` from `FocusRequester` to `PlacedFocusAnchor`; `awaitAndRequest()` waits for `onPlaced` — deterministic, no delay race (`d14671a`, 2026-04-22) |
| MB-85 | Android | Medium | `BrowseShell.kt:462` — focus-restore retry loop with hardcoded `[80, 250, 500]ms` delays can burn 830ms total before giving up, which feels like "return from fullscreen is slow" | **Fixed** | 2026-04-22 | Retry delays tightened to `[50, 150, 400]ms`; bails on first successful attach (`cd7973f`, 2026-04-22) |
| MB-86 | Android | Low | `FeatureHero.kt:423` — Play button has `Icon(..., contentDescription = null)` nested under a parent with its own `Modifier.semantics { contentDescription = ... }`; TalkBack may read redundantly | **Fixed** | 2026-04-22 | PrimaryCta + TonalCta Row semantics use `mergeDescendants = true` so TalkBack treats each button as one atomic node; inner Icon null description no longer creates a spurious child traversal stop (`1e75be1`, 2026-04-22) |
| MB-87 | Android | Medium | `ContentRail.kt:210` — long-press dismiss path fires but `selfRequester.requestFocus()` is only wired on `onClick`; after menu closes the rail card goes dark | **Fixed** | 2026-04-22 | BrowseShell's `ChannelActionsMenu.onDismiss` launches `firstItemFocus.requestFocus()` after 80ms so the focused rail card regains its ring once the menu leaves composition (`d14671a`, 2026-04-22) |
| MB-88 | Android | Low | `CategoryChipBar.kt:77` — `remember(groups, selected, showFavorites)` runs `.indexOf()` on every `showFavorites` toggle even though index doesn't depend on that flag | **Fixed** | 2026-04-22 | Split into `groupPos = remember(groups, selected) { indexOf }` and `selectedIndex = remember(groupPos, selected, showFavorites) { offset calc }`; indexOf only reruns when groups/selected change (`22740e0`, 2026-04-22) |
| MB-89 | Android | Medium | `HomeScreen.kt:141` — `LaunchedEffect(section) { mainContentFocus.requestFocus() }` re-fires on same-section recompositions (e.g. favorites flow updates), yanking focus back to main content unexpectedly | **Fixed** | 2026-04-22 | Added `lastFocusedSection` guard; focus request only fires on actual section transitions (`febe3e2`, 2026-04-22) |
| MB-90 | Android | Low | `SearchScreen.kt:131` — `BasicTextField` `.focusRequester(requester)` order vs `.focusable()` may leave cursor un-drawn on first gain of focus on some Fire TV builds | **Fixed** | 2026-04-22 | Resolved by MB-76: SearchField uses PlacedFocusAnchor (focusRequester before focusable, awaits placement). BasicTextField is self-focusable; no explicit `.focusable()` needed. Ordering correct by construction (`d14671a`, 2026-04-22) |
| MB-91 | Android | Medium | `BrowseShell.kt:311` — EPG batch `snapshotFlow` included `focusedItem?.tvgId` in the pair, causing `distinctUntilChanged` to emit on every channel focus change even when the tvgId list was identical | **Fixed** | 2026-04-22 | Removed focused item's tvgId from the snapshotFlow; `distinctUntilChanged` now compares only the tvgId list (`b6be032`, 2026-04-22) |
| MB-92 | Android | Low | `FeatureHero.kt:177` — `fg by animateColorAsState(...)` on the favorite button animates on every `selected \|\| isFavorite` change with no spring spec, feeling mushy on fast toggles | **Fixed** | 2026-04-22 | PrimaryCta bg and TonalCta bg now use `animateColorAsState(spring(StiffnessMediumLow))` — focus crossfades instead of snapping (`1e75be1`, 2026-04-22) |
| MB-93 | Android | Low | `HomeContent.kt:126` — `continueWatching` / `favorites` / `recentlyAdded` are `mutableStateListOf` mutated from multiple `LaunchedEffect`s; no data race per se in Compose, but sequencing is non-obvious and makes mid-load reads surprising | **Fixed** | 2026-04-22 | All clear+addAll pairs wrapped in `Snapshot.withMutableSnapshot`; no recomposition fires between clear and addAll — rails never flash empty during refresh (`5e053fe`, 2026-04-22) |
| MB-94 | Android | High | Leaving Live TV for Movies/Series keeps the last live channel playing in the preview/hero, so the new tab inherits live playback instead of showing its own poster or preview | **Fixed** | 2026-04-22 | `HomeScreen` stops live preview on any non-LiveTV section, and `BrowseShell` only gives `MiniPlayer` ownership to the focused card; covered by `BrowseShellLogicTest` (2026-04-22) |
| MB-95 | Android | Critical | Heavy churn through channels, tabs, and fullscreen player can crash; player attach/detach churn and uncancelled EPG/paging jobs confirmed | **Fixed** | 2026-04-22 | EPG `LaunchedEffect` key changed from `(type, items.size)` to `(type)` — no more restart on every paginated page; `snapshotFlow` inside handles item additions. MiniPlayer `clearVideoTextureView` on dispose (prev session) eliminates attach/detach crash path (2026-04-22) |
| MB-96 | Android | Medium | First entry into Live TV, Movies, or Series briefly renders the empty-state copy ("no videos") before the initial SQL/page load completes | **Fixed** | 2026-04-22 | Added `hasLoaded(type, group)` flag in BrowseShell; empty state only renders after first page finishes loading (`febe3e2`, 2026-04-22) |
| MB-97 | Android | Medium | BrowseShell `visible` list recomputed on every recomposition (EPG ticks, nowSeconds, sourceName changes) — O(n) parental filter running needlessly | **Fixed** | 2026-04-22 | `visible` wrapped in `remember { derivedStateOf { applyParentalFilters(...) } }`; Compose now tracks only items/hiddenIds/hideAdultContent as snapshot dependencies (2026-04-22) |
| MB-98 | Android | High | Series category selector disappears after opening a series detail and returning; the selected chip does not paint until pressing OK, and selector behavior needs a full cross-screen audit | **Fixed** | 2026-04-22 | Browse entry focus now attaches to the selected chip leaf instead of a non-focusable wrapper; category/rail lazy `focusRestorer` removed to avoid stale pinned focus state, and chip index math counts the separator row (2026-04-22) |
| MB-99 | Android | High | Backing out of a series episode leaves audio playing when navigating to other tabs; triggering a preview in another tab crashes the app (MiniPlayer dangling TextureView) | **Fixed** | 2026-04-22 | Three-part fix: (1) HomeScreen stops ALL playback (not just LIVE) on section change; (2) BrowseShell stops on group/type change; (3) MiniPlayer `onDispose` calls `clearVideoTextureView` to prevent dangling surface reference (2026-04-22) |
| MB-100 | Android | Critical | Movie → Series Episode → Live TV crashes app with `SQLiteConstraintException: FOREIGN KEY constraint failed` from `WatchHistoryRepository.upsert` during `PlaybackController.persistResumePoint` | **Fixed** | 2026-04-23 | Episode sessions wrote `watch_history.content_id = episode.id`, but episodes live in their own table — FK to `content(id)` failed. Fix: track `_currentEpisode` in PlaybackController; persistResumePoint writes `contentId = seriesId, episodeId = ep.id`. Both FKs satisfied; per-episode resume rows are unique (`wh:<seriesId>:<episodeId>`). Surfaced now because MB-99's stop-on-section-change made the persist path fire more aggressively (2026-04-23) |
| MB-101 | Android | Critical | Any DB write failure during `WatchHistoryRepository.upsert` crashes the entire process — no try/catch around the persist coroutine | **Fixed** | 2026-04-23 | `upsert` now wraps the SQLDelight call in try/catch; logs via Kermit and swallows. `CancellationException` is re-thrown so coroutine cancellation still propagates. Resume points are best-effort telemetry, not a load-bearing invariant (2026-04-23) |
| MB-102 | Android | High | Episodes were never persisted to the `episodes` table — `watch_history.episode_id` would FK-fail against `episodes(id)` even after MB-100's fix | **Fixed** | 2026-04-23 | Added `upsert` query to `Episodes.sq`; `ContentDetailService.load` now persists every fetched episode after a series detail loads. Idempotent batch with per-row `runCatching` so a malformed episode doesn't block the rest (2026-04-23) |
| MB-103 | Android | Medium | Tapping a Series row in Favorites opened an empty fullscreen player — `controller.play(series, …)` no-ops on series (toPlayable returns null) but `PlayerLauncher.launch` still fired | **Fixed** | 2026-04-23 | `FavoritesScreen` now takes `onOpenDetail` and routes Series taps through HomeScreen's detail overlay, mirroring the Movies/Series branch in `HomeScreen.onBrowseActivate`. Live and Movie rows still go straight to the player (2026-04-23) |
| MB-104 | Android | Medium | Switching from Live TV to Movies/Series after browsing channels showed stale channel list under the new category chips | **Fixed** | 2026-04-23 | `derivedStateOf { applyParentalFilters(items, …) }` was keyed on nothing, so its lambda kept observing the original `items` SnapshotStateList; `items = remember(type) { … }` allocated a NEW list on type change but the derivedStateOf never rebound. Re-keyed `remember(type) { derivedStateOf { … } }` so it tracks the current items reference (2026-04-23) |
| MB-105 | Android | Low | Misleading comment in `HomeScreen.onPlayEpisode` claimed "uses the real episode id as the content_id in watch_history" — that's the broken invariant from MB-100 | **Fixed** | 2026-04-23 | Comment updated to reflect the post-fix behavior: content_id = seriesId, episode_id = ep.id (2026-04-23) |
| MB-106 | Android | Medium | BACK / detail-close / `onExitToSidebar` requested focus on the sidebar wrapper Column; in Compose 1.7 the descendant focus search was unreliable, so the active row's `MutableInteractionSource` never flipped — no focused gradient or ring until the user nudged the D-pad | **Fixed** | 2026-04-25 | Two passes: v1 attached requester to row's outer Box (still wrong target node); v2 binds requester directly to the focusable inner Row via `.let { base -> if (focusRequester != null) base.focusRequester(focusRequester) else base }.focusable(interactionSource = …)`, with the binding rule extracted to `bindActiveRowFocus(section, current, requester)` and pinned by `AppSidebarFocusBindingTest` (`be3d413`, hardened 2026-04-25) |
| MB-107 | Android | Medium | Parental tab content overflowed the viewport with no scroll — toggles + hidden-channels list got clipped, no way to reach them with the D-pad | **Fixed** | 2026-04-22 | Wrapped tab body in `verticalScroll(rememberScrollState())` outer + bounded `heightIn(max = …)` inner LazyColumn for the hidden list; D-pad scroll now reaches every row (`29fe656`, 2026-04-22) |
| MB-107a | Android | Medium | Settings toggle rows gave no visible focus indicator on Fire TV — focus reached the row but the Material3 Switch's halo was invisible against the raised background, so the user couldn't tell which toggle they were on | **Fixed** | 2026-04-22 | Extracted shared `SettingsToggleRow` composable: Row owns input via `.clickable(interactionSource)`, Switch is display-only (`onCheckedChange = null`), focus state drives an accent border (`width = if (focused) 2.dp else 1.dp, color = palette.FocusRing`). Same pattern inlined into `GroupRow` in SettingsGroupsTab (`8e77a37`, 2026-04-22) |
| MB-108 | Android | High | Settings sidebar tab CENTER didn't auto-focus the content pane — pressing CENTER on a different tab swapped the body but left focus on the sidebar; user had to press RIGHT manually after every tab change | **Fixed** | 2026-04-25 | v1 used `focusGroup + focusRestorer + PlacedFocusAnchor` on the content pane — sometimes lost focus entirely (bounced back to sidebar) because the fresh `key(current)` mount had no last-focused child for restorer to land on. v2 simulates D-pad RIGHT via `LocalFocusManager.moveFocus(FocusDirection.Right)` after `withFrameNanos` (mimics the manual press exactly). Hardened: now waits TWO frames (composition + layout pass), retries once if `moveFocus` returns false, logs a warning to Logcat (`SettingsScreen` tag) on persistent failure so Fire TV regressions are diagnosable (`be3d413`, hardened 2026-04-25) |
| MB-109 | Android | High | Empty / read-only Settings tabs (About, Shortcuts, all 5 placeholder bodies) had ZERO focusable children — `moveFocus(Right)` from the sub-sidebar always failed, focus stayed on the sidebar, the user saw the body update but the app silently refused to enter the pane. Real layer-level root cause of the symptom MB-108 was patching at the wrong layer | **Fixed** | 2026-04-25 | Added `FocusableSpacer` primitive (0-dp `.focusable()` Spacer) to `ui/focus/PlacedFocusAnchor.kt`; dropped one as the first child of `SettingsPlaceholder`, `SettingsAboutTab`, and `SettingsShortcutsTab`. CENTER-into-tab now reliably lands focus inside the pane on every tab; LEFT returns to sidebar. Network/Parental/EPG/General/Playback/Groups already had real focusable controls and didn't need the trap (2026-04-25) |
| MB-110 | Android | Medium | Settings selector chips (`Chip` / `ResizeChip` / `TypeChip` — three duplicated definitions across General / Playback / Groups tabs) had no `.focusable()`, no shared `MutableInteractionSource`, and no focus-aware visual state. On Fire TV the chip row was unreachable via D-pad in some focus paths, and even when focus did land there was no border/colour change to show it | **Fixed** | 2026-04-25 | Extracted shared `SettingsChip` composable (focusable via `.clickable(interactionSource, indication = null)` so it self-registers as a focus target; visible 2dp accent border on focus; pure `chipColors(palette, selected, focused)` helper + `SettingsChipColors` data class for unit-testable colour rules — `focused` border wins over `selected`, `selected` background wins, foreground `TextPrimary` for focus-or-select). Replaced all three call sites; deleted the three private composables. Locked in by `SettingsChipColorsTest` (6 cases covering each rule) (2026-04-25) |
| MB-111 | Android | Medium | `HomeScreen` non-browse content panes (Home / Settings / Guide / Favorites / Search) wrapped their screens in `Box(modifier = Modifier.weight(1f).focusRequester(mainContentFocus))` with no `.focusable()` and no `.focusGroup()`. Compose 1.7's descendant-focus search through a bare wrapper-Box requester is unreliable when the first focusable child isn't placed yet — `mainContentFocus.requestFocus()` could silently no-op. Same class of bug that MB-106 v1 hit on the sidebar | **Fixed** | 2026-04-25 | Added `.focusGroup()` to all 5 wrapper Boxes so each becomes a deterministic descendant-focus search root; the requester now reliably forwards focus to the first focusable child once placed. Single shared comment above the Settings branch documents the pattern for the other four (2026-04-25) |
| MB-113 | Android | High | Sidebar focus indicator looked like "two shapes" — chunky-chamfered tile in the collapsed (92dp) state, elegant long pill in the expanded (260dp) state — even after MB-112 made the FRAME exclusive to focus. Two coupled root causes: (1) `CutCornerCardSmall` uses an *absolute* 16dp cut, which is 23 % of width on the collapsed row and only 7 % on the expanded row — same shape primitive, very different visual character. (2) `Column.focusRestorer()` was layered on top of the explicit `activeRowFocus` binding from MB-106; on BACK / LEFT into the sidebar both ran, and during the COLLAPSED→EXPANDED width animation the layout was in flux long enough that the restorer's "land on last focused descendant" and `activeRowFocus.requestFocus()` could land out of order, leaving the row focused but the `interactionSource` lagging until the user nudged the D-pad — same "detector won't show until OK" symptom MB-106 first surfaced | **Fixed** | 2026-04-25 | (1) Switched `SidebarRow` clip + border from `YancoShapes.CutCornerCardSmall` to `RoundedCornerShape(10.dp)`. Cut-corner aesthetic is preserved everywhere else (chips, cards, heroes) — only the sidebar row, which morphs width, opts out. (2) Removed `Modifier.focusRestorer()` from the sidebar `Column`. The restorer was redundant: the explicit `activeRowFocus` already binds to the *current section's* row by construction (via `bindActiveRowFocus`), and "last focused" inside the sidebar is identical to "current section" because the only way to leave the sidebar with a different row last-focused is to click that row, which simultaneously updates `current`. Stripping the restorer eliminates the race; one explicit binding now drives every focus return (2026-04-25) |
| MB-112 | Android | High | Sidebar / category pills / Settings chips painted nearly-identical accent borders for both `focused` (FocusRing) and `selected` (Accent at low alpha) states. At Fire TV viewing distance (~3 m) a thin coloured outline reads the same regardless of which accent token it uses — user could not tell whether the cursor was on the row or the row was simply the breadcrumb of where they came from ("the category and the live tv will be showing a selection which is proper... but they are the same color"). Also: per-tab `FocusableSpacer()` from MB-109 was dropped at the top of every Settings tab body's `Arrangement.spacedBy(...)` Column, which added the spacedBy gap between the 0-dp spacer and the first heading — every tab's content pushed down 12-16dp from the breadcrumb | **Fixed** | 2026-04-25 | Two-part fix: (1) **Frame = focus, fill = selection**, applied uniformly. `AppSidebar.SidebarRow`: `border = if (focused) 2.dp FocusRing else 0.dp Transparent`; selected row keeps its left-edge accent rail + gradient wash but loses the border. `CategoryChipBar.Chip`: same — focus owns the 2dp ring, selection has only the soft Accent gradient + the leading pip. `SettingsChip.chipColors()`: `border = if (focused) palette.FocusRing else Color.Transparent` — was previously `selected -> Accent.copy(alpha=0.55f)`. (2) **Hoisted `FocusableSpacer`** out of the per-tab Columns up to `SettingsScreen.ContentPane` Box where it sits as a sibling of `key(current) { TabContent(tab) }` — the Box overlays children rather than spacing them, so the trap is layout-neutral and one trap covers all 14 tabs. Locked in by `SettingsChipColorsTest.onlyFocusedPaintsBorder` (truth-table guard) + the renamed `selectedIdleHasTintedBackgroundAndNoBorder` so future "small visual polish" PRs can't put an accent ring back on idle-selected (2026-04-25) |
| MB-114 | Android | High | Closing the in-process detail / search overlay or the parental-PIN gate from a coverflow section did not return focus to the coverflow — focus dropped to the page root (no visible cursor) or stayed on a stale node. `HomeScreen` requested focus on `mainContentFocus` after the overlay closed, but that requester was only bound to the non-browse content panes' wrapper Boxes; in browse sections the requester wasn't bound to any node, so `requestFocus()` silently no-op'd. Same root cause class as MB-111 / MB-106 (requester pointed at the wrong node), but for in-process overlay closes specifically — the existing `windowInfo.isWindowFocused` handler in CoverflowSectionScreen catches Activity-level transitions (player launch / IME / external dialog) but not Compose-internal overlays, which never flip the OS window-focus bit | **Fixed** | 2026-04-25 | Receiver-side fix in `CoverflowSectionScreen`: added a `LaunchedEffect(restoreFocusOnWindowRegain)` that detects a `false → true` transition of the flag and calls `firstItemAnchor.awaitAndRequest()`. `restoreFocusOnWindowRegain` is the existing HomeScreen-driven flag (true when no overlay is mounted: `detailItem == null && !searchOverlayVisible && pendingPlay == null`) — the flag flipping back to true is exactly "an overlay just closed, give focus back." Initial-mount safe via `prevRestore` seeded from the current flag value (no fire on first invocation, no race with the orb's own bring-focus-to-orb-on-mount path). Only fires for in-process overlay closes; OS-level window-focus regains are still handled by the existing `windowInfo` handler. Side effects audited: only browse sections mount this composable; detail / search / PIN-gated play all originate from coverflow user actions so returning focus to the orb is the expected destination (`CoverflowSectionScreen.kt`, 2026-04-25) |
| MB-115 | Android | Low | `ContentDetailScreen` Play-button focus restoration on window-focus regain used the delay-ladder anti-pattern (`for (delayMs in longArrayOf(80L, 250L, 500L)) { delay(delayMs); requestFocus() }`) — flagged in the `native-android-mk` skill checklist as race-prone since it requests focus on fixed timeouts rather than after `onPlaced`. In practice the Play button was usually focused by the third tick, but the fallback chain is exactly the kind of "silently failed in production" pattern PlacedFocusAnchor was introduced to replace | **Fixed** | 2026-04-25 | `playAnchor` (a `PlacedFocusAnchor`) was already in scope at the top of the screen; replaced the delay-ladder with `playAnchor.awaitAndRequest()` inside the `snapshotFlow { windowInfo.isWindowFocused }` collector. Strictly more deterministic — fires after the next `onPlaced` callback rather than blind-firing at 80/250/500 ms. No behavioral change on the happy path; eliminates the "first two ticks no-op" warning noise on slower Fire TV hardware (`ContentDetailScreen.kt`, 2026-04-25) |
| MB-116 | Android | High | Settings tabs that are read-only by design (`About`, `Shortcuts`) and the placeholder bodies (`Storage`, `Notifications`, `Recordings`, `EPG`, `Subtitles`, `Appearance`) silently failed D-pad scrolling. Each Column had `verticalScroll(rememberScrollState())` but no focusable child, so the focus from the sub-sidebar's `moveFocus(Right)` landed on `ContentPane`'s sibling `FocusableSpacer()` (per MB-109's hoisted-trap fix) — that spacer is OUTSIDE the scrolling Column, so D-pad UP/DOWN had no scroll target and the lower half of every page was unreachable on TV. User report: "about and shortcuts when pressing on them, it doesnt take me to the card to scroll in it and read it" | **Fixed** | 2026-04-25 | New `Modifier.dpadVerticalScroll(scrollState)` helper in `ui/focus/DpadScroll.kt`: makes the modified node focusable AND intercepts D-pad UP/DOWN via `onPreviewKeyEvent`, calling `ScrollState.animateScrollBy(±240dp)` per press. Returns `false` from the handler when scroll is at its boundary so focus can still escape — D-pad LEFT or UP-from-top still walks back to the sub-sidebar. Applied to `SettingsAboutTab`, `SettingsShortcutsTab`, and `SettingsPlaceholder` (one helper covers all three plus the 6 placeholder tabs that share `SettingsPlaceholder`). Lives next to `PlacedFocusAnchor` / `FocusableSpacer` because it's the same family of TV-focus primitives (2026-04-25) |
| MB-117 | Android | High | Every text input across Settings (`Network` User-Agent + Connect/Read timeouts, `Parental` PIN field, `Playback` audio/subtitle language fields) was a plain Material3 `OutlinedTextField`, which auto-pops the IME the instant focus lands on it via D-pad. On Fire TV that means flicking past a field with the remote opens the system keyboard with no way to dismiss without entering text — actively user-hostile. The `AddSourceDialog`'s private `ClickToEditField` already had the right pattern (focusable read-only row → CENTER flips `editing = true` → embedded `BasicTextField` then requests focus → IME opens; Done/Enter/Back collapses back) but it was not extracted, so the rest of Settings shipped the broken Material3 default. User report: "whenever the indicator goes to a writing box. the keyboard appear automatically. i want the behavior in every writing box in settings to behave that only if im on it and press ok then the keyboard appears" | **Fixed** | 2026-04-25 | Extracted `SettingsClickToEditField` to `ui/settings/SettingsTextField.kt` with two render modes: **Card** (default — wrapped in PrefCard chrome with title + optional description) for tab-level fields, and **Bare** (no chrome — `bare = true`) for fields inside dialogs. Supports `keyboardType: KeyboardType` (Text / Number / NumberPassword / Ascii / Uri / Password) and `transformation: VisualTransformation` (None / `PasswordVisualTransformation`) so every existing call site swaps in 1:1. Migrated all 6 broken sites (`SettingsNetworkTab` ×3, `SettingsParentalTab` ×1, `SettingsPlaybackTab` ×2) plus the 6 dialog sites in `AddSourceDialog` (which now uses the shared composable in bare mode instead of its private copy). The IME is now physically impossible to open without an explicit OK press on the field. `LaunchedEffect(editing) { if (editing) runCatching { editFocus.requestFocus() } }` is the gate — it only requests focus after `editing = true`, which only flips on the read-only row's onClick (2026-04-25) |

## How to use this register

- **Adding a bug:** append a row with the next MB-NN (mobile) or DB-NN (desktop) ID. Keep summary to one line; details go in the fix commit.
- **Fixing a bug:** flip Status to **Fixed**, add commit ref to the Fix target column, and remove the row on the next doc pass once the fix has stabilized.
- **Dumping a refactor-wide set of bugs:** don't list them here. Create `docs/incidents/YYYY-MM-DD-<topic>.md` and link it from the Status column (e.g. `Open — see [incident](docs/incidents/...)`).

## Cross-references

- [docs/incidents/2026-04-16-html5-player-refactor.md](docs/incidents/2026-04-16-html5-player-refactor.md) — desktop HTML5 refactor bug dump (bugs 1–29, archived)
- [PRODUCTION_PLAN_ANDROID.md](PRODUCTION_PLAN_ANDROID.md) § Reboot Notice — M4R rebuilds the mobile shell; MB-13/15/17/19–23 are all scoped there
- [CLAUDE.md § Native Android — Lessons from MK.8 Audit (2026-04-21)](CLAUDE.md#native-android--lessons-from-mk8-audit-2026-04-21) — rules distilled from MB-35 through MB-63. Apply before every future MK.* commit.
