<?xml version="1.0" encoding="utf-8"?>
<!--
  MK.10.3 — searchable activity descriptor for voice search.

  `voiceSearchMode="showVoiceSearchButton|launchRecognizer"` tells the
  system to wire up a voice button in any launcher search widget that
  surfaces this activity, and to route Google Assistant / Fire TV voice
  remote queries to MainActivity via ACTION_SEARCH.

  No suggestion-provider authority — we don't ship a SearchableInfo
  ContentProvider yet; results are resolved inside the app via the
  shared FTS index when the SEARCH intent lands.
-->
<searchable
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:label="@string/app_name"
    android:hint="@string/search_hint"
    android:voiceSearchMode="showVoiceSearchButton|launchRecognizer"
    android:voiceLanguageModel="free_form" />
